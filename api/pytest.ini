[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py
addopts = -q
markers =
    postgres_integration: requires a real PostgreSQL server and multiple database connections
    celery_redis_integration: requires real Redis plus external Celery worker and Beat processes
