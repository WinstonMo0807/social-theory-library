# 3.0.6 补漏与最小发布验证

2026-09-20。最新授权为完成8项缺口、最小测试、失败修复后直接公网部署，替代先前禁止测试/部署安排。LoRA不在本轮范围。

## 最终交付状态

部署证据已归入NAS本轮deploy-record；本轮隔离恢复数据库副本和两个停止的封面测试容器已清理，生产数据不变。需要重新演练可从保留的校验备份恢复，回退镜像/源码/环境和系列部署访问保留。

本轮8项补漏及封面追加均已上线[公网3.0.6](https://books.winstonmo.com)。应用冻结树59e73c410842fe46f28cac6517450c6b1ee12ca9，归档b2ee98dfcbd2c1d88d71e44861c0e4abb9388be9e8f35dffaac892524c39b5c8。API/Web均3.0.6-59e73c4108，实际ID分别451332d717b4aefc7152d4b8c4ce605fdfe5aa86a10df3cca032f473b71c4e36、26da357d57f435d066b039b8cabc1cedad75d8e5f4c884a639fe9354e2dd702b。运行文件与daf743c276编译输入逐文件比较相同；两项API测试事务标记更新包含在最终API镜像，Web编译树另有明确标签，不以重新贴标签冒充新前端构建。

切换后另发现网关默认2MB不支持说明中的12MB封面，已只对Edition cover接口配置13MB（含表单开销），Django仍限制图片12MB。Edge模板独立热修复SHA256为387452affa31a00779cb1b25c22ce81e617067a8a3a5215c8802cdab4a2c6424，不包含在上面的应用源码归档；部署记录单独保留模板与校验。原其他接口2MB、鉴权、限流及代理头保留。真实nginx候选及活动配置检查通过，公网3MB合成请求封面401、其他API413，未写入真实馆藏。

封面最终Linux/PostgreSQL16项通过（0失败/0跳过/109.259秒），SQLite16项、真实封面浏览器流程、390/1280布局、三态PDF五宽度、两项网关/存储契约通过；生产构建/类型/Django检查及无迁移漂移通过。公网17项探针全部通过，另浏览器真实Reader390/1280画布和翻页通过，无横向溢出/坏图片。匿名认证探针401/刷新400是预期未登录反馈，不计为页面功能错误。

新增封面无migration；基础版已执行0055/0056。生产原件、Page、私人记录关系、暂停OCR及活动语义索引前后摘要相同。未提交/推送Git，保留用户真实暂存区。新备份已恢复验证，回退见DEPLOYMENT/rollback。下文“准备中”是过程记录，以上结果优先。

证据：output/verification/v306/cover-*、output/playwright/v306-cover-{390,1280}.png、v306-public-reader-{390,1280}.png。测试只用合成PDF/图片与隔离库；公网仅读取及未认证合成请求。真实管理员生产图片写入、真实外部模型/OCR与整版全量验收没有重复执行，不冒称通过。

## 实际改动

| 缺口 | 实现与边界 | 本轮证据 |
| --- | --- | --- |
| 建议未随当前填写更新 | curation.py、field_enrichment/service.py按允许字段与对象隔离条件；显式重新查找；不发送私人字段 | 合成候选API，不冒充外部Provider |
| 知识列表只能看首批 | 学科、子学科、理论、学者、主题、阅读路径稳定分页及URL页码/筛选 | 四类26条遍历API；前端构建 |
| 按钮修改未触发未保存保护 | admin-sections.tsx统一草稿setter；单独操作不再隐式保存整页 | tsc/build；显式保存保留 |
| 主题图片失败后文字重复提交 | 文字保存后立即保留ID/版本；图片失败单独反馈并保留待重试文件 | 源码路径核对/构建；未额外运行图片失败浏览器场景 |
| 学科图片未纳入草稿媒体 | 0056两个可空受保护媒体引用；复用原修订/媒体历史；实际公开接口统一读取 | 上传、私有预览、发布、清除、历史和公开接口API |
| 推荐没有真实发布前预览 | 同一选择规则包含自动补足；签名绑定用户/版本/名单，确认后发布同一组 | API防篡改/幂等；浏览器取消/重复点击/实际公开名单 |
| 健康轮询反复检测服务 | 读取HealthCheckRun，过期未知；新检测须显式操作 | GET无探针、空/正常/过期API |
| 修改后采用和移除统计不清 | 用户明确标记保留修改/不再使用；原审计事件区分未分类修改；无成本不估算 | 使用事件计数API和构建 |

OCR新功能同时纳入最小API回归：已发布/未发布版本、幂等、权限/文件版本、逐页保存及恢复。提取返回受控样例，本轮未对生产PDF发起重新OCR。

## 本轮结果

- 48项隔离PostgreSQL接口通过；静态文件目录警告无断言失败。
- 推荐时间格式修复后3项聚焦SQLite API通过；3条真实浏览器流程通过，正常登录与实际写入隔离库。
- 20项前端单测、tsc、生产构建、Django check、makemigrations --check --dry-run通过。
- 已修复fixture参数/必填值和旧按钮定位；真实产品缺陷为推荐确认按钮因时间格式被错误禁用，已修复并补断言复测。
- 日志output/verification/v306/completion-*；失败浏览器记录completion-browser-red。不将历史测试计入本轮数字。

## 基础更新已上线，封面补充正在验证

基础8项已安全切换至3.0.6冻结树8fae6082c1ca2388f191855f3167e2702e2b9266；API/Web均为3.0.6-8fae6082c1。公网就绪、首页/探索/作品/阅读器/登录/理论页、PDF Range206、语义v2_hybrid非回退、阅读定位及匿名私人清单拒绝已通过。Archive SHA256为ddc027e904b4158507db4bd9655472d7de0a4b9cfeff32fec660c17c9f3cdce0。

追加封面采用现有本地PyMuPDF页面渲染与候选排序，参考[Calibre PDF元数据实现](https://raw.githubusercontent.com/kovidgoyal/calibre/master/src/calibre/ebooks/metadata/pdf.py)的直接渲染思路，以及[PyMuPDF页面图像接口](https://pymupdf.readthedocs.io/en/latest/recipes-images.html)；没有复制Calibre实现，不依赖OCR或新增外部AI服务。不是宣称准确率已验证的视觉模型。

PDF入队后仍须等待worker和安全文件校验，完成归档即准备前部页面候选，早于整本文字提取、OCR和AI书目填写。当前Edition页自动读取，默认前三项，可展开全部；管理员可指定任意实际PDF页、上传图片或明确不选封面。文章不强制封面。推荐卡片配图独立折叠，不再混作封面入口。

新增EditionCoverView复用CoverCandidate、CatalogFieldDecision、媒体、EditorialRevision及AuditEvent回执；无新migration。文件指纹/角色/版本校验、幂等、防重复点击和并发刷新不覆盖选择；候选缩略图私有。已公开馆藏封面修改仅进入草稿，正式发布前旧封面保留；原件/Page/历史图片不删除。当前新增封面最小回归和第二次部署仍在进行，不将下面基础更新证据冒充封面已上线。

## 备份与边界

新增封面在Linux候选API镜像和独立PostgreSQL复跑16项全部通过。首次13通过/3失败均为测试关闭流式图片响应时关闭pytest外层事务连接；三项改为真实独立事务后原断言全通过，未改业务代码。候选API/Web真实构建源为daf743c276d88c873dffe6713ef1f0437a633222；最终仅测试/文档更新可在逐文件证明运行输入相同后复用该编译结果。新备份58e0ecf6-348b-48dd-bac6-5b056631e7b6、SHA256 39b1c88a4ac1e61453fe974666996fecd117641f0ae2a45e54068704e206f653已恢复到stl_v306_cover_rehearsal，迁移0、新旧ORM及保护摘要一致。

封面补充最终本地：16项SQLite API、1条真实封面浏览器流程（候选重开可见/任意页/上传/默认/390和1280布局）通过；另1条三态PDF在五宽度回归通过。tsc、生产构建、Django check和迁移漂移检查通过。浏览器实际发现已存封面图片URL重复/api，已修复并补API图片读取断言。失败证据保留，截图output/playwright/v306-cover-390.png、v306-cover-1280.png；未对真实馆藏发起OCR或外部模型调用。封面补充没有schema变更。

备份3273ca45-56ae-40e8-baa6-e2a2faa5c99d，SHA256 a99deab8896900f21ba5b11b61e4f11ad4e4c0a3191955a02f7bd881343adcad，已在隔离PostgreSQL恢复。保留旧源码/环境/Compose/3.0.5镜像。基础切换迁移0055/0056、旧ORM与受保护数据前后摘要均已通过；记录位于NAS storage/backups/pre-v306-20260920-completion/deploy-record。

只通过0055/0056标准迁移，不删除生产数据、不重建索引或Page。原文件、Page、私人记录关系、暂停OCR和活动索引须前后摘要一致。未提交/推送Git，从临时索引冻结当前开发，不改变用户真实Git索引。

本轮为用户授权的最小发布验证，不等于全量A01—A32、全后台角色、五宽度全路由或真实外部Provider/OCR重验。
