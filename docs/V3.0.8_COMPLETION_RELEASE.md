# Social Theory Library 3.0.8 交付与发布证据

记录日期：2026-09-22，Asia/Hong_Kong。

**当前状态：3.0.8及追加OCR/检索布局修复已完成开发、验证、源码推送和公网部署；全库初始索引已激活，真实检索可返回原文。原失败OCR已从4页继续并实际保存第5页，正在后台继续。** 源码、镜像与部署分别记录，不宣称整书OCR已经完成。


## 追加排障与体验修复（2026-09-22）

首次全库构建尚未激活时提前切换应用，导致公开查询返回index_pending和空结果。该部署顺序是本次可用性缺口；不是PDF丢失。现generation `8e62c8bb-59a7-4104-8388-ae43f0e19a59`已active，15个来源完整成功，4483条文档全部确认（4472原文、11知识），16项协调/来源任务成功。原有活动语义索引保留。

公网实测普通查询返回195段原文、11知识、0策展；扩大后400段、原可见顺序保留。14项功能检查覆盖权限令牌、真实上下文、Reader定位、分页、一次扩展、旧接口兼容及运行取消，均通过。该次脚本在最后写报告时因未提供输出参数退出1，不能写作脚本退出0；实际功能输出重建为`production/public-discovery-observed.json`，只修结果写入默认路径，未重跑全部通过项。用户复现查询返回196候选，默认显示3段、定位有效，三通道completed、无警告；后台实测18.0秒。没有相关性人工标注集，以上不是准确率或架构性能优越性证明。

索引由书库NAS自身维护。来源变更在事务提交后去抖派发，独立Celery索引队列处理，Beat每300秒协调补漏；只对变化来源增量处理。初次bootstrap是一次安装动作，不要求以后由Codex逐书编译。另修正原样重新发布的来源被superseded旧任务挡住的边界，提交81ae200的15项定向检查通过；该候选API镜像构建但未切换，合并入后续486af13发布。

OCR故障定位到处理中心历史失败任务。新增独立OCR控制区，入口为“处理中心 → OCR 与文档 → OCR 识别与重试”；可筛选失败任务并直接重试未完成页，也可安全暂停/继续。每2秒查询实际持久化页数，单页计算期间不虚构进度；重试保留任务、文件、已完成页及历史审计。管理员明确点击会开启新的有限尝试周期，自动尝试上限仍为3，过期文件、永久错误、权限与全局暂停保护保留。

OCR断点实跑已验证：原失败任务从4/290页推进到5/290页，第5页实际写入OCR文字和2个文字块，监控读模型显示1.7%，NAS22:41:32观察时正在识别第6页。当前attempt为1/3，旧失败及3次尝试保留在审计，另外6项暂停任务保持原状。第5页本次约5分37秒，NAS单页较慢；界面每2秒读取已保存页数，不伪造页内百分比。整书仍在后台处理，不能写成290页全部完成。证据为`production/ocr-recovery-proof.json`。

附加只读证据脚本首轮误假设旧任务也有all_pages专用completed_page_indexes字段而退出1；改用实际旧任务的Page来源分类及共用剩余页函数后通过，未修改生产任务或放宽原页数/文字断言。登录后的浏览器点击验收未执行，生产重试经相同应用服务发起；权限在定向测试及匿名公网接口中核验。

观点界面恢复3.0.6的左侧筛选、紧凑原文卡片、右侧辅助区和排版比例；保留3.0.8原文/知识/策展三通道、上下文、分页、扩大和取消，不恢复已退役的自动立场分组。

| 追加验证 | 实际结果 |
| --- | --- |
| OCR原控制与新监控/恢复 | 首组20项通过；额外单页断点1项通过 |
| 手工尝试上限修正影响范围 | 14项通过，0跳过；`ocr-manual-retry-final.xml` |
| Web tsc、build | 退出0；`web-ocr-layout-tsc.log`、`web-ocr-layout-build.log` |
| 受影响Web lint | 0错误、1原有img性能警告 |
| 检索契约与SSR | 分别3项、2项通过；`web-layout-tests.log`、`web-layout-ssr.log` |
| Django check、迁移漂移 | 退出0、没有新增迁移 |
| NAS新OCR用例、候选HTTP | 9项PG风险用例通过；API/Web HTTP通过，Web仅复跑失败项 |
| 部署后影响验收 | 8项公网检查通过；旧失败OCR实际保存新页并继续，进度读模型一致 |

修复源码为`486af131405cc1100f5093747f33cf807063c481`（含d77b726与81ae200），Git tree `e8bdf0bc2d0350bf92b56ffc56100003936a2653`；1326文件归档SHA `ea56cb628f441691f419b6c01a97ff42fdae7c269ba2c0e81ce07034ee0ab5c9`。已正常推送main。新BackupJob `273fac46-8240-446a-8006-2624cabb02fc`，33932324字节，归档SHA `88896007ab653723fe1ca97c081fb4600fd89af4e435d959601fa45dc01796d8`。NAS隔离PG9项OCR测试及API就绪通过；候选Web首轮因测试API缺少自身主机名返回500，修正该准确配置后仅复跑Web，HTTP200、21963字节且三区/侧栏结构通过。生产配置没有作该候选修正，已通过的9项未重复。

## 本批修复的实际镜像与公网结果

NAS记录`2026-09-22T22:31:39+08:00`，修复应用`486af131405cc1100f5093747f33cf807063c481`切换退出0，未触发应用回退。API/四Worker/Beat和Web均来自该准确提交，Inference保留e02b57013f镜像；切换后重启计数均0。没有新迁移，原件、人工确认、私人数据摘要及活动discovery索引前后相同，Cloudflare、PG、Redis、Meili和Inference容器身份未变。

| 服务 | 镜像标签 | 完整ID |
| --- | --- | --- |
| API/四Worker/Beat | `social-theory-library-api:3.0.8-486af13140` | `sha256:b101b92fc3d41b829751785f340d8e615340822a505dda38bb2ee779aae6cfa4` |
| Web | `social-theory-library-web:3.0.8-486af13140` | `sha256:b71a56adf53b782b24e54423d35c602721bf656242044a8e27d02f1be0cc3acc` |
| Inference（保留） | `social-theory-library-inference:3.0.8-e02b57013f` | `sha256:e33888d74b1a441b5f54543dc32cc7e4a5b5bde815c0fa83028eab2d1315a65c` |

本批8项公网影响检查全部通过，脚本退出0：新三栏页面、OCR监控匿名拒绝、真实异步查询、活动索引保留、当前原文修订上下文、Reader定位及PDF Range206。查询返回197段候选、11知识、0策展，用时34.41秒（含公网轮询）；不与先前用户复现的后台18.0秒混为同一计时口径。证据为`output/verification/v308/production/recovery-public.json`。

切换首轮被8月历史备份残留状态挡住，镜像未替换，Beat自动恢复。确认其准确身份/时间戳及所有运行队列静默后，只读分类并保留原记录；重试切换退出0，无应用回退。首轮与重试日志分别保留，不把失败写成首次成功。新回退入口是`storage/backups/recovery-20260922-ocr-layout-final/deploy-record/rollback.sh`，保留原e02 API/Web、环境、源码和已经完成的活动索引。旧3.0.7整版回退入口仅作历史保留。

## 初次3.0.8部署的源码与交付范围

| 项目 | 已核对值 |
| --- | --- |
| 应用源码提交 | `e02b57013f53931519ae49831cc2bac6911d7925` |
| Git tree | `8af0dcc9058cad78acba1e949b9cdff3c721f6ec` |
| 分支与远端 | `main`，已正常推送 `WinstonMo0807/social-theory-library`，远端 SHA 核对一致 |
| 发布归档 | 1,324 个源码文件 |
| 归档 SHA-256 | `1e138aa1ffcdc8f494c4eee63238ad03117a184f39b8b21c0e67b8311b49118d` |
| 暂存源码安全扫描 | 1,324 文件，无禁止路径或凭据命中；diff 检查通过 |
| 本文记录提交 | 事后文档另行提交；应用镜像源码身份仍按e02及上述486af13分别标识 |

本轮接入原文、知识入口、策展三个独立检索通道；异步查询、稳定游标、扩展与取消、相邻原文上下文均使用真实接口。检索结果在返回与上下文读取时重新核对权限、公开修订和文件定位。Processing Center 展示真实文字、关键词、向量与任务状态；本地编码和重排共用有界 CPU 推理服务。旧语义索引、PDF Reader、人工确认、私人笔记及既有文件版本保留。

界面沿用 3.0.7 黑、白、暖灰排版。开发包八个原始参考文件保留在本地输入目录并忽略，未加入公开 Git。详细实现和接口边界见 [实现说明](V3.0.8_IMPLEMENTATION.md)、[查询会话说明](V3.0.8_QUERY_SESSIONS.md)及[本地推理说明](DISCOVERY_LOCAL_INFERENCE.md)。

## 集中检查与定向复跑

证据位于本地忽略目录 `output/verification/v308/`，生产备份与隔离验证记录另保存在下述 NAS `deploy-record`。下表每行是一次独立运行；同一用例在不同环境或修复后可能再次出现，**不将各行相加为独立测试总数**。后端 XML 均无跳过项。

| 环境／运行 | 实际结果 | 范围及证据 |
| --- | --- | --- |
| 本地 Django 检查 | 退出 0 | `manage.py check`；`django-check.log` |
| 本地迁移漂移检查 | 退出 0，无变更 | `makemigrations --check --dry-run`；`migration-check.log`；补 0062 后亦单独核对 |
| API 契约生成与一致性 | 均退出 0 | `npm run api:generate`、`npm run api:check`；`api-generate.log`、`api-check.log` |
| 本地新增后端首组 | 22 通过，111.992 秒 | 会话、公开投影、后台权限与任务重试；`backend-local.xml` |
| 本地旧入口兼容与模型忙碌 | 38 项中 36 通过、2 失败，54.153 秒 | 两项失败是旧测试错误响应形状断言；`backend-compat.xml` |
| 本地仅复跑上述失败项 | 2 通过，51.235 秒 | 按现有统一错误契约修断言，未放宽验证；`backend-compat-fixed.xml` |
| NAS 隔离 PostgreSQL 首组 | 83 项中 81 通过、2 失败，104.622 秒，退出 1 | 新检索、旧观点接口、旧语义索引生命周期／一致性／任务派发；同两项旧断言失败；`backend-first.xml` |
| NAS 仅复跑上述失败项 | 2 通过，83.945 秒，退出 0 | 只变更旧测试错误响应断言，未修改应用行为；`backend-fixed.xml` |
| 本地排队与运行超时风险 | 1 通过，76.686 秒 | 串行 CPU 队列等待不当作运行租约丢失；`backend-queue-wait.xml` |
| 本地旧模型 INSERT 风险 | 1 通过，47.912 秒 | 模拟旧 ORM 不发送 `index_family`；`backend-legacy-default.xml` |
| 本地激活与回退首组 | 2 通过，49.546 秒 | 总数不变时仍重核来源修订、active 回退保留原索引与资料；`backend-activation-rollback.xml` |
| 本地回退影响范围补验 | 3 通过，48.121 秒 | 分别覆盖 building、ready、active 退役与迟到任务保护；`backend-rollback-fixed.xml` |
| 本地 ready 修订更新 | 1 通过，48.689 秒 | 保留同一可见建库协调任务，避免完成状态过期及重复建库；`backend-ready-refresh.xml` |
| Web lint 首次 | 6 错误、22 警告 | 三处组件的 effect 内状态更新；`web-lint.log` |
| Web 受影响文件 lint | 0 错误、1 警告 | 修复上述错误，保留一项图片加载性能提示；`web-lint-fixed.log`；未反复全量 lint |
| Web 类型检查 | 退出 0 | `tsc --noEmit`；`web-tsc.log` |
| Web 检索用例 | 3 通过，0 跳过 | 原文定位、三通道／扩展／分页、旧范围筛选；`web-discovery-tests.log` |
| Web 正式构建 | 退出 0，Build complete | `npm run build`；`web-build.log`；NAS 正式镜像构建另行记录 |
| 构建后 SSR | 2 通过，0 跳过，47.083 秒 | 无伪造证据的三个空通道、待查询问题及旧筛选保留；`web-ssr.log` |

首轮后端失败均因测试仍读取 `response.data["q"]`，而实际 API 使用既有统一错误封装；修复测试后只复跑两个失败参数。没有删除断言、跳过用例或改宽权限。日志中的 pypinyin 弃用提示、图片加载提示及 Vinext 路由静态分类提示分别保留，未伪称全库零警告。

## NAS 真实本地模型

实际主机为无 AVX/AVX2 的 N5105。模型目录只读挂载，独立 internal 网络，无宿主端口；使用 ONNX Runtime CPU 1.22.1、两线程和 batch=1。没有调用付费服务、外部书目、外部 OCR 或外部推理 API。准备阶段下载固定修订的模型文件和离线依赖，不把下载成功作为推理成功。

| 角色 | 固定模型与修订 |
| --- | --- |
| 编码 | `intfloat/multilingual-e5-small@614241f622f53c4eeff9890bdc4f31cfecc418b3` |
| 重排 | `cross-encoder/mmarco-mMiniLMv2-L12-H384-v1@1427fd652930e4ba29e8149678df786c240d8825` |

首轮功能 smoke 退出 0，但双 tokenizer 常驻使 swap 达到 200 MiB 上限。修复模型与 tokenizer 生命周期后，仅复验有关流程，r2 的 10 次调用及断言均通过，退出 0、OOM/oom_kill 为 0、容器重启 0。

- E5 四条短文冷调用 3.983 秒，热调用 0.116 秒。
- 中英、繁简、断词换行和非 BMP 合成文字共 3,019 字，5 个原位切片完整覆盖，最大 318 token；超长输入返回 422，向量空间身份错误返回 409。
- 20 个合成候选重排冷调用 8.595 秒、热调用 1.676 秒；再切回 E5 为 4.150 秒，输出一致。最终 health 记录两种实际推理已完成。
- 调用后进程 RSS 约 836–850 MiB，采样 swap 最大 17.9 MiB；含文件缓存的 cgroup 峰值仍达到 1,600 MiB 上限并发生回收，无 OOM。这不是满负荷或并发容量承诺。

r2 验证候选镜像 ID 为 `sha256:62c11c6a0d5ba5a563472370268141b561965ad44612571b8d2ff30492e29bd3`，身份为当时未提交工作区候选，**不是正式部署镜像 ID**。`inference-final-source-comparison.json` 已核对其七个推理源文件与应用提交：六个逐字一致，`model-lock.json` 仅行尾差异。原始证据分别保留在 `inference/` 与 `inference-r2/`。

## 隔离恢复副本的真实联动

有限 E2E 使用真实恢复 PostgreSQL、真实 Meilisearch 和上述本地模型，共 14 项检查通过，退出 0，27.298 秒。限定索引耗时 16.114 秒，三通道查询与重排 8.985 秒。

样本为一份真实公开版本的三个完整原文单元（1,579 字，含 embedded 与 OCR 来源）、一位真实公开学者，以及明确标记的隔离策展 fixture；建立七条派生文档，三个通道分别返回 5、1、1 条。核对了真实编码及 Meili 提交、私人编辑笔记排除、匿名 capability 与缓存头、稳定游标、准确文件／修订／页与上下文、迟到任务拒绝，以及收紧权限或撤下后缓存结果和上下文立即不可见。权限收紧在副本事务内回滚，旧语义索引与受保护摘要不变。

首次 E2E 因脚本错误假设恢复副本必有未公开版本而失败，发生在 fixture、索引及编码之前。修正样本选择后运行成功，没有通过修改馆藏事实制造样本。该 E2E 调用真实任务函数和任务账本，但使用进程内 DRF view，**未验收真实 Celery worker 消息投递或公网 HTTP 链路**；这两项仍属于后续部署验收。

随后仅补另一份真实 PDF 的一个完整 embedded 原文单元（41 字），实际编码、Meili 提交、向量召回与精确文件／修订／页上下文通过，11.480 秒；没有重复已通过的全套 E2E。两个有限代次均已退役，隔离策展恢复草稿，临时验收服务已停止。

恢复副本只读统计为 15 个符合投影条件的来源、2,858 个原文单元、1,509,130 字；其中四个符合公开投影条件的全文版本，另有十一项知识来源，当前无真实公开策展来源。文件的 `inherit` 权限按所属作品的实际公开权限解析，不能只数原始 `access_status=public` 就推断匿名可读数量。该统计不是完整索引完成证据。

真实繁体馆藏仍有覆盖边界：对四个有效版本的 2,847 个原文单元检查指定十种繁简差异字形，未发现符合样本；这不是语言鉴定。模型级繁简合成样本通过，不能替代真实繁体馆藏验收。证据为 `e2e-result.json`、`e2e-supplement-result.json`、`corpus-summary.json`；`e2e-final-source-difference.json` 记录有限 E2E 后没有未纳入的 API 工作区增量。

## 初次整版备份、迁移与回退（历史入口）

新鲜应用 BackupJob 为 `9b028f78-ffcc-4032-9f64-397979c067a8`，NAS 17:15:06 +08:00 完成，隔离恢复于 17:16:08 完成，两者退出 0。数据库归档 30,110,398 字节，SHA-256 为 `0df9ffb29f91827017e1c795d7af4b50ed57ab68edd1078c078e9591e99deafd`。

NAS 记录目录：`/volume2/library/docker/social-theory-library/storage/backups/pre-v308-20260922-v308/deploy-record`。保留旧环境、源码、Compose、应用镜像和回退脚本；恢复数据库仅连接独立 internal 网络，现已停止并保留卷。

隔离副本已实际应用 catalog 0060、0061、0062 与 ingestion 0018。0062 为 `index_family` 增加持久数据库默认值；不仅做了新 ORM 测试，还使用真正的旧 3.0.7 镜像在副本事务中创建不含该字段的 SemanticIndexVersion，核对数据库值为 `semantic`，随后回滚且无残留，退出 0。旧／新 ORM、迁移前后及写入探针后的受保护摘要一致，范围包括原件、文件版本、页、人工锁定与确认、原文修订和定位、私人阅读数据、暂停 OCR 及旧 active 语义索引。

回退采用保留 schema 的应用回退：先停止新增 discovery 服务与旧应用写入进程，用新候选 ORM 将 discovery 的 building、ready、active 代次退役，并取消未完成 discovery 索引任务，包括尚未创建代次的协调任务；随后切回旧应用镜像和配置。派生文档、备份、原件、数据库卷与旧有效语义索引均保留，不删除或重建生产数据。激活新代次前重新核对当前来源集合、修订、完成状态及 Meili 实际数量。

首次整版切换已经成功，迁移和受保护摘要检查通过、未触发回退。该阶段保留3.0.7镜像和退役辅助脚本，用于整版回退。新增OCR/UI修复的独立回退只切回e02 API/Web，保留discovery活动代次，不执行上述整版退役流程。

## 用户授权的历史 Docker 清理

已按实际依赖及准确容器身份清理书库开发遗留的 57 个容器，其中 9 个原本运行；删除三个孤立网络。核对 12 个生产容器和七个其他项目容器的 ID、镜像 ID、重启次数均未变，全部十九个既有 Docker 卷和 210 个镜像 ID 保留。本轮恢复副本、正在使用的测试资源、运行与回退镜像保留。

清理后两个生产 worker ping 通过，公网 `/api/ready/`、`/`、`/explore/opinions` 均 200，仍为 3.0.7、数据库正常、待迁移 0；这是清理连续性检查，不是 3.0.8 最终验收。

需保留的数据偏差记录：历史测试容器 `stl-v306-postgres-20260913` 使用 1536 MiB tmpfs PGDATA。确认无客户端且本轮不再依赖它后正常停止，tmpfs 内容随停机释放；随后冷归档校验仅得到 1,536 字节空目录，**不是有效数据库备份**。空容器已在授权清理范围内删除，损失限于这份废弃历史测试库。真实生产数据库、本轮新鲜恢复副本、全部持久卷及原备份未受影响。旧测试 Meilisearch 的可写数据已停机复制并留 SHA-256 清单后再删除容器。

详细准确 ID、逐项动作及保留核对见 `nas-cleanup-report.md`、`nas-cleanup-final-verification.json` 与 `nas-cleanup-results.json`；NAS 对应记录位于 `deploy-record/nas-cleanup-20260922`。

应用切换后追加清理本轮不再使用的12个已停止临时容器与3个空闲测试网络，NAS18:21:10—18:21:15执行退出0，没有停止任何运行容器。15个现生产服务、7个其他项目、最新恢复PG容器及网络、全部21个持久卷和镜像保持不变。追加批次的1421镜像ID统计含中间构建镜像，不能与前批210个常规镜像直接比较。独立证据为`nas-cleanup-v308-final-report.md`及配套JSON，NAS副本为`deploy-record/nas-cleanup-v308-final-20260922`。两次合计删除69个容器、6个网络，分批保留准确动作与核对范围。

## 正式候选验收

`final-candidate-result.json` 记录三个正式镜像构建退出 0，Compose 配置检查退出 0，候选迁移演练退出 0。三个镜像的源码提交、tree 与归档标签均与本文冻结身份一致。

正式 API 在隔离恢复副本中完成迁移及 HTTP 就绪检查，NAS 18:05:46 +08:00 完成：版本 3.0.8、数据库正常、待迁移 0；迁移前、新 ORM 与旧 ORM 受保护摘要一致。该 ready 响应明确只验证数据库与 schema，不替代业务验收。

正式 Inference 镜像仅补验与最终镜像身份有关的两次短真实调用，未重复全套模型 smoke：`/embed` 为 5.923 秒、`/rerank` 为 6.560 秒，均 HTTP 200，向量单位范数及完整重排排列核对通过，退出 0，NAS 18:07:22 +08:00 完成。固定 artifact 与模型修订匹配，OOM/oom_kill 为 0；cgroup 峰值仍为 1,600 MiB，采样 swap 为 18,591,744 字节。正式镜像与 r2 的 app/runtime 逐字一致，其余推理源码规范化行尾后相同。

候选临时服务已停止；这份切换前证据中生产仍为 3.0.7，API/Web 重启计数为 0，未执行生产迁移或切换。随后启动的正式切换不包含在该候选通过结论中。

## 初次发布记录与后续索引验收

| 发布事项 | 当前证据状态 |
| --- | --- |
| 正式 API 镜像标签与完整 ID | `social-theory-library-api:3.0.8-e02b57013f`；`sha256:c378dc3675a772e6512ac1eeb80e9f5b8662875845b08f6262d292a9d6ec3e92` |
| 正式 Web 镜像标签与完整 ID | `social-theory-library-web:3.0.8-e02b57013f`；`sha256:ed57828586932584d164d6d3c602fd41ba180cd1bae811e8e9dd6d13ff8f3b19` |
| 正式 Inference 镜像标签与完整 ID | `social-theory-library-inference:3.0.8-e02b57013f`；`sha256:e33888d74b1a441b5f54543dc32cc7e4a5b5bde815c0fa83028eab2d1315a65c` |
| 最终候选迁移／新旧 ORM／HTTP／推理检查 | 已通过，范围见 `final-candidate-result.json` |
| 生产迁移及受保护摘要比较 | catalog0060/0061/0062、ingestion0018成功，保护摘要逐字一致 |
| 应用容器公网切换 | NAS2026-09-22T18:12:58+08:00，退出0，公网ready3.0.8/pending0 |
| 全库初始 discovery generation／文档数／激活状态 | 8e62c8bb已active，15/15来源、4483/4483文档；原文4472、知识11 |
| 真实 Celery 队列与四个 worker 联动 | 四worker ping通过，16项协调/来源索引任务成功，公网查询实际完成 |
| 公网原有业务／私人权限／Reader | 23项全部通过，39.0秒，实际记录见public-smoke.json |
| 最终公网原文／知识／策展新检索 | 14项实际功能检查通过；报告写入失败单独记录，详见上方排障记录；策展通道实际无公开内容 |
| 是否触发回退及结果 | 本次切换未触发回退 |
| 部署后文档记录提交 | 随本次交付记录提交，与应用镜像源码提交分开 |

真实浏览器验收按用户明确授权豁免，未把SSR或HTTP探针写成视觉、登录交互验收通过。完整索引及公网检索已实际核验，OCR恢复仅按已保存页数报告，不冒称290页整书已完成。

实际公网23项覆盖8个HTML页面200、544页Reader manifest、PDF Range206／1024字节／Content-Range及PDF文件头、题名检索与原文结果和准确定位、6个匿名私人接口401、公开推荐与共享关系列表真实空集。切换后应用与Edge重启次数0，cloudflared容器ID、镜像ID、重启次数与切换前一致。原始受限证据保留NAS deploy-record，本地非凭据副本在output/verification/v308/production。

本机公网探针记录为2026-09-22T10:11:54.137692Z—10:12:33.112618Z，与NAS时钟有偏差；分别保留两端原时间，不据此倒推操作顺序。初始代次为8e62c8bb-59a7-4104-8388-ae43f0e19a59，建立15个符合条件来源的派生投影，实时确认文档与来源任务进度另记index-observations.jsonl。
