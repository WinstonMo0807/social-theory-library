# 3.0.6 发布前核对与回退清单

更新日期：2026-09-20（香港时间）。本文件沿用9月14日的发布准备审计，记录当前门槛，不是上线记录；早期审计的子任务限制不代表当前实施范围。

## 当前结论

2026-09-20最新指令：剩余开发项及OCR呈现已交付本地代码，用户取消本轮测试/检查/核对，LoRA暂不处理。没有执行发布检查、构建、备份演练或部署；下方“易用性收尾后统一验证”的安排已被取消。原生产数据与回退保护要求不因此被绕过，当前不能将未经验证的新源码作为已验收发布物。未提交或推送，未修改生产数据库、镜像、文件或索引。

发布门槛尚未关闭。当前 `main/aa97727` 之后有较多未提交的 3.0.6 修改；`V3.0.6_VERIFICATION.md` 的 A01–A32 仍有待核实项。不得以版本字符串、构建成功、页面能打开或局部测试总数代替完整验收。

9月20日新增整页保存、身份建议预填、时间线出处、全本OCR与字段联动/可见反馈，尚未更新隔离NAS及生产镜像；不能沿用9月19日94072fee测试源称为最新源已验收。时间线专项已经结束；此后OCR和字段联动尚未测试。用户现要求全部实际功能完成前禁止任何测试，已遵守；其余易用性收尾后才统一执行全量/真实服务验证，随后仍需新鲜备份、回退入口及提交/镜像/源码映射。完成后部署的授权不取消这些门槛；Git推送没有新增授权。

## 核对计划

| 项目 | 状态 | 证据边界 |
| --- | --- | --- |
| 阅读当前进度、部署与回退规则 | 已完成相关段落读取 | 历史生产记录不证明当前 NAS / 公网状态 |
| 核对代码变化、迁移头、契约、版本与依赖 | 进行中 | 仅本地源码与已有报告 |
| 核对旧发布脚本并给出最小备份 / 恢复 / 回退顺序 | 待核对 | 不执行旧脚本，不读私钥或真实环境值 |
| 列出冻结源包扫描和逐项上线门槛 | 待完成 | 门槛缺证据即保持待核实 |

## 已核实的边界

- `CURRENT_PROGRESS.md` 明确要求所有 F01–F12 / A01–A32 保留，不因追加易用性或 Reader 排查缩减；用户授权满足门槛后的部署，不自动授权 GitHub 推送。
- 最近完整API报告为1447项中1405通过/42环境跳过；完整浏览器126项通过。两者均早于9月20日整页保存、身份预填及时间线出处增量，不代替冻结源码后的最终回归。最新专项结果见验证账本，不合并不同运行的数量，也不把旧1416项当最近完整报告。
- A30已完成合成备份恢复到独立空PG库：11类记录摘要、7原件、21个Page及私人关系一致，8公开详情和2个PDF Range通过。该演练不是新鲜生产备份、全NAS灾难恢复或0055回退兼容证明。
- 新增catalog0055仅扩展既有EditorialRevision的对象类型choices，无新表或数据重建。隔离NAS已更新94072fee，0055迁移及33项真实PG验证通过；旧ORM读写兼容另有证据。真实旧镜像回退读取见下条，新鲜生产备份与正式切换仍未完成。
- 2026-09-19独立恢复库应用回退：新源→生产当前旧镜像2486aee8→新源三次各20详情200、10个PDF Range206，新timeline_event草稿摘要一致且未公开，3个容器退出0。旧镜像不支持编辑新增草稿类型，应保留其记录、返回新版后再编辑，不执行反向迁移或覆盖数据库。
- 3.0.5 最新历史运行记录是 API / Worker / Beat `3.0.5-6c43f6e34f`、Web `3.0.5-e6fedd6b57`。部署前必须重新读取实际镜像 ID、配置、迁移、队列和公网状态，不把旧标签直接当本轮回退目标。
- `docs/HANDOFF.md`、`docs/rollback.md` 含 2026-08-16 的旧基线和旧发布脚本。当前以 `CURRENT_PROGRESS.md`、`docs/CURRENT_STATE.md`、`docs/DEPLOYMENT.md` 顶部记录及新鲜实测为准，不能整段照搬旧回退操作。
- 仅允许应用级回退；默认保留当前数据库、新增记录、文件历史和活动索引。覆盖生产数据库、反向迁移、重建索引或撤销当前人工决定都不是普通回退步骤。

## 本次审计记录

- 历史2026-09-19审计曾记录360px时间线长词撑宽和分页缺项；随后已修并取得v306-dedicated-2150等证据，不继续作为“尚未开发”的事实。9月20日新增出处选择仍在专项验证；最终全量及生产门槛未关闭。
- 已读取仓库 `AGENTS.md`、应用开发规则、当前计划顶部和相关架构 / 进度 / 问题 / 部署 / 回退段落；恢复脚本 `session-catchup.py` 退出 0，无额外报告。
- 沿用 planning-with-files 的持久核对方式，实施进度同步CURRENT_PROGRESS与验证账本。本页旧审计的子任务限制已结束，不应阻止主线同步记录。
- 初次组合读取发生输出截断，后续改为限定当前段落。未把未完整显示的历史段落记为已读。
