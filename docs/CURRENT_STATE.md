# 当前状态

更新于2026-09-13。此页区分已上线功能、最新源码与未实测条件。早期阶段记录不作为当前上线证明。

下一步用户要求重新设计整体架构，尤其后台和上架过程。准备讨论时请先读[实际架构与场景](GPT_ARCHITECTURE_CONTEXT.md)、[上架过程](INGESTION_AND_PUBLICATION.md)和[重设计任务](REDESIGN_BRIEF.md)。当前实现是可靠基线，不是不可调整的未来设计。本次仓库同步只更新文档和main代码，不再次部署生产。

本次升级和已发布PDF未展示问题已完成交付。功能实现、最终部署及必要验收均已记录；下文保留的能力边界不等同于未实现承诺。临时演练资源已清理，数据库备份、回退镜像、源码归档和日志仍可恢复。

## 公网和馆藏

公网为https://books.winstonmo.com，沿用NAS原Compose项目、同一API、数据库、任务系统、搜索索引和文件存储。2026-09-13最终API源提交a829bb8，Web源提交fa6b3bc；两者web源文件已比较完全相同，最后仅修正后台日期状态比较。精确镜像见[当前进度](../CURRENT_PROGRESS.md)。不存在第二套需要同步的书库。

已上线增量包括无上传手工编目和纯书目发布、统一字段决定与标识符、候选采用及撤销、发布差异与历史恢复、Person非冲突人工合并及撤回、五类图片统一媒体选择与草稿发布、Reader职责拆分及正式版本读取。

《质的研究方法与社会科学研究》原人工发布已保存，但活动公开修订为空。旧调度遗漏graph/timeline，发布完成查询又触发PostgreSQL可空连接锁错误。后台依据published标志过早显示成功。前端没有认错旧PDF，原文件也未丢失。

同一正式记录已经恢复公开，馆藏从6增至7。后台事件completed，编辑和处理均ready，公开状态published且待发布差异为空。日期对象与快照字符串造成的误提示已修正。公网题名搜索、详情、544页Reader、PDF Range206及%PDF-检查通过，真实浏览器已渲染画布。未达到质量要求的OCR不会因此自动进入公开正文检索。

## 数据和权限

- 9个原始PDF与3679页记录身份摘要不变，没有覆盖或删除原件。
- 6个OCR任务保持暂停。全局开关在2026-09-13 00:47已记录为false，最终部署前后保持该值，没有自动恢复这些任务或代替用户改设置。
- 活动语义UID仍为semantic_passages_20260818210650_4cf87bc9，3005条记录。公网语义查询为v2_hybrid、fallback_used=false。
- 私有媒体持久化到NAS，仅供API和Worker使用，Edge不暴露目录。
- 仍按管理员、编辑者、读者和匿名权限访问，没有创建或绕过管理员登录。

## 明确的能力边界

Person合并支持人工确认、无身份或唯一关系冲突且至多一份学者档案。复杂冲突明确阻断，不自动丢弃关系。历史图片保持兼容，没有要求自动批量搬移全部旧图。

生成类型覆盖已登记管理操作及公共Work/Edition、Reader等核心契约。复杂历史Evidence和未登记旧接口保留显式兼容类型，不宣称所有JSON均已自动生成。旧表读统计已建立，但需要真实观察期才能判定零使用，因此保留兼容表。

外部Provider、GPU Claim执行器、真实OCR吞吐、登录读者私人写入和全范围PostgreSQL并发未在本次逐项实测，继续报告实际等待或降级状态。观点benchmark gate没有擅自打开。匿名浏览器登录探测的401及无刷新会话400仍遵守鉴权。

npm audit已降为0 critical、2 high、4 moderate。image-size/Vinext两项告警没有已发布修复，已知使用路径为受信任构建图片；开发工具四项moderate不在生产运行依赖中。保留告警，不等于零风险。

## 交付依据

[架构](ARCHITECTURE.md)、[界面](UI_3.0.5.md)、[迁移](MIGRATION_3.0.5.md)、[部署和回退](DEPLOYMENT.md)、[验证](V3.0.5_VERIFICATION.md)、[接口契约](V3.0.5_API_CONTRACT.md)、[问题](ISSUES.md)。界面基础层、样式和接口领域整理已经上线，菜单焦点归还、引用方向键以及390px布局通过；未登录的后台写入仍不标记为公网实测。
