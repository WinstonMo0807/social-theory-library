# 3.0.5 验证记录

所有状态仅对应本轮命令和当时源码。基线为 `340b94f`。本地 SQLite 不代表 PostgreSQL 并发或生产可用。

## 2026-09-13最终验证

- 最终公网API为6c43f6e34f、Web为e6fedd6b57。后台目标书ready/ready/published、changes=[]、event=completed。菜单进入/退出焦点及背景modal通过，APA按ArrowRight选中并聚焦Chicago且tabStops=1。390px实书与Reader无水平溢出，Reader有4个真实PDF画布、loading=false。两个移动截图已查看。临时PG演练库与构建目录已清理，生产备份和回退镜像保留。
- 发布日期与旧快照兼容专项17项通过，退出0，54.50秒。真实PG恢复副本只读检查确认publication=published、changes=[]，没有改写日期或旧快照。
- API领域声明与消费者机械拆分已核对101声明、45消费者正文和90公开导出相同。27项API相关检查通过，退出0，1.738秒。
- web目录执行`npx tsc --noEmit --pretty false`无错误；CSS/API领域/反馈16项通过，退出0，1.491秒。CSS展开后的3413规则及10728声明摘要与冻结基线一致。
- web目录`npm test`构建成功，212项通过，退出0，35.791秒。没有为架构拆分重跑整个后端历史套件。
- 本地发布锁修复及元数据可见性11项XML通过，54.064秒，恢复后句柄失效，退出码未知。报告为stl-v305-publication-pg-locks.xml。
- NAS隔离PostgreSQL16用已部署镜像重现可空JOIN锁失败，挂只读补丁后`_finish_event`完成及租约释放成功。两个命令退出0，检查事务全部回滚，没有生产SQL修补。
- 公网目标书题名命中、详情200、公开馆藏7本、文件Range206、Content-Range bytes0-31/33287860及%PDF-通过。真实浏览器渲染3个PDF画布、loading=false、桌面无水平溢出，截图output/playwright/v305-public-reader.png已查看。
- 公网语义及观点请求均为v2_hybrid、fallback_used=false。未冒充benchmark升级。登录读者私人写入、管理员写入及真实外部Provider未在这次公网逐项执行。

## Phase 0 基线

| 检查 | 命令与环境 | 状态 |
| --- | --- | --- |
| Git | `git status --porcelain=v1`、`git branch --show-current`、`git log -1 --oneline` | PASS，起始干净，基线分支和 340b94f 已核对 |
| 后端关键面 | api 目录，`DATABASE_URL=sqlite:///:memory:`，`.venv/Scripts/python.exe -m pytest tests/test_cataloging_intelligence_v304.py tests/test_field_assistant_v304.py tests/test_catalog_consistency_audit_v304.py tests/test_admin_workflow_v280.py tests/test_resilient_publication_v260.py --tb=short` | FAIL，退出 1，29 通过、12 失败、2 个依赖弃用警告，50.13 秒 |
| 前端关键面 | web 目录，`node --experimental-strip-types --test --test-force-exit tests/admin-workflow-v280.test.mjs tests/workflow-research-candidates-v290.test.mjs tests/reader-upload-layout.test.mjs tests/public-page-control-v303.test.mjs` | FAIL，退出 1，24 项中 18 通过、6 失败 |

前端失败名称为 section validation、contributor editor、focus mode、classification/curation pickers、front matter contributors、candidate inspector。当前输出显示多个旧文案/源码形态断言与 3.0.4 不符。必须逐项审查意图和真实行为后处理，不能直接删除失败测试或把失败算作通过。

后端失败包括一个候选采用状态、六个旧工作台字段/步骤契约，以及五个旧发布/Reader 夹具或移除的同步 index 入口。当前 3.0.4 发布专项和一致性审计在该组中通过，但不能覆盖这些失败。处理优先保留作者确认、公开资格和异常情况下旧 revision 的真实行为要求。

## 待执行的最终门槛

### 首个发布安全修复

### 字段契约和纯书目发布

### MetadataCandidate 会话迁移

### API 契约基础

### 全回归差异治理

本次按用户要求收口，不再进行重复全量轮次。checkpoint5原12失败在缓存隔离/媒体契约修正后，19项定向通过，退出0，61.96秒。新增发布可见性、完整投影捕获、会话接入/字段统一/旧读统计、Identifier、原发布/投影/MetadataReview合并7文件79项通过，退出0，73.25秒，报告stl-v305-release-core-checks.xml。

Reader相关25项及源码等量核对通过，最终Shell1356行减到518行，保持原请求和UI。标识符包含模型历史值与原候选入口的30项通过、19取消选择，62.48秒；最后纯函数23通过、3取消选择，0.76秒，均退出0。核心API生成、漂移、5实际路由响应结构和TypeScript通过。

最终3.0.5版`npm run api:check`及`npm test`退出0，完整构建/204项通过，30.135秒。新增媒体私有挂载单项通过，0.254秒；全仓lint退出0，9条既有硬导航的新版本规则warning，未关闭规则。Django check、makemigrations --check --dry-run及diff检查通过。没有用这些本地结果替代生产验收。

完整后端checkpoint5最终1248 tests、1204 passed、12 failed、32 skipped，退出1，526.42秒，报告stl-v305-regression-checkpoint5.xml。应用源码f710492，运行期间未改应用及测试文件。失败涉及test_knowledge_matrix_v231旧肖像上传预期、test_public_knowledge_control_v303字段覆盖，以及test_scoped_search/test_semantic_and_publication_v223共10项HTTP429。已解除源码冻结，进入原因核对，不将测试隔离问题与功能故障混写，也不将32项跳过计通过。

2026-09-12 18:20开始完整后端checkpoint5，对应用源码f710492执行。api目录，`DATABASE_URL=sqlite:///:memory:`、`PYTHONIOENCODING=utf-8`，命令`../.venv/Scripts/python.exe -m pytest tests --tb=short --junitxml=C:/Users/12297/AppData/Local/Temp/stl-v305-regression-checkpoint5.xml`。运行期间应用与测试源码冻结，尚无最终结果；记录提交bc90b36不改变应用源码。不得将专项成绩当作本次全量成绩。

知识图片完整浏览器18 passed、0 failures/errors/skipped，退出0，98.609205秒，报告stl-v305-knowledge-media-e2e-full.xml。web目录设置`PLAYWRIGHT_JUNIT_OUTPUT_FILE=C:/Users/12297/AppData/Local/Temp/stl-v305-knowledge-media-e2e-full.xml`，执行`npx playwright test --config=playwright.cataloging.config.ts --reporter=list,junit --output=test-results/knowledge-media-full --max-failures=2`。包括原编目/媒体、人物查重/合并/撤回、学者肖像，以及节点加载保护、Node和Path图片发布/清除。实际API没有成功mock，测试库及internal SSR凭据仅用于隔离环境。

截图发现横幅图片未铺满旧背景区域，最小修改为图片占满既有横幅框，原背景在有媒体时不绘制。重新`npm test`构建成功，198项通过，退出0，18.956秒，TypeScript/切片lint通过。只复核`knowledge-media-v305.spec.ts`三项，追加横幅实际图片高度及390px图片不超出视窗断言，报告stl-v305-knowledge-media-e2e-responsive.xml，3 passed、0 failures/errors/skipped，63.258111秒，于12:45:53写入。18:14续接进程句柄失效，退出码未知；完整XML和四张截图均已核对，不为退出码重复运行。该次使用`--output=test-results/knowledge-media-responsive`保留独立痕迹。没有生产数据库、文件或活动索引操作。

知识图片改用textbox角色定位后3项通过，退出0，65.573231秒，报告stl-v305-knowledge-media-e2e-field-values.xml。分别验证延迟真实GET时节点控件禁用、Node/Path填写说明后与媒体同草稿发布、公开图片可加载、清除前后公开200/404及后台原图仍可读，Path阶段原ID保留。两张公开页面截图已读取。正在扩大到同配置中的全部18项，不将专项成绩当全部回归通过。

disabled浏览器组3项1通过/2失败，退出1，84.688822秒，报告stl-v305-knowledge-media-e2e-disabled.xml。失败位置变为填写后的getByLabel断言，快照明确含textbox简介/路径介绍及完整新值，没有提交PATCH。改用稳定的textbox角色与可访问名称核对，保留disabled加载保护及填入值断言。该测试定位问题不作为应用丢数据记录，正在复核完整3项。

知识媒体guarded浏览器3项1通过/2失败，退出1，86.962464秒，报告stl-v305-knowledge-media-e2e-guarded.xml。延迟GET的inert检查通过，但两个场景仍提交空说明。继续核对后加入原生fieldset disabled，把可填写状态与对象加载完成绑定；路径增加同样保护。测试额外检查disabled与填入值，未改后端保存约束。构建和198项前端再通过，退出0，18.186秒，disabled场景正在复核。

知识媒体两项浏览器改为页面真实保存后，reading_path通过、knowledge_node失败，退出1，75.741816秒，报告stl-v305-knowledge-media-e2e-ui.xml。trace中节点PATCH summary为空，初次资料读取的setDraft覆盖了测试先输入的简介。此为真实加载期间编辑保护缺口，不把测试改为固定等待来掩盖。节点编辑器现按URL对象/旧映射建立独立上下文，资料打开前form inert且显示加载或真实读取错误，保留已有请求和重试。新增延迟真实GET的加载保护测试。重新构建/198项前端通过，退出0，19.019秒，TypeScript/切片lint通过，3项浏览器复核正在运行。

知识媒体再加入`tests/test_theory_system_v231.py tests/test_reading_path_semantics_migration_v301.py`后7文件68通过，退出0，93.10秒，报告stl-v305-knowledge-media-final.xml。Django check/migration drift、TypeScript、API生成漂移和全仓lint均退出0。首轮两个浏览器场景的准备步骤直接用page.request.patch而未通过页面请求封装，被安全校验403拒绝；已改为真实页面填写简介/路径介绍并保存，不取消任何鉴权或防伪检查，当前正在复核。该首轮报告为stl-v305-knowledge-media-e2e-initial.xml，不计通过。

2026-09-12 知识节点和阅读路径图片接入0053，api目录设置`DATABASE_URL=sqlite:///:memory:`、`PYTHONIOENCODING=utf-8`，执行`../.venv/Scripts/python.exe -m pytest tests/test_knowledge_media_v305.py tests/test_scholar_portrait_media_v305.py tests/test_media_v305.py tests/test_editorial_revision_v300.py --tb=short --junitxml=C:/Users/12297/AppData/Local/Temp/stl-v305-knowledge-media-initial.xml`。首组46通过/1失败，退出1，79.40秒，失败是复用组合草稿时误改Node原审计说明默认文案；应用恢复原默认说明，未删除断言。

加入`tests/test_api_contract_v305.py`后53通过，退出0，60.23秒，报告stl-v305-knowledge-media-expanded.xml。前端`npm test`构建成功，198项通过，退出0，18.141秒。TypeScript和切片eslint通过。新API生成契约已生成，正在扩大原理论/阅读路径回归及真实浏览器测试，不计生产验收。

肖像面板间距/中文标签调整后，`PLAYWRIGHT_JUNIT_OUTPUT_FILE=C:/Users/12297/AppData/Local/Temp/stl-v305-scholar-portrait-e2e-responsive.xml npx playwright test --config=playwright.cataloging.config.ts scholar-portrait-v305.spec.ts --reporter=list,junit --output=test-results/scholar-portrait-responsive`，1 passed，退出0，63.533909秒。额外验证390px整页无水平溢出，桌面草稿、公开图片和窄屏面板截图均已读取。最终TypeScript/切片lint再次通过。截图和临时测试库不进入Git。

2026-09-12 肖像完整浏览器组15 passed，0 failures/errors/skipped，退出0，99.387109秒，报告stl-v305-scholar-portrait-e2e-final.xml。命令为web目录设置`PLAYWRIGHT_JUNIT_OUTPUT_FILE=C:/Users/12297/AppData/Local/Temp/stl-v305-scholar-portrait-e2e-final.xml`后`npx playwright test --config=playwright.cataloging.config.ts --reporter=list,junit --output=test-results/scholar-portrait-final --max-failures=2`。沿用独立临时SQLite和test-only内部SSR配置，保留生产权限逻辑，不伪造业务成功。覆盖编辑者上传、填写来源/许可、选择肖像、私有预览真实图片宽度、发布前404、显式发布后WebP及公开页面图片加载、清除草稿时公开旧图仍200、发布清除后404及原rendition后台仍可读。原14场景全部通过。仅面板间距及中文标签再调整后构建/195项通过，退出0，20.255秒，另加390px布局检查正在复核。TypeScript、API漂移、Django/migration和全仓lint均退出0，未执行生产验收。

肖像最终增加同一草稿指纹及部分Person更新预览保留检查，同组六文件81通过，退出0，83.50秒，报告stl-v305-scholar-portrait-final.xml。完整前端再次`npm test`构建成功，195通过，退出0，27.107秒。Django check及migration drift均退出0。单一肖像浏览器首轮失败，81.877352秒，报告stl-v305-scholar-portrait-e2e-initial.xml，实际图片API200但Web纯SSR标签未按独立runtime API源读取，naturalWidth为0。新增小型客户端图片组件，服务器保持同源路径，浏览器按明确runtime源渲染，未mock业务成功或改生产反向代理。完整15场景正在复核。

2026-09-12 学者肖像新增0052，api目录设置`DATABASE_URL=sqlite:///:memory:`、`PYTHONIOENCODING=utf-8`后执行`../.venv/Scripts/python.exe -m pytest tests/test_scholar_portrait_media_v305.py tests/test_media_v305.py tests/test_editorial_revision_v300.py --tb=short --junitxml=C:/Users/12297/AppData/Local/Temp/stl-v305-scholar-portrait-initial.xml`，31通过/1失败，退出1，59.75秒。失败为测试假定public消费者，实际Public投影交由bibliographic_search/public_cache；改核对真实required_projections集合，未改调度器语义。

加入`tests/test_person_merge_v305.py tests/test_person_resolution_preview_v305.py`后72通过，退出0，69.30秒，报告stl-v305-scholar-portrait-expanded.xml。再加入`tests/test_api_contract_v305.py`和旧指纹/无效上传/人物归属变更测试，guarded进程在续接后无句柄、无XML、无存活pytest，不计结果。重跑同组80通过，退出0，74.23秒，完整报告stl-v305-scholar-portrait-guarded2.xml。Web `npm test`构建成功，195 passed，退出0，22.540秒；TypeScript与切片eslint通过，API契约已重新生成，浏览器验收尚在运行。

2026-09-11 人物整理前端新增无学者档案的人物只读查找和Owner最近操作查询。api目录以`DATABASE_URL=sqlite:///:memory:`、`PYTHONIOENCODING=utf-8`执行`../.venv/Scripts/python.exe -m pytest tests/test_person_resolution_ui_api_v305.py tests/test_person_merge_v305.py tests/test_person_resolution_preview_v305.py tests/test_api_contract_v305.py --tb=short --junitxml=C:/Users/12297/AppData/Local/Temp/stl-v305-person-ui-api.xml`。首轮49通过/1失败，退出1，74.19秒；新fixture重复使用空邮箱，违反既有唯一约束，修正测试账号后同命令50通过，退出0，84.88秒，完整报告stl-v305-person-ui-api-final.xml。

Web `node --import tsx --test --test-force-exit tests/person-resolution-v305.test.mjs` 3通过，退出0。`npx tsc --noEmit --pretty false`、切片eslint、`npm run api:generate`与`npm run api:check`退出0。`npm test`构建成功，191项中190通过/1失败，退出1；失败为导航白名单尚未包含新建的真实`/admin/people`路径，正在补齐该路径并保留其实际文件存在性检查。

独立临时SQLite服务的本地dev首次冷请求500，随后未改源码重试200。浏览器开发环境的`/api`没有反向代理，初始登录404；仅将测试浏览器runtime origin指向8105后真实登录和人物预览成功。没有用成功mock替代业务API。桌面与390px移动截图已读取，未出现横向溢出；预览面板返回queued，不记为用户已经看到。

补齐真实导航白名单后`node scripts/run-unit-tests.mjs`191项通过，退出0，17.533秒。首次13场景E2E因原编目空白和新输入定位同时匹配region/textbox而中止，没有完整XML。仅修正定位后人物7场景3通过、1断言失败、1超时错误、2未执行，退出1，133.5298秒，报告stl-v305-person-ui-e2e-person.xml。实际合并POST已200，但SSR读取site-config共用匿名120/min额度，结果页出现429转500；未将已执行操作误称失败。

独立fixture随后补既有内部SSR请求的test-only凭据，前后端配对且不发送给浏览器。批量账号登录使用仅此fixture的60/min阈值，业务权限不变，生产限流与内部认证实现未改。本验收不证明生产限流承载能力。新人物数据链接关闭自动预取，减少未选择对象的额外请求。再次13场景12通过/1断言失败，退出1，94.326211秒，报告stl-v305-person-ui-e2e-configured.xml。原编目已通过；剩余为h3误标role=status丢失标题语义，改为aria-live并保留标题。另补撤回响应丢失检查后，完整前端192项通过，退出0，19.326秒，新的构建、TypeScript和切片lint通过。浏览器14场景正在复核。

浏览器命令均为web目录设置`PLAYWRIGHT_JUNIT_OUTPUT_FILE=C:/Users/12297/AppData/Local/Temp/<上述报告名>`后执行`npx playwright test --config=playwright.cataloging.config.ts --reporter=list,junit`，后两轮另指定`--output=test-results/person-ui-configured`或`person-ui-final`保留独立痕迹。人物专项以`person-resolution-v305.spec.ts`限定文件。Vinext取消导航仍可能输出ERR_STREAM_UNABLE_TO_PIPE，未宣称消除。未连接生产或真实索引。

最终浏览器组14 passed、0 failures/errors/skipped，退出0，83.619086秒，报告stl-v305-person-ui-e2e-final.xml。真实API覆盖无Profile查人、Editor/Administrator不得越权、双Profile拒绝、实际合并及重开撤回、合并成功响应丢失后同body/同幂等键重试且只有一条记录、另一页旧预览409拒绝、查重断网显错及真实重试、撤回成功响应丢失后仅GET核实且POST只有一次。另含既有编目与媒体6场景。未mock成功业务响应；仅runtime origin指向独立服务和明确故障注入。该组之后只读摘要SQL又收紧为明确字段，后端专项另行记录，不把浏览器结果当后续SQL性能证明。

最后列表摘要SQL收紧后，同前文四文件pytest命令51 passed，退出0，53.60秒，报告stl-v305-person-ui-summary-final.xml。查询捕获确认Person列表不读biography/external_ids，历史列表不读snapshot/rollback_fingerprint且仅一条记录SELECT。此SQL优化不改API契约或执行/回滚事务。`npm run api:check`、Django `check`及`makemigrations --check --dry-run`、`npx tsc --noEmit --pretty false`和`git diff --check`退出0；全仓lint退出0，唯一warning来自临时浏览器设置脚本，清理该本轮生成文件后再次检查退出0且无warning。截图和失败报告仍保留，没有生产操作。

2026-09-11 Person执行器首次 `test_person_merge_v305.py test_person_resolution_preview_v305.py` 31 passed，退出0，63.43秒，报告stl-v305-person-merge-initial.xml。接HTTP、权限和契约后50 passed/2 failed，退出1，60.88秒，报告stl-v305-person-merge-api.xml；错误为新fixture误用requested_by字段及旧只读断言固定false，已纠正。之后扩大运行未留下完整XML且没有存活进程，不计结果。

保护专项新增3项先红，40.927秒，报告stl-v305-person-merge-guards-red.xml完整，恢复后句柄失效，退出码未知。分别复现业务JSON内部日期键未参与回滚保护、错误使用pending而漏掉真实queued研究状态，以及bulk upsert可改写操作记录。修复后扩大82 passed/1 failed，退出1，59.10秒，报告stl-v305-person-merge-guards-green.xml；剩余为候选fixture缺少conflict_group，改用现有FieldEnrichmentService及受控Provider生成完整候选，未放宽约束。

随后83 passed，退出0，53.45秒，报告stl-v305-person-merge-verified.xml。补文档型书目、文件字节和合并后私人笔记保留、回滚HTTP幂等及基础资料保留说明后，`tests/test_person_merge_v305.py tests/test_person_resolution_preview_v305.py tests/test_api_contract_v305.py tests/test_role_capabilities_v301.py tests/test_publication_commands_v305.py tests/test_query_lexicon_transactions.py` 84 passed，退出0，71.16秒，报告stl-v305-person-merge-files-final.xml。

这些命令在api目录设置`DATABASE_URL=sqlite:///:memory:`与`PYTHONIOENCODING=utf-8`，使用`../.venv/Scripts/python.exe -m pytest <上述文件> --tb=short --junitxml=C:/Users/12297/AppData/Local/Temp/<报告名>`。迁移0051仅由测试库执行，没有生产迁移。最终`manage.py check`、`manage.py makemigrations --check --dry-run`、Web `npx tsc --noEmit --pretty false`、`npm run api:generate`、`npm run api:check`及`git diff --check`均退出0。发布消费者用明确test-only确认，不证明真实PG锁、Celery、Meilisearch或NAS状态。未改前端组件，浏览器操作页尚未接入。

2026-09-11 新人物查重/只读预览首组 `tests/test_person_resolution_preview_v305.py` 8 passed/1 failed，退出1，56.23秒，报告 stl-v305-person-preview-initial.xml。失败为新fixture缺少必填variant_type，模型按原规则拒绝，补alias/en/editorial资料后9 passed，退出0，53.30秒，报告 stl-v305-person-preview-green.xml。该次命令从仓库根目录运行 `.venv/Scripts/python.exe -m pytest api/tests/test_person_resolution_preview_v305.py`，设置同前文的临时SQLite环境。其他运行仍以api目录为准。

新API未提供执行/回滚，使用实际Django关系读取、HTTP权限与SQL捕获，确认预览不发INSERT/UPDATE/DELETE等变更命令。补整型标识符兼容、前导零保护和普通Administrator不能越过Owner权限后，`test_person_resolution_preview_v305.py test_role_capabilities_v301.py test_api_contract_v305.py` 28 passed，退出0，57.68秒，报告 stl-v305-person-preview-contracts.xml。TypeScript、生成API契约及`npm run api:check`退出0。

随后活动词典预览改为只读当前generation的具体词条及资格，使用已有只读状态校验，不在预览中调用ensure/sync/rebuild；增加退役行排除、空词典不初始化、错误指针不修复和超限标识测试，人物/权限/契约组32 passed，退出0，44.76秒，报告stl-v305-person-preview-lexicon.xml。分开“词典不可用”和“范围截断”提示后，加入`test_query_lexicon_sync_rebuild.py`，48 passed，退出0，45.62秒，报告stl-v305-person-preview-final.xml。

最终输入边界补查发现external_ids的键名可能被当作Django查询操作符。`test_person_resolution_preview_v305.py -k identifier_keys` 4 failed/16 deselected，退出1，41.92秒，报告stl-v305-person-id-keys-red.xml；isnull/contains触发异常，exact/in漏配。改为显式`external_ids__{scheme}__exact`，保留普通JSON键语义，未放宽scheme或标识符校验。

最终命令为api目录设置`DATABASE_URL=sqlite:///:memory:`和`PYTHONIOENCODING=utf-8`，`../.venv/Scripts/python.exe -m pytest tests/test_person_resolution_preview_v305.py tests/test_role_capabilities_v301.py tests/test_api_contract_v305.py tests/test_query_lexicon_sync_rebuild.py --tb=short --junitxml=C:/Users/12297/AppData/Local/Temp/stl-v305-person-preview-verified.xml`。结果52 passed，退出0，46.16秒。Django check、makemigrations --check --dry-run、TypeScript、API生成与漂移检查和diff检查通过。没有schema变更或前端组件，未运行浏览器验收/真实PostgreSQL/生产词典服务。本次只读预览不证明Person Merge执行或回滚可用。

2026-09-10 完整后端checkpoint4对源码bd81669执行 `../.venv/Scripts/python.exe -m pytest tests --tb=short --junitxml=C:/Users/12297/AppData/Local/Temp/stl-v305-regression-checkpoint4.xml`，api目录，`DATABASE_URL=sqlite:///:memory:`、`PYTHONIOENCODING=utf-8`。完整XML于23:31:43写入，1180 tests、1148 passed、0 failures、0 errors、32 skipped，728.984秒。测试期间冻结应用与测试源码，只有只读检查。续接后write_stdin句柄失效，退出码未知。

该次跳过为31项requires PostgreSQL，以及1项要求RUN_QUERY_LEXICON_LARGE_TEST=1的有界大数据演练。它们仍为待核实，不计通过。没有真实索引/Provider/OCR/生产验收，不把可运行回归无失败当作v3.0.5功能全部实现。此报告替代checkpoint3成为最新完整后端结果。

2026-09-10 剩余旧功能组 `test_knowledge_matrix_v231.py test_theory_system_v231.py test_passage_index_synchronization.py` 24 passed/6 failed，退出1，74.76秒，报告 stl-v305-legacy-projection-baseline.xml。推荐和公开理论证据夹具缺少活动快照，已发布节点的别名候选应先保存编辑修订；合并测试的旧译名未标记已审核，不应自动传播到目标。

段落索引新增资格门槛先红，`test_passage_index_synchronization.py` 9 passed/2 failed，退出1，2.07秒，报告 stl-v305-index-publication-guard-red.xml。确认 index_asset 未检查显式修订所属 Edition 和 metadata_ready，可能执行不属于合法发布的索引创建/清理。最小修复补两项检查，所有索引调用均mock，没有真实外部写入。原差集清理、分批分页、正式快照文字和修订ID范围的断言保留，准备态或非法归属必须在任何索引变更前退出。

原三文件首轮修复35 passed/1 failed，退出1，72.56秒，报告 stl-v305-legacy-projection-green.xml。新增失败为误将外层ENTITY_MERGED映射成内部merge，恢复实际update并核对外层事件FK。加入 `test_publication_invariants_v305.py test_projection_propagation_v300.py test_ingestion_integration.py` 后6文件58 passed，退出0，182.13秒，报告 stl-v305-index-theory-publication-final.xml。Django check、makemigrations --check --dry-run 与 diff 检查退出0，无新增schema。该组仍为本地SQLite和模拟索引确认，不证明真实外部服务。执行环境与pytest参数同本节前文。

2026-09-10 命题/候选四文件 `test_claim_curation_v300.py test_claim_pipeline_viewpoint_v300.py test_field_enrichment.py test_task2a_query_lexicon_search.py` 初始48 passed/8 failed，退出1，46.11秒，报告 stl-v305-claims-candidates-baseline.xml。区分真实修订缺失、作者确认、catalog_namespace mock 和候选采用不等于正式发布的行为；没有直接将8项全判为旧测试。

`test_viewpoint_snapshot_boundary_v305.py` 首次15项先红12 failed/3 passed，退出1，62.09秒，报告 stl-v305-viewpoint-boundary-red.xml。实际复现无正式修订、metadata-only、撤回、superseded、外版本指针、未选文件以及匿名访问受限文件仍有基线结果，上一正式正文不可查，以及结果混用索引/草稿元数据。首次修正组合21 passed/8 failed，退出1，46.84秒，报告 stl-v305-viewpoint-boundary-green.xml；文件名含green但实际失败，原因是移除作者即时查询时误删 facets 仍使用的 Contribution import，已恢复。

筛选项另先红3 failed，13 deselected，退出1，47.64秒，报告 stl-v305-viewpoint-facets-red.xml，确认正式筛选项标题和成员也需固定快照。修正后五文件72 passed/1 failed，退出1，72.07秒，报告 stl-v305-claims-candidates-green.xml；剩余是新增 aliases 断言未考虑 Person 保存时原有规范化处理，改为比较重建前后真实持久值。

HTTP 边界增补7 failed/2 passed/16 deselected，退出1，66.24秒，报告 stl-v305-viewpoint-http-red.xml。其中主要依据不匹配在 DerivedClaim.clean 就被正确拒绝，改为断言现有保护，不绕过模型或计作新业务缺陷。另6项复现 taxonomy、作品、作者、年份筛选被陈旧命中绕过，以及非法索引 UUID 异常。HTTP 用例保留真实 API 与服务，只模拟外部检索结果及容量槽；检索 stub 使用明确参数，不能用宽泛 kwargs 隐藏接口错误。

扩大8文件另含 `test_viewpoint_public_v300.py test_claim_benchmark_admin_v302.py test_publication_invariants_v305.py`，21:37:20 的报告 stl-v305-viewpoint-candidates-final.xml 完整记录103项中102 passed/1 failed，0 errors/0 skipped，74.525秒。恢复后句柄失效，退出码未知。最后失败为新增测试读取不存在的返回值 source_registry_version，已改为检查 QueryLexiconState 与 active_generation 的实际注册表版本。

最终同8文件复测103 passed，退出0，73.75秒，报告 stl-v305-viewpoint-candidates-verified.xml。26项新增边界测试包含服务与真实DRF HTTP入口，真实外部索引仍未执行。保留主要证据一致性的现有模型拒绝，不创建绕过约束的坏记录。

相关 pytest 在 api 目录，命令前设置 `DATABASE_URL=sqlite:///:memory:` 与 `PYTHONIOENCODING=utf-8`，解释器 `../.venv/Scripts/python.exe`，参数 `-m pytest <上述文件> --tb=short --junitxml=C:/Users/12297/AppData/Local/Temp/<报告名>`。Django check、makemigrations --check --dry-run、Web npm run api:check（仅 verified 契约范围）以及 git diff --check 均退出0。无 schema/前端更改，未重跑真实浏览器或生产 OCR、Worker、Meilisearch/PG，未改变活动索引或历史文件。

2026-09-10 OCR 旧入口回归 `tests/test_processing_backfill_v260.py tests/test_processing_pause_and_ai_filter.py tests/test_selective_ocr_consumption_v302.py` 22 passed，退出0，49.65秒，报告 stl-v305-ocr-staged-consumption.xml。移除已不存在的 processing.index_asset/queue_semantic_job mock 后，检查真实文档修订、未发布版本不产生 Catalog 发布事件，保留原件字节/人工页码/分批与暂停恢复断言。

加入 `tests/test_document_intelligence_v300.py` 后，四文件28 passed，退出0，49.22秒，报告 stl-v305-ocr-publication-boundaries-final.xml。该用例由仅 state=published 改为先断言不可见，再激活指定 OCR DocumentRevision 后检查命题可见；旧 stale/superseded 命题仍拒绝。只改测试，应用源码仍为 Reader 提交0a43ac5。命令环境为 api 目录、`.venv/Scripts/python.exe -m pytest`、`DATABASE_URL=sqlite:///:memory:`、`PYTHONIOENCODING=utf-8`、`--tb=short --junitxml=C:/Users/12297/AppData/Local/Temp/...xml`。真实 OCR/Worker/索引均未执行，不能从该专项推算新的完整后端成绩。

2026-09-10 Reader/文件发布旧夹具第一轮 20 passed/4 failed，退出1，45.70秒，报告 stl-v305-reader-publication-fixtures.xml。三个失败来自旧同步索引 mock 和封面绕过正式编辑修订的假设，另一个复现真实页码接口问题。`test_page_metadata_readiness_v305.py` 先红3 failed/2 passed，退出1，41.29秒，报告 stl-v305-page-metadata-red.xml。其中一个是新 fixture 重复建立活动 revision 的唯一约束错误，改为创建时传入 fulltext_ready，未修改数据库约束。

页码接口保留活动版本及文件权限，正文仍使用原完整资格，`test_page_metadata_readiness_v305.py test_resilient_publication_v260.py::test_page_segment_maps_pdf_page_73_to_printed_page_50` 11 passed，退出0，41.62秒，报告 stl-v305-page-metadata-green.xml。随后 `test_page_metadata_readiness_v305.py test_reader_cover_semantic.py test_resilient_publication_v260.py` 34 passed，退出0，45.62秒，报告 stl-v305-reader-publication-events.xml。封面通过真实维护发布 HTTP 和明确 test-only 消费者确认后公开；发布失败与撤回测试通过真实持久事件协调，并在 ProjectionState 边界模拟外部失败，未实际调用 Meilisearch 或 Redis。

补访问状态与私人阅读记录测试，命令 `tests/test_page_metadata_readiness_v305.py -k 'access_text_capability or private_reader_records'` 1 failed/2 passed/10 deselected，退出1，43.15秒，报告 stl-v305-reader-text-capability-red.xml。失败复现 OCR 任务成功但正文未激活时 access 报告可用。随后给同一返回字段加入正式全文检查，仍保留原 OCR 状态和文件下载策略。

最终 `tests/test_page_metadata_readiness_v305.py tests/test_reader_cover_semantic.py tests/test_resilient_publication_v260.py tests/test_publication_invariants_v305.py tests/test_scoped_search.py tests/test_reader_recent_and_saved.py tests/test_ingestion_integration.py` 70 passed，退出0，148.07秒，报告 stl-v305-reader-readiness-final.xml。Django `manage.py check`、`manage.py makemigrations --check --dry-run`、Web `npm run api:check`（仅 verified 契约范围）和 `git diff --check` 退出0。本轮未改前端，未重跑 Web 构建或浏览器 E2E。前一轮的浏览器成绩不能替代本轮新功能实测。

以上 pytest 均在 api 目录，以 `.venv/Scripts/python.exe -m pytest`、`DATABASE_URL=sqlite:///:memory:`、`PYTHONIOENCODING=utf-8`、`--tb=short --junitxml=...` 执行。报告保存在 `C:/Users/12297/AppData/Local/Temp/`。本轮新断言未实际打开浏览器或连接真实 OCR、生产 PostgreSQL/索引，不能作为外部验收。一次页码测试补丁位置错误在运行前检查并修正；一次文档补丁误写标题导致整次校验失败，确认未写入后按真实标题重新应用。

2026-09-10 完整后端 checkpoint3 针对 4120c66 的应用源码运行，命令 `DATABASE_URL=sqlite:///:memory: .venv/Scripts/python.exe -m pytest tests --tb=line --junitxml=.../stl-v305-regression-checkpoint3.xml`。报告于 10:55:52 完成，1133 tests、1063 passed、38 failures、32 skipped、0 errors，583.194 秒。续接后 exec/write_stdin 句柄失效，退出码未知。剩余失败按投影/发布事件旧契约、Reader/正文正式快照、OCR 已退役同步入口、候选审核语义继续诊断，不能全归为旧测试或擅自降低断言。production release gate 仍失败。

checkpoint3 后 `test_dependency_runtime_v300.py test_projection_propagation_v300.py test_semantic_index_v2_lifecycle.py test_catalog_foundation_models.py test_viewpoint_public_v300.py` 39 passed，退出 0，44.24 秒，报告 stl-v305-projection-contracts.xml。旧 mocks 显式接收并核对 catalog_revision，元数据参数仍使用保存的 runtime snapshot；标题变化只要求当前的六类投影，失败/重试/租约及类型/年份范围约束保留。没有修改实际投影服务或吞掉失败。

`test_admin_lifecycle_v250.py test_knowledge_studio_v300.py test_editorial_revision_v300.py` 30 passed，退出 0，47.10 秒，报告 stl-v305-entity-publication-events.xml。知识实体正式发布通过 KnowledgePublicationEvent 的真实 domain_event FK 检查领域事件，核对对象、原编辑修订 provenance、版本号和 withdraw 状态；不再假定两类事件共享幂等键。仅测试调整，未改应用源码。补丁曾两次上下文不匹配且没有写入，核对 Git diff 后按真实变量与模型字段修正。

2026-09-10 推荐图例切片先以旧 API 的替换/删除复现 2 failed，旧文件被直接删除，64.06 秒。接入统一媒体和编辑修订后首组 36 passed，退出 0，70.48 秒。中间一轮没有完成报告且句柄失效，未计通过。扩展首组 53 passed/1 failed，发现推荐图例仍触发正文索引；补 Public-only 字段规则后 56 passed，退出 0，110.37 秒。旧报告类型 fixture 缺发布机构阻断一次，保留要求并补测试数据后 57 passed，退出 0，91.52 秒。

推荐图例最终 `test_recommendation_media_v305.py test_media_v305.py test_reader_cover_semantic.py::test_admin_recommendation_image_override_is_public_and_removable test_publication_commands_v305.py test_editorial_revision_v300.py test_catalog_contracts_v305.py` 59 passed，退出 0，98.98 秒。覆盖旧文件保留、发布/清除/恢复、FK 引用保护、图例与书封独立、图片只更新展示、多版本上下文、待发布类型以及匿名/Reader 拒绝。报告 stl-v305-recommendation-context-final.xml，生产数据库及真实索引仍待核实。

前端最新 `npm test` 重新构建及 188 项通过，退出 0，Node 测试 40.514 秒。TypeScript、Django check、migration drift、生成契约检查通过。图例编辑器提取时误删了后方已有 helper，TypeScript 立即发现；已从当前 HEAD 精确恢复原 helper 后重跑，没有用忽略错误通过构建。

Playwright 共 6 项，其中媒体用例新增推荐图选择、真图 naturalWidth=640、移除选择和书封不变。第一次 6 passed/1.8 分钟但 FieldAssistant API 有 SQLite lock 500，因此不计无错误验收。隔离 fixture 路径校验后使用本机 Django 支持的 IMMEDIATE 事务，并新增正常编目 500 响应断言；随后 6 passed，退出 0，2.0 分钟，无 database is locked 输出。最后修改多版本参数后再次运行，10:35:05 的 .last-run.json 为 passed/无失败；续接后句柄失效，最终退出码未知。生产 PostgreSQL 设置和业务事务锁未改。

推荐图例切片最终 `npm run lint`、`npm run api:check` 组合命令退出 0，Django check 与 makemigrations --check --dry-run 退出 0，diff 检查通过。已启动完整后端 checkpoint3，运行过程中应用源码保持不变，最终结果另记，不据专项成绩推算整体失败数。

2026-09-10 公开 Reader/证据首组 23 passed/2 failed，90.81 秒。修正测试 helper 指定 Reader Asset 和 RAG 正式全文前置条件后，26 项通过。新增旧封面归属测试先复现失败，1 failed/26 passed，66.49 秒。最小代码修复后 `test_cover_identity_v305.py test_work_curated_claims_v300.py test_public_knowledge_core_consumers_v301.py test_admin_configuration.py::test_reading_history_coalesces_recent_page_updates test_ingestion_batch_policies.py::test_registered_asset_requires_login_but_remains_available_to_reader test_library_rag_task6.py test_media_v305.py test_reader_recent_and_saved.py test_scoped_search.py` 62 passed，退出 0，70.69 秒，报告 stl-v305-public-reader-cover-green.xml。没有放宽生产公共过滤；测试活动快照不证明真实索引可用。

工作台/智能编目/发布命令首组 29 passed/1 failed，50.42 秒。剩余用例原本假定人工关联会覆写机器报告，核对后保持原报告，并新增实际流水线重跑。复现旧代码用 OCR 覆盖人工关联 Work 的题名，定向 1 failed，78.47 秒。只读有效状态修复后 `test_manual_work_reconciliation_v305.py test_admin_workflow_v280.py test_ingestion_reconciliation.py test_entity_resolution_decisions.py test_session_candidates_v305.py` 39 passed，退出 0，49.45 秒。强 Edition 复用保护及发布命令扩大组待记录最终结果。

最后同组加入 `test_cataloging_intelligence_v304.py test_publication_commands_v305.py`，59 passed，退出 0，94.71 秒，报告 stl-v305-manual-work-reuse-final.xml。包含更强 existing_edition 保护不降级、明确人工关联与撤销、旧报告不覆盖、发布前置条件与恢复发布。Django check、makemigrations --check --dry-run、npm run api:check、git diff --check 均退出 0。未执行生产 PostgreSQL 或真实并发 Worker 验收。

2026-09-09 最后 `test_metadata_review_contract_v305.py test_catalog_contracts_v305.py test_ingestion_admin_foundation.py test_admin_foundation_backfill.py test_complete_ingestion_workflow.py test_ingestion_integration.py` 34 passed，退出 0，180.31 秒，2 个既有 pypinyin 弃用警告，报告 stl-v305-review-ingestion-final.xml。Django check、makemigrations --check --dry-run、npm run api:check 与 diff 检查退出 0。验证原 PDF/Annotation 不覆盖、替换期间旧版本可读、激活后新版本服务、Range/代理条件、撤回后隐藏及重新上架后等待处理。不证明外部索引或模型实际可用。

Reader/替换集成到达活动索引断言后发现派发失败分支误改 SemanticIndexVersion。新 `test_semantic_dispatch_safety_v305.py` 先运行 active/building 两组，1 failed/1 passed，明确复现 active 被改为 failed，78.33 秒。修复仅让未完成候选失败，活动和保留版本不变，并拒绝迟到的旧 task_id 错误覆盖已执行或已重派的任务。与 `test_durable_pipeline_dispatch.py` 共 12 passed，退出 0，87.57 秒，已单独保存提交 8405d9f。

复核/旧 backfill/完整入库扩大组为 31 passed/3 failed，188.69 秒。三个失败均是新测试辅助函数误将 ProjectionState.source_revision 写成 canonical_revision，已修正测试字段名。此前辅助函数确认全部投影触发 projection_revision_not_ahead，已改为只确认事件实际登记的 required_projections，不扩大源版本或放宽数据库约束。修正后入库组为 4 passed/1 failed，179.76 秒，最后一项仅在重新上架末步遗漏消费者确认。已经完成的搜索/Range/替换/旧笔记保留断言未删除；补齐重新上架的可见性时序后最后扩大组正在收取结果。
元数据复核契约组 `test_metadata_review_contract_v305.py test_catalog_contracts_v305.py test_ingestion_admin_foundation.py` 首次 21 passed/2 failed，两条新错误断言未使用统一 details 结构。修正并增加出版日期、归档身份原子拒绝后，25 passed，退出 0，43.81 秒，报告 stl-v305-review-contract-final.xml。`npm run api:check` 退出 0。完整发布消费者集成仍待继续，未更改生产数据或权限。

`test_complete_ingestion_workflow.py` 两项中文图书/期刊文章 HTTP 上架已通过，合跑 Reader/替换文件场景时为 2 passed/1 failed，87.00 秒。第三项请求本来未传 language 却依赖默认中文，现明确提交语言；首次运行还观察到旧 eager embedding 尝试缺本地模型，因此该目录/Reader 测试改用非 eager 队列并保留真实持久事件。消费者完成由 tests/publication_fixtures.py 明确确认，不能作为真实 embedding、Meilisearch 或 Celery 运行证据。

字段助手规范关系修复 `test_field_assistant_legacy_v305.py test_field_assistant_v304.py test_v3_canonical_identity.py test_session_candidates_v305.py` 37 passed，退出 0，54.61 秒。8 个新增用例覆盖草稿、已发布编辑修订、缺映射、错类型、未发布目标、身份不同、旧人工关系不覆盖及字段决定失败时事务回滚。静态 inventory 断言恢复通过，不声称这等同生产运行时零旧写。

加入映射与目标的事务内 select_for_update 后，`test_field_assistant_legacy_v305.py test_v3_canonical_identity.py` 23 passed，退出 0，49.00 秒。Django check、makemigrations --check --dry-run 和 git diff --check 退出 0。SQLite 能验证应用流程及事务失败还原，不能证明 PostgreSQL 并发锁。

2026-09-09 `DATABASE_URL=sqlite:///:memory: .venv/Scripts/python.exe -m pytest tests --tb=line --junitxml=.../stl-v305-wave2-backend.xml` 完整 XML 为 1095 tests、61 failures、0 errors、32 skipped、1002 passed，396.086 秒。报告写入时间 11:12:13。续接后命令句柄已失效，因此退出码未知。所有已有新媒体专项在全量中通过；不能把剩余失败视为全是旧测试问题。

首页权限切片 `npx tsc --noEmit --pretty false` 与 `npm run lint` 退出 0，首组权限/导航 26 项通过。`npm test` 曾出现集中 SSR 错误，原因为待核实。无改动完整 Node 重跑 184 passed，退出 0；最新重新构建后的 `npm test` 184 passed，退出 0，Node 部分 17.061 秒。不是通过降低断言或增加重试次数得到的结果。

最后 `npx playwright test --config=playwright.cataloging.config.ts` 6 passed，退出 0，1.1 分钟。增加非 superuser Administrator 测试账户，仅存在一次性本地 SQLite。Editor 浏览器不发统计请求，显式绕过页面的 API 请求仍 403；管理员得到真实 200。队列失败用 route.abort 注入网络中断，恢复后重试真实接口，不伪造成功业务响应。普通编目、读者拒绝及媒体旅程仍通过。

### 分层功能健康

### 媒体库与作品封面

2026-09-09 `node --import tsx --test --test-force-exit tests/settings-controls.test.mjs` 23 passed，退出 0，修正的是新增媒体导航的预期路由。`npm test` 完整构建及 181 项通过，退出 0。`npm run api:check`、`npx tsc --noEmit --pretty false`、`npm run lint` 均无错误输出，组合命令退出 0。

草稿封面预览与媒体不可变内容保护组 `test_media_v305.py test_publication_commands_v305.py test_editorial_revision_v300.py` 33 passed，退出 0，77.46 秒。首次误写不存在的 `test_editorial_revision.py`，退出 1、无测试执行，随后定位到真实路径。增加媒体资料快照及公开重发布集成后，扩大组 47 passed/1 failed；失败是夹具给 Edition 而非 Work 的发布投影置为 current，已根据实际 event.object_type/object_id 修正。

最终同组增加 `test_catalog_contracts_v305.py`，48 passed，退出 0，48.91 秒，XML 为临时目录 `stl-v305-media-final4.xml`。`npx playwright test --config=playwright.cataloging.config.ts` 3 passed，退出 0，56.7 秒。覆盖真实媒体上传/保存/选择与编目，不等于真实 Meilisearch 投影、PostgreSQL 锁或生产验收。页面取消后的 Broken pipe 和 Editor 首页统计 403 仍观察到。

初始接口用例因运行中修改服务常量导致旧模块 ImportError，未计为有效通过。稳定源码重跑 8 passed，增加并发检查后 9 passed。作品封面发布隔离组 35 passed，增加发布引用保护和小图 srcset 后 36 passed，退出 0，67.90 秒。

真实浏览器先发现 select 标签定位、Image 私网 URL 拦截以及保存刷新卸载编辑器问题，均保留失败记录并修根因。16:53 重新构建和 3 项真实 E2E 全通过，包含上传、元数据、裁切预览和选作书目封面。未关闭 SSRF 防护，业务 API 不 mock。后续独立媒体用例结果以当前进度为准。

`test_functional_readiness_v305.py test_system_health_schedule_v292.py test_admin_preview_and_health_permissions_v292.py` 34 passed，退出 0，66.99 秒。空数据库不得通过功能门槛新增用例 1 passed。公网 JSON 检查补齐后 `test_functional_readiness_v305.py` 8 passed，退出 0，54.43 秒。migration drift、TypeScript、lint、当前 Web build 和 npm test 181 项通过。均未请求生产。

2026-09-07 00:59 完整后端 XML 为 1074 tests、86 failures、32 skipped，956 passed，371.484 秒，进程句柄续接后失效，未假定退出码。后续针对 Reader/收藏/范围检索/Range 的旧正式发布 fixture 增加显式活动 revision，27 passed，退出 0。

前端 17 个失败对应旧组件位置、文案、候选控件与诊断职责迁移，逐项保留来源和人工决定断言后全部收口；并发现学者 external_identifier 助手真实遗漏，已补回现有受控组件。`npm test` 重新构建并自动发现全部测试，181 passed、0 failed、退出 0。TypeScript 和完整 lint 退出 0。不是全部浏览器功能验收。

Semantic/Opinion 原组补活动 Catalog/Document revision 后 23 pass/6 fail，再补发布绑定 job、异步撤回 outbox 和 DB 读权限 fixture 后 28 pass/1 fail。最终旧 fallback 测试正确要求保留空 Asset 资格过滤，定向 1 pass。没有恢复已移除的同步索引入口或放宽匿名过滤。

### Publication 命令、diff 与回滚

首组 36 passed/1 failed，旧精简 fixture 没有 document key，恢复路径插入空 key 破坏结构一致性。已保持原始快照结构后 37 passed。扩大旧 EditorialRevision 组 26 passed/2 failed，两条旧契约改为真实 maintenance HTTP 与标准扁平字段集合后定向 2 passed。

增加 Model/QuerySet 内容不可变保护后，原 Reader 夹具在建 revision 后再写指针被正确拒绝。改成创建时传入文档信息后 `test_publication_commands_v305.py test_publication_invariants_v305.py test_cataloging_intelligence_v304.py test_editorial_revision_v300.py` 38 passed、退出 0、46.97 秒。2 条 pypinyin 弃用警告。不是 PostgreSQL 并发或真实索引恢复验证。

00:55 最终构建、真实编目 Playwright 2 passed（51.8 秒）、TypeScript、lint、api:check 和 migration drift 全部退出 0。健康状态独立性新增用例单独 1 passed。浏览器包含字段 diff，不包含真实生产 rollback 或真实 Meilisearch 恢复。

错误契约、response serializer 与 schema 权限组先为 30 pass/1 fail，失败是查询计数 fixture 缺少 django_db 标记。修正后 `test_api_contract_v305.py` 6 passed、退出 0，46.23 秒，包含 schema 零业务查询及权限拒绝。

`npm run api:check` 与 `npx tsc --noEmit --pretty false` 退出 0。generated TS 首次发现 response.status 被默认为 optional，已修正真实 response serializer 并重新生成，未手改生成类型。

23:57 当前代码构建与 `npm run test:e2e:cataloging` 2 passed，退出 0，56.2 秒。MetadataCandidate/会话/实体/字段助手 31 passed。上述不覆盖全量 API schema，目前只对 verified 编目范围严格检查，见 V3.0.5_API_CONTRACT.md。

首组 39 passed/2 failed，失败为字符串年份规范化顺序。新增 integer normalizer 后 `test_session_metadata_v305.py test_catalog_contracts_v305.py test_admin_foundation_backfill.py test_ingestion_admin_foundation.py` 26 passed，退出 0，43.01 秒，2 个既有依赖警告。首次误写不存在的 test_admin_foundation.py 未运行测试，已纠正并保留记录。

最终补上字段助手标量采用验证后，契约与 Field Assistant 20 passed，退出 0，44.01 秒。19:28 当前构建成功，真实 Playwright 2 passed，退出 0，49.1 秒，增加无上传新建作者并关联的真实 POST 与回读。

前端临时状态按上下文隔离后 `npx tsc --noEmit --pretty false`、全仓 `npm run lint` 均退出 0，lint 不再有基线 8 errors/3 warnings。`node --import tsx --test tests/context-state-v305.test.mjs` 3 passed，退出 0。未禁用 React 规则；旧请求仍取消，过时回调不能覆写新上下文。

`test_catalog_contracts_v305.py test_cataloging_sessions_v305.py test_session_candidates_v305.py test_field_assistant_v304.py test_cataloging_intelligence_v304.py` 50 passed，退出 0，44.40 秒。增加 session 发布状态关联后同组去除无改动的 field assistant 文件，44 passed，退出 0，45.35 秒。均有 2 个 pypinyin 依赖弃用警告。

覆盖契约唯一/旧接口同源、ISBN10/13 checksum、DOI 规范化和空值、DRF/发布同一验证、纯书目正式 revision/公开详情、没有虚构 Reader Asset、旧文档模式仍要求文件、坏文件不能规避门槛、非法 ISBN 阻断。投影完成采用受控状态，不称真实外部消费者可用。

ISBN 验证依据 [International ISBN Agency](https://www.isbn-international.org/index.php/content/what-isbn/10) 和其用户手册校验位规则；DOI 依据 [DOI Foundation Handbook](https://www.doi.org/doi-handbook/html/)。只实施语法/校验与规范化，没有向注册机构声称验证实际书目身份。

### CatalogingSession 基础

后续服务集成 `test_cataloging_sessions_v305.py test_session_candidates_v305.py test_research_orchestrator_v292.py test_candidate_verification_v301.py test_entity_resolution_decisions.py test_field_assistant_v304.py` 退出 1，110 passed/1 failed。唯一失败旧 preview fixture 没有活动修订。显式建立 metadata-only 测试 snapshot 后，`-k preview_urls_and_public_visibility` 3 passed/51 deselected，退出 0。没有放宽公开产品过滤。

Web 新文件 typecheck、定向 ESLint 通过，构建成功。`npm run test:e2e:cataloging` 启动隔离本地 Django 和 Vinext production server。第一次命令路径失败，第二次 dev 地址超时；确认 CLI 参数是 --hostname 后采用 production server。浏览器首次 1 pass/1 标题选择器冲突，修正只限定 h1 后 `npx playwright test --config=playwright.cataloging.config.ts -g non-superuser` 1 passed，退出 0。Reader 拒绝和 Editor 新建/保存/重开均已有真实 API 证据，非生产 E2E。

E2E fixture 脚本始终创建新的临时 SQLite，不接收外部 DB 地址，只监听 127.0.0.1。仅 runtime-config.js 指定开发 API 地址，业务请求不 mock。观察到导航取消 stream 错误和 Editor dashboard 403，后续仍需治理。截图显示新会话入口尚未应用旧 Workbench focus-mode，需要在工作台改造时收敛。

`DATABASE_URL=sqlite:///:memory: .venv/Scripts/python.exe -m pytest tests/test_cataloging_sessions_v305.py --tb=short --junitxml=.../stl-v305-sessions.xml` 退出 0，12 passed、2 个依赖警告，40.21 秒。覆盖真实无上传草稿、幂等创建、已有版本复用、真实上传引用、保留草稿的结束操作、发布中禁止结束、数据库唯一约束、跨版本基线拒绝、普通非 superuser 管理员及匿名/读者拒绝。候选迁移和纯书目发布尚未包含。

新增 `tests/test_publication_invariants_v305.py`。修复前 3 failed、6 passed，明确复现错误快照、公开 detail 200 和发布未阻止错误指针。修复后运行该文件与 `test_cataloging_intelligence_v304.py`、`test_catalog_consistency_audit_v304.py`，22 passed、2 个依赖警告，退出 0，45.35 秒。XML 为本机临时目录 `stl-v305-publication-guard.xml`。

`manage.py check` 与 `makemigrations --check --dry-run` 退出 0。本项只改变服务端资格判断和写入前置校验，无 schema、TS 或界面变更；前端基线和真实生产/权限/E2E 状态不由该专项覆盖。旧会话测试句柄不可读后仅重跑末组，未重复完整基线。

### 完整基线结果

- Backend `DATABASE_URL=sqlite:///:memory: .venv/Scripts/python.exe -m pytest tests --tb=line --junitxml=.../stl-v305-baseline-backend.xml` 退出 1。90 failed、888 passed、32 skipped、2 warnings，352.17 秒。XML 在本机临时目录，未提交。
- Frontend 自动枚举全部 `tests/*.test.mjs`，以 `node --import tsx --test --test-force-exit` 运行。退出 1，178 项中 17 失败、161 通过。全部失败名单保存为 XML。部分 3.0.4 测试未在旧固定 npm 清单中，后续门槛改为自动发现。
- `npx tsc --noEmit --pretty false` 退出 0。
- `npm run lint` 退出 1，8 errors、3 warnings。5 个受影响组件已有精确位置记录，未禁用规则。
- `npm run build` 退出 0，仍有 Vite 未来 configLoader 兼容提示，非本轮构建失败。
- 本机没有可调用的 docker/psql CLI。真实 PostgreSQL、Redis、Celery 及生产恢复门槛仍待建立隔离环境，不用 SQLite 成绩代替。

backend、frontend、typecheck、lint、build、public/auth/workflow Playwright、migration、consistency、functional smoke、external smoke 全部仍需最终候选树验证。不得部署。
