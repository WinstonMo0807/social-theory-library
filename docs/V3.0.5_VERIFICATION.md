# 3.0.5 验证记录

所有状态仅对应本轮命令和当时源码。基线为 `340b94f`。本地 SQLite 不代表 PostgreSQL 并发或生产可用。

## Phase 0 基线

| 检查 | 命令与环境 | 状态 |
| --- | --- | --- |
| Git | `git status --porcelain=v1`、`git branch --show-current`、`git log -1 --oneline` | PASS，起始干净，基线分支和 340b94f 已核对 |
| 后端关键面 | api 目录，`DATABASE_URL=sqlite:///:memory:`，`.venv/Scripts/python.exe -m pytest tests/test_cataloging_intelligence_v304.py tests/test_field_assistant_v304.py tests/test_catalog_consistency_audit_v304.py tests/test_admin_workflow_v280.py tests/test_resilient_publication_v260.py --tb=short` | FAIL，退出 1，29 通过、12 失败、2 个依赖弃用警告，50.13 秒 |
| 前端关键面 | web 目录，`node --experimental-strip-types --test --test-force-exit tests/admin-workflow-v280.test.mjs tests/workflow-research-candidates-v290.test.mjs tests/reader-upload-layout.test.mjs tests/public-page-control-v303.test.mjs` | FAIL，退出 1，24 项中 18 通过、6 失败 |

前端失败名称为 section validation、contributor editor、focus mode、classification/curation pickers、front matter contributors、candidate inspector。当前输出显示多个旧文案/源码形态断言与 3.0.4 不符。必须逐项审查意图和真实行为后处理，不能直接删除失败测试或把失败算作通过。

后端失败包括一个候选采用状态、六个旧工作台字段/步骤契约，以及五个旧发布/Reader 夹具或移除的同步 index 入口。当前 3.0.4 发布专项和一致性审计在该组中通过，但不能覆盖这些失败。处理优先保留作者确认、公开资格和异常情况下旧 revision 的真实行为要求。

## 待执行的最终门槛

### 首个发布安全修复

### 字段契约和纯书目发布

### MetadataCandidate 会话迁移

### API 契约基础

### 全回归差异治理

2026-09-10 推荐图例切片先以旧 API 的替换/删除复现 2 failed，旧文件被直接删除，64.06 秒。接入统一媒体和编辑修订后首组 36 passed，退出 0，70.48 秒。中间一轮没有完成报告且句柄失效，未计通过。扩展首组 53 passed/1 failed，发现推荐图例仍触发正文索引；补 Public-only 字段规则后 56 passed，退出 0，110.37 秒。旧报告类型 fixture 缺发布机构阻断一次，保留要求并补测试数据后 57 passed，退出 0，91.52 秒。

推荐图例最终 `test_recommendation_media_v305.py test_media_v305.py test_reader_cover_semantic.py::test_admin_recommendation_image_override_is_public_and_removable test_publication_commands_v305.py test_editorial_revision_v300.py test_catalog_contracts_v305.py` 59 passed，退出 0，98.98 秒。覆盖旧文件保留、发布/清除/恢复、FK 引用保护、图例与书封独立、图片只更新展示、多版本上下文、待发布类型以及匿名/Reader 拒绝。报告 stl-v305-recommendation-context-final.xml，生产数据库及真实索引仍待核实。

前端最新 `npm test` 重新构建及 188 项通过，退出 0，Node 测试 40.514 秒。TypeScript、Django check、migration drift、生成契约检查通过。图例编辑器提取时误删了后方已有 helper，TypeScript 立即发现；已从当前 HEAD 精确恢复原 helper 后重跑，没有用忽略错误通过构建。

Playwright 共 6 项，其中媒体用例新增推荐图选择、真图 naturalWidth=640、移除选择和书封不变。第一次 6 passed/1.8 分钟但 FieldAssistant API 有 SQLite lock 500，因此不计无错误验收。隔离 fixture 路径校验后使用本机 Django 支持的 IMMEDIATE 事务，并新增正常编目 500 响应断言；随后 6 passed，退出 0，2.0 分钟，无 database is locked 输出。最后修改多版本参数后再次运行，10:35:05 的 .last-run.json 为 passed/无失败；续接后句柄失效，最终退出码未知。生产 PostgreSQL 设置和业务事务锁未改。

推荐图例切片最终 `npm run lint`、`npm run api:check` 组合命令退出 0，Django check 与 makemigrations --check --dry-run 退出 0，diff 检查通过。已启动完整后端 checkpoint3，运行过程中应用源码保持不变，最终结果另记，不据专项成绩推算整体失败数。

2026-09-10 公开 Reader/证据首组 23 passed/2 failed，90.81 秒。修正测试 helper 指定 Reader Asset 和 RAG 正式全文前置条件后，26 项通过。新增旧封面归属测试先复现失败，1 failed/26 passed，66.49 秒。最小代码修复后 `test_cover_identity_v305.py test_work_curated_claims_v300.py test_public_knowledge_core_consumers_v301.py test_admin_configuration.py::test_reading_history_coalesces_recent_page_updates test_ingestion_batch_policies.py::test_registered_asset_requires_login_but_remains_available_to_reader test_library_rag_task6.py test_media_v305.py test_reader_recent_and_saved.py test_scoped_search.py` 62 passed，退出 0，70.69 秒，报告 stl-v305-public-reader-cover-green.xml。没有放宽生产公共过滤；测试活动快照不证明真实索引可用。

工作台/智能编目/发布命令首组 29 passed/1 failed，50.42 秒。剩余用例原本假定人工关联会覆写机器报告，核对后保持原报告，并新增实际流水线重跑。复现旧代码用 OCR 覆盖人工关联 Work 的题名，定向 1 failed，78.47 秒。只读有效状态修复后 `test_manual_work_reconciliation_v305.py test_admin_workflow_v280.py test_ingestion_reconciliation.py test_entity_resolution_decisions.py test_session_candidates_v305.py` 39 passed，退出 0，49.45 秒。强 Edition 复用保护及发布命令扩大组待记录最终结果。

最后同组加入 `test_cataloging_intelligence_v304.py test_publication_commands_v305.py`，59 passed，退出 0，94.71 秒，报告 stl-v305-manual-work-reuse-final.xml。包含更强 existing_edition 保护不降级、明确人工关联与撤销、旧报告不覆盖、发布前置条件与恢复发布。Django check、makemigrations --check --dry-run、npm run api:check、git diff --check 均退出 0。未执行生产 PostgreSQL 或真实并发 Worker 验收。

2026-09-09 最后 `test_metadata_review_contract_v305.py test_catalog_contracts_v305.py test_ingestion_admin_foundation.py test_admin_foundation_backfill.py test_complete_ingestion_workflow.py test_ingestion_integration.py` 34 passed，退出 0，180.31 秒，2 个既有 pypinyin 弃用警告，报告 stl-v305-review-ingestion-final.xml。Django check、makemigrations --check --dry-run、npm run api:check 与 diff 检查退出 0。验证原 PDF/Annotation 不覆盖、替换期间旧版本可读、激活后新版本服务、Range/代理条件、撤回后隐藏及重新上架后等待处理。不证明外部索引或模型实际可用。

Reader/替换集成到达活动索引断言后发现派发失败分支误改 SemanticIndexVersion。新 `test_semantic_dispatch_safety_v305.py` 先运行 active/building 两组，1 failed/1 passed，明确复现 active 被改为 failed，78.33 秒。修复仅让未完成候选失败，活动和保留版本不变，并拒绝迟到的旧 task_id 错误覆盖已执行或已重派的任务。与 `test_durable_pipeline_dispatch.py` 共 12 passed，退出 0，87.57 秒，已单独保存提交 8405d9f。

复核/旧 backfill/完整入库扩大组为 31 passed/3 failed，188.69 秒。三个失败均是新测试辅助函数误将 ProjectionState.source_revision 写成 canonical_revision，已修正测试字段名。此前辅助函数确认全部投影触发 projection_revision_not_ahead，已改为只确认事件实际登记的 required_projections，不扩大源版本或放宽数据库约束。修正后入库组为 4 passed/1 failed，179.76 秒，最后一项仅在重新上架末步遗漏消费者确认。已经完成的搜索/Range/替换/旧笔记保留断言未删除；补齐重新上架的可见性时序后最后扩大组正在收取结果。
元数据复核契约组 `test_metadata_review_contract_v305.py test_catalog_contracts_v305.py test_ingestion_admin_foundation.py` 首次 21 passed/2 failed，两条新错误断言未使用统一 details 结构。修正并增加出版日期、归档身份原子拒绝后，25 passed，退出 0，43.81 秒，报告 stl-v305-review-contract-final.xml。`npm run api:check` 退出 0。完整发布消费者集成仍待继续，未更改生产数据或权限。

`test_complete_ingestion_workflow.py` 两项中文图书/期刊文章 HTTP 上架已通过，合跑 Reader/替换文件场景时为 2 passed/1 failed，87.00 秒。第三项请求本来未传 language 却依赖默认中文，现明确提交语言；首次运行还观察到旧 eager embedding 尝试缺本地模型，因此该目录/Reader 测试改用非 eager 队列并保留真实持久事件。消费者完成由 tests/publication_fixtures.py 明确确认，不能作为真实 embedding、Meilisearch 或 Celery 运行证据。

字段助手规范关系修复 `test_field_assistant_legacy_v305.py test_field_assistant_v304.py test_v3_canonical_identity.py test_session_candidates_v305.py` 37 passed，退出 0，54.61 秒。8 个新增用例覆盖草稿、已发布编辑修订、缺映射、错类型、未发布目标、身份不同、旧人工关系不覆盖及字段决定失败时事务回滚。静态 inventory 断言恢复通过，不声称这等同生产运行时零旧写。

加入映射与目标的事务内 select_for_update 后，`test_field_assistant_legacy_v305.py test_v3_canonical_identity.py` 23 passed，退出 0，49.00 秒。Django check、makemigrations --check --dry-run 和 git diff --check 退出 0。SQLite 能验证应用流程及事务失败还原，不能证明 PostgreSQL 并发锁。

2026-09-09 `DATABASE_URL=sqlite:///:memory: .venv/Scripts/python.exe -m pytest tests --tb=line --junitxml=.../stl-v305-wave2-backend.xml` 完整 XML 为 1095 tests、61 failures、0 errors、32 skipped、1002 passed，396.086 秒。报告写入时间 11:12:13。续接后命令句柄已失效，因此退出码未知。所有已有新媒体专项在全量中通过；不能把剩余失败视为全是旧测试问题。

首页权限切片 `npx tsc --noEmit --pretty false` 与 `npm run lint` 退出 0，首组权限/导航 26 项通过。`npm test` 曾出现集中 SSR 错误，原因为待核实。无改动完整 Node 重跑 184 passed，退出 0；最新重新构建后的 `npm test` 184 passed，退出 0，Node 部分 17.061 秒。不是通过降低断言或增加重试次数得到的结果。

最后 `npx playwright test --config=playwright.cataloging.config.ts` 6 passed，退出 0，1.1 分钟。增加非 superuser Administrator 测试账户，仅存在一次性本地 SQLite。Editor 浏览器不发统计请求，显式绕过页面的 API 请求仍 403；管理员得到真实 200。队列失败用 route.abort 注入网络中断，恢复后重试真实接口，不伪造成功业务响应。普通编目、读者拒绝及媒体旅程仍通过。

### 分层功能健康

### 媒体库与作品封面

2026-09-09 `node --import tsx --test --test-force-exit tests/settings-controls.test.mjs` 23 passed，退出 0，修正的是新增媒体导航的预期路由。`npm test` 完整构建及 181 项通过，退出 0。`npm run api:check`、`npx tsc --noEmit --pretty false`、`npm run lint` 均无错误输出，组合命令退出 0。

草稿封面预览与媒体不可变内容保护组 `test_media_v305.py test_publication_commands_v305.py test_editorial_revision_v300.py` 33 passed，退出 0，77.46 秒。首次误写不存在的 `test_editorial_revision.py`，退出 1、无测试执行，随后定位到真实路径。增加媒体资料快照及公开重发布集成后，扩大组 47 passed/1 failed；失败是夹具给 Edition 而非 Work 的发布投影置为 current，已根据实际 event.object_type/object_id 修正。

最终同组增加 `test_catalog_contracts_v305.py`，48 passed，退出 0，48.91 秒，XML 为临时目录 `stl-v305-media-final4.xml`。`npx playwright test --config=playwright.cataloging.config.ts` 3 passed，退出 0，56.7 秒。覆盖真实媒体上传/保存/选择与编目，不等于真实 Meilisearch 投影、PostgreSQL 锁或生产验收。页面取消后的 Broken pipe 和 Editor 首页统计 403 仍观察到。

初始接口用例因运行中修改服务常量导致旧模块 ImportError，未计为有效通过。稳定源码重跑 8 passed，增加并发检查后 9 passed。作品封面发布隔离组 35 passed，增加发布引用保护和小图 srcset 后 36 passed，退出 0，67.90 秒。

真实浏览器先发现 select 标签定位、Image 私网 URL 拦截以及保存刷新卸载编辑器问题，均保留失败记录并修根因。16:53 重新构建和 3 项真实 E2E 全通过，包含上传、元数据、裁切预览和选作书目封面。未关闭 SSRF 防护，业务 API 不 mock。后续独立媒体用例结果以当前进度为准。

`test_functional_readiness_v305.py test_system_health_schedule_v292.py test_admin_preview_and_health_permissions_v292.py` 34 passed，退出 0，66.99 秒。空数据库不得通过功能门槛新增用例 1 passed。公网 JSON 检查补齐后 `test_functional_readiness_v305.py` 8 passed，退出 0，54.43 秒。migration drift、TypeScript、lint、当前 Web build 和 npm test 181 项通过。均未请求生产。

2026-09-07 00:59 完整后端 XML 为 1074 tests、86 failures、32 skipped，956 passed，371.484 秒，进程句柄续接后失效，未假定退出码。后续针对 Reader/收藏/范围检索/Range 的旧正式发布 fixture 增加显式活动 revision，27 passed，退出 0。

前端 17 个失败对应旧组件位置、文案、候选控件与诊断职责迁移，逐项保留来源和人工决定断言后全部收口；并发现学者 external_identifier 助手真实遗漏，已补回现有受控组件。`npm test` 重新构建并自动发现全部测试，181 passed、0 failed、退出 0。TypeScript 和完整 lint 退出 0。不是全部浏览器功能验收。

Semantic/Opinion 原组补活动 Catalog/Document revision 后 23 pass/6 fail，再补发布绑定 job、异步撤回 outbox 和 DB 读权限 fixture 后 28 pass/1 fail。最终旧 fallback 测试正确要求保留空 Asset 资格过滤，定向 1 pass。没有恢复已移除的同步索引入口或放宽匿名过滤。

### Publication 命令、diff 与回滚

首组 36 passed/1 failed，旧精简 fixture 没有 document key，恢复路径插入空 key 破坏结构一致性。已保持原始快照结构后 37 passed。扩大旧 EditorialRevision 组 26 passed/2 failed，两条旧契约改为真实 maintenance HTTP 与标准扁平字段集合后定向 2 passed。

增加 Model/QuerySet 内容不可变保护后，原 Reader 夹具在建 revision 后再写指针被正确拒绝。改成创建时传入文档信息后 `test_publication_commands_v305.py test_publication_invariants_v305.py test_cataloging_intelligence_v304.py test_editorial_revision_v300.py` 38 passed、退出 0、46.97 秒。2 条 pypinyin 弃用警告。不是 PostgreSQL 并发或真实索引恢复验证。

00:55 最终构建、真实编目 Playwright 2 passed（51.8 秒）、TypeScript、lint、api:check 和 migration drift 全部退出 0。健康状态独立性新增用例单独 1 passed。浏览器包含字段 diff，不包含真实生产 rollback 或真实 Meilisearch 恢复。

错误契约、response serializer 与 schema 权限组先为 30 pass/1 fail，失败是查询计数 fixture 缺少 django_db 标记。修正后 `test_api_contract_v305.py` 6 passed、退出 0，46.23 秒，包含 schema 零业务查询及权限拒绝。

`npm run api:check` 与 `npx tsc --noEmit --pretty false` 退出 0。generated TS 首次发现 response.status 被默认为 optional，已修正真实 response serializer 并重新生成，未手改生成类型。

23:57 当前代码构建与 `npm run test:e2e:cataloging` 2 passed，退出 0，56.2 秒。MetadataCandidate/会话/实体/字段助手 31 passed。上述不覆盖全量 API schema，目前只对 verified 编目范围严格检查，见 V3.0.5_API_CONTRACT.md。

首组 39 passed/2 failed，失败为字符串年份规范化顺序。新增 integer normalizer 后 `test_session_metadata_v305.py test_catalog_contracts_v305.py test_admin_foundation_backfill.py test_ingestion_admin_foundation.py` 26 passed，退出 0，43.01 秒，2 个既有依赖警告。首次误写不存在的 test_admin_foundation.py 未运行测试，已纠正并保留记录。

最终补上字段助手标量采用验证后，契约与 Field Assistant 20 passed，退出 0，44.01 秒。19:28 当前构建成功，真实 Playwright 2 passed，退出 0，49.1 秒，增加无上传新建作者并关联的真实 POST 与回读。

前端临时状态按上下文隔离后 `npx tsc --noEmit --pretty false`、全仓 `npm run lint` 均退出 0，lint 不再有基线 8 errors/3 warnings。`node --import tsx --test tests/context-state-v305.test.mjs` 3 passed，退出 0。未禁用 React 规则；旧请求仍取消，过时回调不能覆写新上下文。

`test_catalog_contracts_v305.py test_cataloging_sessions_v305.py test_session_candidates_v305.py test_field_assistant_v304.py test_cataloging_intelligence_v304.py` 50 passed，退出 0，44.40 秒。增加 session 发布状态关联后同组去除无改动的 field assistant 文件，44 passed，退出 0，45.35 秒。均有 2 个 pypinyin 依赖弃用警告。

覆盖契约唯一/旧接口同源、ISBN10/13 checksum、DOI 规范化和空值、DRF/发布同一验证、纯书目正式 revision/公开详情、没有虚构 Reader Asset、旧文档模式仍要求文件、坏文件不能规避门槛、非法 ISBN 阻断。投影完成采用受控状态，不称真实外部消费者可用。

ISBN 验证依据 [International ISBN Agency](https://www.isbn-international.org/index.php/content/what-isbn/10) 和其用户手册校验位规则；DOI 依据 [DOI Foundation Handbook](https://www.doi.org/doi-handbook/html/)。只实施语法/校验与规范化，没有向注册机构声称验证实际书目身份。

### CatalogingSession 基础

后续服务集成 `test_cataloging_sessions_v305.py test_session_candidates_v305.py test_research_orchestrator_v292.py test_candidate_verification_v301.py test_entity_resolution_decisions.py test_field_assistant_v304.py` 退出 1，110 passed/1 failed。唯一失败旧 preview fixture 没有活动修订。显式建立 metadata-only 测试 snapshot 后，`-k preview_urls_and_public_visibility` 3 passed/51 deselected，退出 0。没有放宽公开产品过滤。

Web 新文件 typecheck、定向 ESLint 通过，构建成功。`npm run test:e2e:cataloging` 启动隔离本地 Django 和 Vinext production server。第一次命令路径失败，第二次 dev 地址超时；确认 CLI 参数是 --hostname 后采用 production server。浏览器首次 1 pass/1 标题选择器冲突，修正只限定 h1 后 `npx playwright test --config=playwright.cataloging.config.ts -g non-superuser` 1 passed，退出 0。Reader 拒绝和 Editor 新建/保存/重开均已有真实 API 证据，非生产 E2E。

E2E fixture 脚本始终创建新的临时 SQLite，不接收外部 DB 地址，只监听 127.0.0.1。仅 runtime-config.js 指定开发 API 地址，业务请求不 mock。观察到导航取消 stream 错误和 Editor dashboard 403，后续仍需治理。截图显示新会话入口尚未应用旧 Workbench focus-mode，需要在工作台改造时收敛。

`DATABASE_URL=sqlite:///:memory: .venv/Scripts/python.exe -m pytest tests/test_cataloging_sessions_v305.py --tb=short --junitxml=.../stl-v305-sessions.xml` 退出 0，12 passed、2 个依赖警告，40.21 秒。覆盖真实无上传草稿、幂等创建、已有版本复用、真实上传引用、保留草稿的结束操作、发布中禁止结束、数据库唯一约束、跨版本基线拒绝、普通非 superuser 管理员及匿名/读者拒绝。候选迁移和纯书目发布尚未包含。

新增 `tests/test_publication_invariants_v305.py`。修复前 3 failed、6 passed，明确复现错误快照、公开 detail 200 和发布未阻止错误指针。修复后运行该文件与 `test_cataloging_intelligence_v304.py`、`test_catalog_consistency_audit_v304.py`，22 passed、2 个依赖警告，退出 0，45.35 秒。XML 为本机临时目录 `stl-v305-publication-guard.xml`。

`manage.py check` 与 `makemigrations --check --dry-run` 退出 0。本项只改变服务端资格判断和写入前置校验，无 schema、TS 或界面变更；前端基线和真实生产/权限/E2E 状态不由该专项覆盖。旧会话测试句柄不可读后仅重跑末组，未重复完整基线。

### 完整基线结果

- Backend `DATABASE_URL=sqlite:///:memory: .venv/Scripts/python.exe -m pytest tests --tb=line --junitxml=.../stl-v305-baseline-backend.xml` 退出 1。90 failed、888 passed、32 skipped、2 warnings，352.17 秒。XML 在本机临时目录，未提交。
- Frontend 自动枚举全部 `tests/*.test.mjs`，以 `node --import tsx --test --test-force-exit` 运行。退出 1，178 项中 17 失败、161 通过。全部失败名单保存为 XML。部分 3.0.4 测试未在旧固定 npm 清单中，后续门槛改为自动发现。
- `npx tsc --noEmit --pretty false` 退出 0。
- `npm run lint` 退出 1，8 errors、3 warnings。5 个受影响组件已有精确位置记录，未禁用规则。
- `npm run build` 退出 0，仍有 Vite 未来 configLoader 兼容提示，非本轮构建失败。
- 本机没有可调用的 docker/psql CLI。真实 PostgreSQL、Redis、Celery 及生产恢复门槛仍待建立隔离环境，不用 SQLite 成绩代替。

backend、frontend、typecheck、lint、build、public/auth/workflow Playwright、migration、consistency、functional smoke、external smoke 全部仍需最终候选树验证。不得部署。
