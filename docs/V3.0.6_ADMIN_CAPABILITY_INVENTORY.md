# 3.0.6 后台路由与能力清单

首次审计：2026-09-13；当前源码入口复核：2026-09-20。本文件同时保留实施前清单和逐项实施证据，不能将基线列中的旧行为读作现状。管理端最新功能说明见[管理端画像](ADMIN_ARCHITECTURE_PROFILE.md)。公开编辑—读取的权威字段登记继续复用 `api/catalog/services/public_knowledge_control.py`，不在此建立第二份字段注册表。

## 2026-09-20 当前入口摘要

- 重新枚举仍为43个后台page.tsx，逐一对应R01—R43；当前菜单来自admin-shell.tsx的六组，不是下方首次审计的五组。
- 上传、手工会话、维护共用WorkflowEditor；队列含非上传事项，library按作品/Edition/质量查询并分页。整页显式保存、字段预填及撤销、封面四种选择、Edition文件/OCR操作见管理端画像及最终交付。
- 理论时间线已纳入`/admin/theories/{nodeId}?section=timeline`；R39旧URL保留但不单列菜单。R04/R19/R24及publication的旧item入口通过preservingAdminRedirect保留参数；R37仍复用同一理论组件，并非已删除URL。
- 撤回馆藏统一CanWithdrawWork（A/O），Editor仍可发布。ProcessingCenter GET允许后台只读；POST与新增OCR控制为RETRY_JOBS（A/O），菜单另按系统状态能力显示。Owner敏感能力不扩张。
- 当前运行3.0.6已上线；下方“未测试/未部署”是各阶段时点。最终只执行授权的最小发布验证，非43路由全部场景在最终源码的新验收，见[V3.0.6_COMPLETION_RELEASE](V3.0.6_COMPLETION_RELEASE.md)。

## 工作记录与证据边界

2026-09-20最终追加封面：R09、R12及手工编目共用WorkflowEditor中的EditionCoverEditor，GET/POST `catalog/admin/editions/{edition}/cover/`；GET为CanAccessBackOffice，写入CanEditMetadata。对象/源文件指纹、幂等、当前版本和冲突检查由接口执行。早期候选/任选PDF页/上传/默认均复用原CoverCandidate/媒体/字段决定/修订，公开影响仅在正式发布时生效，失败可刷新、重新准备或换选，历史保留。对应A07/A13/A14/A26/A27/A32；`test_cover_workbench_v306.py`及两项既有封面/两项完整入库回归共16个PG通过，`admin-usability-v306.spec.ts`新封面流程与两宽度通过。未增加后台page或删除兼容入口。应用59e已部署；证据与其他8项补漏结果见V3.0.6_COMPLETION_RELEASE，不把此组局部结果替代全部43入口重验。

2026-09-20本轮剩余开发交付（未测试）：本清单原路由/能力协议继续有效，未新增或删除page入口，也未重新运行路由盘点/验收。R17新增真实全量分页与完整计数：普通/语义任务由同一查询响应返回，`job_type/status/q/page/page_size`筛选分页；OCR暂停记录有独立`ocr_query/ocr_inventory_page`及完整总量；上传和人工待办分别翻页。动态标题来自当前Work/Edition及既有编辑修订，兼容item/asset回链保留，操作仍由原RETRY_JOBS决定。R09/R12/R17的OCR共用版本内控制及真实进度，不要求输入UploadItem ID。名称/页码/权限呈现与用户截图要求对应A03/A08/A10/A12/A17/A18/A26/A27/A31，均未运行新测试。

R07/R30补学科切换/离开保护、文字与图片保存结果分开；R21/R25/R26/R35–R37的别名、分类、机构和阅读建议接可撤销预填及显式整页保存；R10和对象侧栏继续解释已保存内容、公开范围及关联写入，未保存时禁止误发布。候选采用沿用CanReviewCandidate，后台对象保存沿用既有编辑权限/If-Match；学者译名不丢原结构化身份资料。R34的主题共用编辑器及兼容关系未删除；R39仍是理论管理的次级/兼容全库入口，不另增导航项。对应A07/A13–A16/A19/A24/A26/A32；本轮只标“已修改、未测试”，下表历史“验证后保留/通过”不覆盖这些新改动。

媒体、推荐、人员整理、服务配置、专业诊断、审计、备份、用户权限、公开前台和Reader沿用原清单中的已实现能力，本轮不冒充对它们重新验收。当前开发交付详见V3.0.6_USABILITY_REVIEW，用户取消本轮开发后的所有测试/检查/核对，LoRA排除，未生产切换。

2026-09-20测试暂停后的新增代码尚未验收：R09/R12书目文件区和R17处理中心共用`GET ingestion/processing-center/?ocr_edition_id=&work_id=&ocr_page=`与同端点POST `start_ocr/pause_catalog_ocr/resume_catalog_ocr/cancel_catalog_ocr`。读取允许原LibraryStaff，命令要求RETRY_JOBS（Admin/Owner），没有扩权给Editor；对象始终是所选Edition/Asset，旧上传链接无需改变，处理中心URL保留work/edition。原ProcessingJob/AuditEvent及公开事件承担进度/幂等/恢复，无新表、第二套处理器或新page.tsx。映射A03/A08/A13/A17/A18/A26/A28/A30/A32，新增`test_catalog_ocr_v306.py`只有此前红测，之后未运行。R09/R12的标量助手及R学者/主题/学科/理论的现有助手也有新对话框/反馈/上下文代码，不能沿用下表旧结果称为新代码通过；统一测试待全部功能实现后执行。

01:41本次收口：身份增量96 API、242前端单测通过；同一业务源码的扩大浏览器26个通过场景与修正测试焦点准备后4个填写通过场景去重为27，详见验证账本（不称单批27全过）。五视口/键盘及修前修后图已归档，43路由不变。仅关闭此次身份填写与发现的局部布局问题，表中其他未完成项和整版门槛不据此改成通过。

2026-09-20身份填写增量：重新枚举仍为43个page.tsx，未增加平行工作台或删除兼容入口。上传、手工与维护共用WorkflowEditor，作者/译者/出版社建议进入当前表单，保存继续由原IsCatalogEditor和整页权限检查执行；人物创建/合并权限不变。映射A13/A15/A22/A24/A26/A27/A32到`test_workspace_edits_v306.py`、`assisted-save-v306-live.spec.ts`及原候选/编目权限回归。新96项API及15项浏览器通过只覆盖此增量和所列回归，不能替代表中每一模块验收；图片复核发现的窄屏标题/宽屏表单问题另有真实红测及修后证据。

2026-09-20实施增量：再次枚举仍为43个page.tsx。R35/R36理论编辑增加`section=timeline`次级管理，路径nodeId优先；R39保留作全库及旧链接入口，node/event/筛选/page/returnTo均不删除，菜单不再单列。理论编辑直接提供所选理论时间线入口。新书目保存统一`POST library/works/{work_id}/edits/`，Edition身份与edit_version必传，候选复核仍检查原IsCatalogEditor，knowledge/curation仍检查IsKnowledgeEditor，其余沿用CanEditMetadata；发布/撤回/人物合并权限不变。兼容分节接口未删除。最新18项真实隔离浏览器通过/退出0，覆盖理论次级/旧入口、准确对象和整页保存，报告`output/playwright/v306-workspace-verified-0032/results.xml`；不据此宣称表中全部43入口新组合验收完成。

- 已读仓库 AGENTS、应用开发规则、GPT_ARCHITECTURE_CONTEXT、INGESTION_AND_PUBLICATION、REDESIGN_BRIEF、CURRENT_STATE，以及 ARCHITECTURE/PROGRESS/ISSUES 的当前增量与相关历史边界。
- 首次审计枚举为 **43 个 page.tsx**。当时侧栏 **5 组、21 个链接、19 个不同 pathname**（历史，当前为六组）；`library?view=editions`、`library?view=quality`不是独立页面文件。
- 基线源码为本地 `aa97727`。43个入口均已完成源码映射；这只完成盘点，不是43个页面已完成3.0.6行为验收。后续实现与验收应在逐项交付表另记，不以历史测试数充抵。
- 本子任务只做源码审计和文档写入，没有启动业务服务、调用生产、运行浏览器操作或执行部署。A01—A32 在本文件中的编号仅为验收映射，不表示已执行。
- 使用 planning-with-files 的文件化记录原则，将发现写入本交付文档；根计划与 CURRENT_PROGRESS 由主任务统一更新，避免并行覆盖。

## 实施前源码差异（aa97727基线）

以下INV项目和协议描述保留首次盘点依据，不代表3.0.6当前源码仍存在同一问题。逐入口最终行为证据仍须核对；当前实现进展及未关闭门槛见V3.0.6_VERIFICATION。当前导航已改为六个任务分组，不再沿用下方基线中的五组/21链接统计。

| 编号 | 当前源码事实 | 3.0.6处理要求；不是已完成结论 |
| --- | --- | --- |
| INV-01 | `WorkflowQueueView` 只查有 Edition 的 UploadItem，先取30项，再分类并每类取12项。 | 全来源业务筛选、稳定分页与全量计数；保留未建 Work/Edition 的来源任务。 |
| INV-02 | `WorkLibraryListView` 后端每页40项；`WorkLibrary`读取 next/previous，却没有页码请求或翻页控件。 | 41项以上完整遍历、筛选/排序/返回恢复。 |
| INV-03 | `view=editions`仍渲染Work级表格；当前页只从主版本链接进入维护。 | Edition级列表和精确版本的文件操作。 |
| INV-04 | 主Studio未渲染已有PublicPageTree；专门对象上下文/诊断实现已经有该能力。 | 主入口接同一只读公开控制结果，不复制登记。 |
| INV-05 | review/publication单项重定向只保留item；candidates固定转knowledge；未透传其余query。 | 保留适用的对象、版本、筛选、返回参数，未知对象不能替换。 |
| INV-06 | AdminShell未登录的next只使用pathname，且侧栏固定写“API已连接”。 | 登录续回保留完整任务上下文；会话有效不冒充系统功能健康。 |
| INV-07 | SettingsAdmin无条件读取备份列表；备份属于Owner能力，普通Admin并不因此自动取得权限。 | 按接口能力分隔子模块，保留明确拒绝和恢复说明。 |
| INV-08 | 侧栏“备份与存储”指向`DistributionAdmin`，实际是Provider/用量管理；备份列表及创建在SettingsAdmin。Provider配置API允许Admin，但侧栏该链接要求Owner备份能力。 | 将真实备份与恢复依据放在正确作用范围，菜单和接口不互相冒充。 |
| INV-09 | 兼容Taxonomy页仍输出`/admin/theory-schools/new`及`/admin/theory-schools/{id}`，43个page清单不存在该路径；TheorySchool管理API仅List/Retrieve。 | 不删除历史来源；用有效LegacyKnowledgeMapping进入规范节点，无法映射时明确解释。 |

## 逐路由清单

每个入口由下表与其“能力协议”行共同定义，协议中的接口、服务、主要操作、公开影响、数据状态、处理状态及恢复要求逐项继承，避免相同组件写成相互漂移的不同能力。路径参数如`[workId]`来自实际page文件，`new`是动态编辑页允许的特殊值，不额外计文件。

角色：E=Editor；A=Administrator；O=System Owner。E/A/O均有后台访问与证据读取；R=读者和匿名均不应取得后台写或草稿预览。下表写的是**实际后端基线能力**，侧栏可见性单独列明，不把隐藏按钮当授权。所有条目共同映射A26、A31、A32；额外场景列仅列差异。

新归属：H1待办与上架、H2馆藏、H3知识与关联、H4公开展示、H5处理与服务、H6系统管理。六组导航已实现。下表最后列于2026-09-19按当前证据更新；已修改表示有代码和所列验证，不表示全部原始场景已关闭。其余基线列保留首次盘点以便追溯。

| ID | 真实路由 | 组件/角色/对象基线 | 能力协议 | 新归属 | 实施前兼容上下文及保留要求（历史） | 额外验收 | 3.0.6处理结果 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R01 | `/admin` | AdminDashboard；E/A/O；全库工作摘要 | Q | H1 | 链接当前item工作台、上传、发布、馆藏；统计按权限读取；摘要必须可进入同条件完整列表 | A01,A03,A05,A10,A12,A23 | 已修改；全来源待办、普通编辑权限与失败重试，W-live 20项组合 |
| R02 | `/admin/about` | AboutAdmin；E/A/O；AboutPageBlock | AB | H4 | 当前无对象query；保留既有路由及块key，不能以菜单未列为由删除 | A19,A20,A25 | 验证后保留；真实编辑字段及页面来源，M-live；公开控制见权威登记 |
| R03 | `/admin/analytics` | AdminAnalytics；A/O；30日用量/审计摘要 | AN | H6 | 当前固定days=30；无逐对象URL；需从指标追踪原事件且不公开私人明细 | A24,A28 | 验证后保留；真实统计分母及读取失败恢复，M-live；不扩充无依据指标 |
| R04 | `/admin/candidates` | redirect；目标E/A/O；旧候选入口 | KD | H3 | 当前固定到`/admin/knowledge`，不转交query；保留可识别对象/字段/来源/返回，专业诊断另在R32 | A15,A16,A19 | 已修改；保留对象与查询参数的兼容跳转，C-live/路由矩阵 |
| R05 | `/admin/cataloging/new` | ManualCatalogForm；E/A/O；新manual会话/Work/Edition | C,W | H1 | create request_key在失败重试时保留；成功转精确session#work；返回当前仅`/admin/library` | A05,A09,A13,A14 | 已修改；普通Editor手工建目、来源及返回，W-live与真实服务A05 |
| R06 | `/admin/cataloging/[sessionId]` | CatalogingWorkbench→WorkflowEditor；E/A/O；精确会话版本 | C,W | H1/H2 | session读取后使用work_id/edition_id；已结束会话仅通用馆藏返回；未建版本显示等待且可刷新 | A01–A09,A13–A18 | 已修改；手工来源、准确版本、保存保护，W-live与真实服务A05 |
| R07 | `/admin/disciplines` | DisciplinesAdmin；E/A/O；Discipline | D,K | H3 | 列表内选对象；当前无显式discipline query读取，须保持选择/返回，不默认另一学科 | A15,A16,A19,A20,A27 | 已修改；读取版本、冲突保护及真实字段，双账户7项/C-live |
| R08 | `/admin/distribution` | DistributionAdmin；A/O配置、O敏感操作；CloudProvider/CloudUsage | B | H6 | 当前侧栏因can_run_backup仅O可见，但API配置允许A；真实备份在R28；原路径保留 | A23,A26,A30 | 已修改；存储与备份导航权限分开，M-live；真实外部云存储待核实 |
| R09 | `/admin/intake/[itemId]` | IntakeWorkspace→WorkflowEditor；E/A/O；UploadItem及对应Edition | U,W | H1/H2 | 保留item、Edition、#file/#work/#bibliography/#publication与来源批次；未建立版本仍显示来源 | A01–A09,A13–A18 | 已修改；v306-services-context-2306真实上传/发布/公开读取通过；可选语义未配置不再阻止获准PDF，原失败保留 |
| R10 | `/admin/knowledge` | KnowledgeWorkspace；E/A/O；规范对象/编辑修订/候选/关系 | K | H3/H4 | object_type/object_id实际读取并写URL；q仅组件状态；找不到指定对象不替代；保留相关完整编辑/公开预览 | A15,A16,A19,A20,A23,A24 | 已修改；主入口编辑范围、草稿预览到真实公开读取，K-live 3项 |
| R11 | `/admin/library` | WorkLibrary；E/A/O；Work，拟补Edition | L | H2 | page目前只读q/view；editions/quality复用该页；保留document_type、page、ordering、return与准确edition | A01–A05,A08,A09,A11,A12 | 已修改；45条完整分页、版本选择、主版本重试与返回，C-live 8项 |
| R12 | `/admin/library/works/[workId]` | WorkflowEditor maintenance；E/A/O；Work+Edition | W | H2 | 当前仅edition query传入；保留work/edition及页内步骤、列表筛选返回；缺edition不得默选错版本 | A01–A09,A13–A18 | 已修改；v306-services-context-final-2314同版本补/换PDF、新Range及原件/Page/私人外键保护通过；无需查找上传ID |
| R13 | `/admin/media` | MediaLibrary；E/A/O；MediaAsset/Rendition与选用对象 | M | H2/H4 | edition+slot(cover/recommendation)、scholar、object_type+object_id实际读取；保留来源编辑页和当前版本返回 | A07,A14,A25,A28 | 已修改；引用、选图、草稿隔离与明确发布，W-live 20项组合 |
| R14 | `/admin/people` | PersonResolutionWorkspace；E/A/O查找；O预览/合并/记录/回滚；Person | P | H3 | source/target/record实际读取，personPage写URL；保留来源书目/职责与合并后受影响Edition入口 | A13,A14,A21,A22,A28 | 已修改；作品职责预览、Owner合并、重试/回滚/冲突，W-live人物用例 |
| R15 | `/admin/preview/knowledge/[objectType]/[objectId]` | AdminKnowledgePagePreview；E/A/O证据；精确知识对象 | PK | H4/对象内 | page/module实际读取；当前返回通用Studio；必须返回同一object/page/module，拒绝无效对象，noindex | A19,A20,A25,A27,A32 | 已修改；准确对象、预览权限和返回，K-live及双账户7项 |
| R16 | `/admin/preview/works/[editionId]` | AdminWorkPagePreview→WorkDetailView；E/A/O证据；Edition草稿 | PW | H1/H2对象内 | edition来自路径，return_url由API生成；保留当前版与调用页上下文，noindex；绝不替代公开URL | A01–A09,A27,A32 | 已修改；准确Edition与嵌套返回、私人操作禁用，C-live及API权限测试 |
| R17 | `/admin/processing` | ProcessingCenter/FunctionalHealthPanel；A/O导航；部分GET E/A/O；任务/服务 | H | H5 | type/status/review_status/surface实际保留；item可回工作台，asset-only当前仅文本；补精确Edition/object链接 | A10,A16–A18,A23,A24 | 已修改；当前对象处理影响与真实任务读取，M-live；OCR/模型实跑待核实 |
| R18 | `/admin/publication` | PublicationDesk或?item重定向；E/A/O；上传发布准备 | Q,U | H1 | ?item转精确intake#publication，其余query丢失；普通页当前只取上传首批100；需全来源及跨页 | A01–A05,A10,A12,A17,A18 | 已修改；全来源全量计数与服务端分页，C-live/跨页发布台行为 |
| R19 | `/admin/publication/[itemId]` | redirect；目标E/A/O；兼容上传发布 | W | H1 | item encode后到intake#publication，现不转交其他query；保留edition/session/filter/return | A01,A03,A06,A12 | 已修改；v306-services-context-2306成功上传对象307保留Edition、Session、筛选及嵌套返回，已修Vinext query探测适配 |
| R20 | `/admin/query-lexicon` | QueryLexiconWorkspace；A/O只读，O写；词典状态/词条 | X | H5专业 | 当前q/status仅组件状态；保留词条/源对象/失败样例到知识或馆藏的回链，不替代编目 | A15,A18,A23,A24 | 验证后保留；Owner边界与真实词典版本读取，M-live及真实Redis恢复4项 |
| R21 | `/admin/reading-paths` | ReadingPathWorkbench；E/A/O；ReadingPath/stages/items | RP,K,M | H4 | ?path精确读取，路径首载保护已有；保留stage/item身份、来源work和当前编辑返回 | A07,A14,A19,A20,A25,A27 | 已修改；显式保存发布、真实图片刷新与规则说明，W-live/M-live |
| R22 | `/admin/recommendations` | RecommendationsAdmin；E/A/O；placement policy与选择结果 | REC | H4 | placement为选中策略，当前无URL恢复；保留真实candidate类型/选择/排序；旧TheorySchool候选兼容待验证 | A19,A20,A25 | 已修改；类别切换保护、显式发布确认/防重、真实规则，119项组合中的推荐行为 |
| R23 | `/admin/review` | ReviewQueue；E/A/O；上传复核队列 | Q,U | H1 | 当前search/status组件状态，API scope=review；保留批次/来源/过滤/返回并纳入非上传事项 | A04–A06,A10,A12,A15,A16 | 已修改；旧异常与未绑定上传不漏、真实筛选计数，C-live 8项 |
| R24 | `/admin/review/[itemId]` | redirect；目标E/A/O；兼容上传复核 | W | H1 | item encode后到intake#bibliography，其他query未转交；保留当前版/会话/返回 | A04–A06,A13 | 已修改；v306-services-context-2306成功对象复核重定向与完整query通过，与R19共用适配 |
| R25 | `/admin/scholars` | ScholarsAdmin；E/A/O；ScholarProfile+Person | S,K,M | H3 | q/create实际读取；/scholars/new由动态页处理；选择后到精确scholar；当前返回会丢列表q | A07,A14–A16,A19–A22,A25,A27 | 已修改；人物/学者来源与当前对象，M-live/肖像和人物行为 |
| R26 | `/admin/scholars/[scholarId]` | ScholarsAdmin指定编辑；E/A/O；精确学者或new | S,K,M | H3 | scholarId，不把Person ID当Profile ID；人物查重source使用person_id；保留输入/失败/肖像目标 | A07,A14–A16,A19–A22,A25,A27 | 已修改；延迟加载、双人冲突、草稿隔离与肖像发布，双账户7项/W-live |
| R27 | `/admin/semantic-index` | SemanticIndexAdmin；A/O查看/局部重试，O全局切换；索引/评估 | SI | H5专业 | 现未读取URL对象参数；保留asset/version/evaluation set/run，失败样例需回当前馆藏 | A17,A18,A23,A24,A28,A30 | 验证后保留；真实资格数量与Owner索引切换边界，M-live；真实模型未配置 |
| R28 | `/admin/settings` | SettingsAdmin/PromptRegistryAdmin；A/O普通设置，O备份/Prompt；SiteSetting/runtime | SET,B | H6；公开字段H4 | 当前无对象query；保留旧设置路由和公开品牌字段，按能力隔离备份/Prompt，不回显密钥 | A23,A24,A25,A26,A30 | 已修改；设置分栏/输入保护与Owner备份实页通过；合成空PG恢复11类摘要一致，非生产/全NAS恢复，生产备份门槛仍在 |
| R29 | `/admin/status` | SystemStatusCenter；A/O；只读系统/发布任务状态 | ST | H5专业 | 现独立诊断入口无对象query；保留到处理中心/知识任务/词典链接，不能冒充书目操作完成 | A17,A18,A23 | 验证后保留；当前服务字段和明确刷新，M-live |
| R30 | `/admin/subdisciplines` | SubdisciplinesAdmin；E/A/O；Subdiscipline/parent/Discipline | D,K | H3 | ?subdiscipline实际精确读取；保留父层级和返回，不能改成另一个对象 | A14–A16,A19,A20,A27 | 已修改；对象版本、保存冲突与公开隔离，双账户7项/M-live |
| R31 | `/admin/system-health` | AdminSystemHealth；A/O；旧端到端自检结果 | SH | H5专业 | 原GET/POST诊断保留；执行自检是明确按钮操作，不把页面读取变成全量/外部调用 | A17,A23,A30 | 已修改；过期/未知与实际检测时间，M-live；打开页面不触发收费检测 |
| R32 | `/admin/system-health/candidates` | CandidateReview；E/A/O证据/候选决定；全类候选诊断 | KD | H5专业 | 当前status/kind/query是组件状态；对象动作由后端descriptor提供；保留源工作台/候选位置 | A13–A16,A20,A24 | 已修改；候选状态与显式决定、对象回链，M-live；使用统计按既有事件 |
| R33 | `/admin/system-health/knowledge` | KnowledgeWorkspaceDiagnostics；E/A/O后台/证据；对象、投影和公共树 | K,KD | H5专业 | object_type/object_id需保持与主Studio一致；保留专业细节，不把此页作为主入口验收替代 | A15–A20,A23,A24 | 已修改；只读诊断及返回同一Studio对象，K-live/M-live |
| R34 | `/admin/taxonomy` | TaxonomyAdmin combined；E/A/O；旧TheorySchool+Topic | T,K | H3兼容 | 主题链接有效；theory-schools页面路径缺失，旧API只读；保留legacy_id安全映射及原来源说明 | A19,A20,A22,A27 | 已修改；兼容来源保留，死链改为真实规范编辑链接，M-live |
| R35 | `/admin/theories` | TheoryNodesAdmin；E/A/O编辑，O合并；KnowledgeNode | N,K,M | H3 | node/node_type/legacy_id实际读取；完整编辑与公共理论页保留规范身份 | A07,A14–A16,A19,A20,A22,A25,A27 | 已修改；规范理论列表、状态与编辑版本，M-live/双账户7项 |
| R36 | `/admin/theories/[nodeId]` | 同TheoryNodesAdmin指定对象；E/A/O，O合并；KnowledgeNode | N,K,M | H3 | 路径nodeId可被?node覆盖，需明确冲突处理；保留legacy_id、node_type与来源返回 | A03,A14,A19,A20,A22,A27 | 已修改；精确对象、首载输入保护、冲突与公开隔离，M-live/W-live/双账户7项 |
| R37 | `/admin/theory-nodes` | 同TheoryNodesAdmin兼容页；E/A/O，O合并；规范节点 | N,K,M | H3兼容 | 保留node/node_type/legacy_id；这是同组件不是独立新实体系统，退役需真实跳转证据 | A14,A19,A20,A22,A27 | 已修改；兼容理论入口进入同一规范编辑器，M-live/路由矩阵 |
| R38 | `/admin/theory-relations` | TheoryRelationsAdmin；E/A/O；KnowledgeRelation/ReviewTask | NR,KD | H3 | relation及列表筛选/页码/return_to由URL保留；不默认另一理论；保留Evidence和职责 | A13–A16,A19,A20,A22,A27 | 已修改；v306-dedicated-2150双账户保存草稿/409/明确发布、关系及建议各35条分页与五宽度通过；pg-dedicated-0919含真实行锁与迁移兼容 |
| R39 | `/admin/theory-timeline` | NormalizedTimelineAdmin；E/A/O；TheoryTimelineEvent | TL | H3 | 实际仍用admin/theory-timeline；event、筛选/页码/返回由URL保留，保留已有规范关联ID及来源 | A14–A16,A19,A20,A27 | 已修改；同组实际事件保存/发布、35条完整分页、360/390长词红测修复及五宽度整表单通过；相同关联来源与ID保留 |
| R40 | `/admin/topics` | TaxonomyAdmin topic；E/A/O编辑，O合并；Topic | T,K | H3 | create实际读取；/topics/new由动态entityId处理；保留主题详情及列表返回 | A14–A16,A19,A20,A22,A27 | 已修改；主题来源、真实列表与公开字段，M-live/K-live |
| R41 | `/admin/topics/[entityId]` | TaxonomyAdmin指定topic；E/A/O，O合并；Topic或new | T,K | H3 | entityId/new明确区分；保留合并目标/公开预览/缺失原因，禁止切成其他主题 | A14–A16,A19,A20,A22,A27 | 已修改；准确对象、延迟加载、双人冲突与发布，K-live/双账户7项 |
| R42 | `/admin/uploads` | AdminUpload；E/A/O；UploadBatch/UploadItem/R2Staging | U | H1 | 保留批次、client_token与恢复上传；当前结果回intake(item)；来源批次不等于Work身份 | A04,A06,A08–A10,A13,A17,A18 | 已修改；v306-services-context-2306完整上传到公开Reader通过，真实Worker和S3协议替身；外部Cloudflare R2仍待核实 |
| R43 | `/admin/users` | UsersAdmin；A/O；User/role/password | US | H6 | 当前对象选择为组件状态；用户ID/返回需保留，Owner身份保护不能由UI重设计弱化 | A26,A28,A32 | 验证后保留；用户详情及Owner保护，M-live/账户API；无额外扩权 |

## 能力协议：真实API、服务、状态、影响和恢复

以下API均相对于浏览器同源`/api`；GET为只读。`{id}`等是实际对象ID占位，不是可由前端随意替换的权限凭据。动态`publish_url`、`decision_url`采用后端返回的已有动作合同。接口存在和源码调用可证明连线，不能证明某次操作成功。

| 协议 | 已核对的实际调用接口与主要操作 | 后端视图/关联服务及持久对象 | 数据状态、处理状态与公开影响 | 现有恢复方式与本轮必要补项 |
| --- | --- | --- | --- | --- |
| Q | GET `ingestion/dashboard/`、`catalog/admin/workflows/queue/`；ReviewQueue GET `ingestion/items/?scope=review&ordering=-updated_at`；PublicationDesk GET `ingestion/items/?scope=publication&ordering=-updated_at&page_size=100`；PDF预览GET item/preview；首页有授权统计和hot-searches | `workflow_views.WorkflowQueueView`、`ingestion.views.UploadItemListView/DashboardView`；`admin_workspace`、`admin_workflow` | 当前queue只查上传，且先截断；publication/review首批与全量计数需统一。Work/Edition/Session与上传源不能合并；书目公开与派生完成独立 | 页面已有刷新/读取错误。补category/source/q全量筛选、稳定分页、真实counts、当前对象准确workbench_url；不丢无Edition上传 |
| U | POST `ingestion/batches/create/`、batch/items、batch/chunks、batch/failures；GET batch/chunks(client_token)、items/{id}；POST item/retry、metadata-import；R2 init/detail/parts/sign/parts/confirm/parts/failure/complete/abort/retry-import由既有上传客户端调用 | `ingestion.views`、`services.r2_staging/files/dispatch/pipeline/metadata_import/processing`；UploadBatch/Item/Attempt/Asset/ProcessingJob | 传输、暂存导入、文件校验、编目、正式公开各自有状态；上传成功不等于已公开；原件和页身份保留 | 分片恢复、同token恢复、原任务重试和失败详情。R2到NAS与直接上传分别验证；失败次数有限；补当前Edition补/换文件而非找上传编号 |
| C | POST `catalog/admin/cataloging-sessions/`(source_type=manual、request_key)；GET `cataloging-sessions/{session}/?workspace=0`；会话API另有abandon、候选decision/revert和metadata-import | `cataloging_views`、`services.cataloging_sessions`、`field_decisions`、原candidate决定服务；CatalogingSession/Work/Edition | 新manual建真实纯书目；不造UploadItem；会话published/abandoned不等于所有能力就绪；已结束会话保留历史 | 创建页面复用request_key、错误保留输入；会话读取有重试/未建版本提示。补结束会话到准确Edition、新编辑与来源追踪 |
| W | GET `catalog/admin/intake/{item}/`或`library/works/{work}/?edition={edition}`；PATCH对应`sections/{step}/`；GET `editions/{edition}/publication/prepare/`、history；POST rollback；POST item/publish、item/withdraw或work/publication；候选采用/校验用响应descriptor；封面candidate GET/POST/select；fileAction现调用item/{retry,resume,replace} | `workflow_views`、`backoffice_views`、`publication_command_views`、`admin_workspace/admin_workflow/work_editor/cataloging_sessions/field_decisions/publication_commands/publication_eligibility/knowledge_publication` | 当前context、health、权限和revision是事实来源；已发布维护写EditorialRevision，公开读取CatalogPublicationRevision；PDF pending/valid/invalid不得转成布尔。现旧撤回Admin、维护Editor可撤回的分歧单列 | 显式保存、未保存保护、预检指纹、409并发、历史公开恢复、PublicationRetryControl复用原event；补Edition级文件接口、真实差异/生效位置/多版本选择。新政策见权限表，不从UI推断 |
| L | GET `catalog/admin/library/works/?q=&view=&document_type=&page=`；当前API每页40，响应count/next/previous/results；页面只发送q/view | `workflow_views.WorkLibraryListView`、`admin_workspace.work_library_queryset/serialize_work_library_row` | 基线只有Work行；主版本/文件/知识/策展多个状态，不应靠Edition.state判断公开；editions视图需真实Edition/assets | 当前只有搜索/刷新；补page/ordering/URL返回和Edition行。41+数据遍历后才能关闭F04/F05 |
| M | GET/POST `catalog/admin/media/`；PATCH media/{id}；POST media/{id}/renditions；GET受保护rendition文件；POST `editions/{edition}/media/{cover,recommendation}/`；肖像/知识image选择走`scholar-media`/`knowledge-media`客户端 | `media_views/scholar_media_views/knowledge_media_views`、`services.media/scholar_media/knowledge_media/editorial_drafts`；MediaAsset/Rendition、EditorialRevisionMedia、CatalogPublicationMedia | 媒体原件、衍生图、草稿引用与活动快照分别保留；上传本身不公开；修改已公开选用通过原编辑修订 | 有编辑版本校验/消息和列表retry；补引用对象、草稿/当前/历史使用位置、返回同版与回退结果，不删除历史原图 |
| K | GET `catalog/admin/knowledge-workspace/?object_type=&q=&selected_type=&selected_id=`；POST revision.publish_url；KnowledgeObjectContextPanel同端点；字段助手调用既有field-assistant接口；GET `knowledge-publications/status/`或edition/knowledge-status，POST event/retry | `backoffice_views`、`knowledge_studio/backoffice/public_knowledge_control/editorial_revision/field_assistant/knowledge_publication` | canonical值、当前草稿、冲突、候选、关联、公共位置与projection分别呈现；公共模块来源从原登记读取，不再抄表 | 读取retry、发布失败提示、原event安全重试。主Studio接PublicPageTree或同数据等价表现；明确手工/关系/推荐/统计生成，不提供无效文本框 |
| P | GET `catalog/admin/people/?search=&limit=20`、person/duplicates；O GET person/merge-preview(target_person)、merge-records及详情；POST person/merge、record/rollback | `person_resolution_views`、`person_resolution/person_merges`；Person/ScholarProfile/PersonMergeRecord、原关系和公开事件 | 先核对身份与完整引用；仅非冲突且至多一学者档案。merge saved与公开更新分开；原贡献职责/署名不可改写 | 指纹、request_key和条件回滚；超时复用请求并重开记录。补按作品/职责/公开位置摘要，技术字段折叠，双档案/后续冲突明确人工路径 |
| PW | GET `catalog/admin/page-preview/editions/{edition}/`，再用受控PDF预览资源；复用WorkDetailView | `AdminWorkPagePreviewView`、`page_preview`/编辑修订物化、公开组件 | 权限保护的草稿/已保存值，不是公开API；具体Edition，不泄漏diagnostic/private字段 | 读取失败有重试；return_url是当前实际维护上下文。补保存前后差异、错误/无文件/多版本和预览角色拒绝 |
| PK | GET `catalog/admin/knowledge-preview/{type}/{id}/`；page/module选择实际组件 | `AdminKnowledgeObjectPreviewView`、`knowledge_studio.knowledge_object_preview_payload`；公开学者/主题/理论/路径等组件 | 公开样式的管理员草稿，只读当前对象；page/module内容来自登记与真实serializer，不靠静态覆盖率 | 读取错误/重试及明确不可预览模块；补精确返回和实际组件使用断言 |
| D | GET/POST `catalog/admin/disciplines/`、`subdisciplines/`；GET/PATCH对应{id}，含JSON/旧multipart图片；关联Picker使用同API；K提供上下文和发布 | `knowledge_views`、`canonical_mutations/editorial_revision`；Discipline/Subdiscipline、原层级关系 | 规范对象/人工编辑、草稿修订及公开资格；图片旧二进制兼容不可绕过已发布保护；结构/来源字段影响分类页面 | 保存错误保留输入、对象Context和发布修订；补无选择/错误ID、公开关系生成范围、层级并发与URL恢复 |
| AB | GET `catalog/admin/about-blocks/`；PATCH block/{id} | `knowledge_views.AdminAboutPageBlock*`；AboutPageBlock | About块是直接持久内容；非书目发布流程。不得把独立块保存假称CatalogueRevision发布 | 保存/重新读列表；需核对真实/about消费者、显示开关和更新时点；没有单独草稿不得虚构 |
| AN | GET `catalog/admin/usage-analytics/?days=30`；只读摘要 | `views.AdminUsageAnalyticsView`、既有reading/usage记录 | 搜索/阅读统计不等于智能建议质量、采纳准确率或节时；私人正文不可成为公开管理字段 | 读取失败可重试；补统计口径/分子分母/时间范围/原事件回查，缺数据显示无数据 |
| T | GET旧`catalog/admin/theory-schools/`及{id}只读；Topic GET/POST/PATCH/DELETE `admin/topics/`及{id}；TopicMergePanel GET merge-preview、POST merge；lifecycle/relations按对象动作 | `views.AdminTheorySchool*/AdminTopic*`、`topic_merge_views`、`canonical_mutations/editorial_revision/topics`、LegacyKnowledgeMapping | TheorySchool不是新规范写对象；Topic独立身份/草稿/关系；删除、撤回、合并语义不能混淆 | 旧理论写应引导规范node映射；主题合并保留冲突校验/Owner。无映射明确不可自动迁移，不偷换身份 |
| S | GET/POST/PATCH `catalog/admin/scholars/`及{id}；ScholarPortraitPanel；EntityRelationsAdmin；K上下文/EditorialRevision发布 | `views.AdminScholar*`、`scholar_media_views`、`editorial_drafts/editorial_revision/canonical_mutations` | Person身份与ScholarProfile展示独立；肖像精确引用、未发布草稿不公开；关系保留角色 | 首次载入保护/显式保存、冲突错误、草稿预览；Person查重source精确，不能把姓名相似作为自动合并 |
| N | GET/POST/PATCH `catalog/admin/theory-system/nodes/`及{id}；GET versions、merge-preview；POST merge；EditorialRevision列表及publish_url；K及媒体panel | `theory_system_views`、`knowledge_nodes/editorial_drafts/editorial_revision/knowledge_media` | KnowledgeNode类型/状态/版本/关系；公开传统、概念等按真实node_type；merge有Owner边界，旧映射受核验 | 保存错误/冲突、版本历史和Owner受限恢复；指定node/legacy冲突必须明确，不能猜别的节点 |
| NR | GET/POST/PATCH/DELETE `catalog/admin/theory-system/relations/`及{id}；GET review-tasks、POST task/action、DELETE task；实体与EvidencePicker | `theory_system_views`、`knowledge_nodes`、候选审核/编辑修订服务；KnowledgeRelation/TheoryReviewTask/Evidence | 人工规范关系和候选待审分开；删除候选不等于移除公开关系；实际端点、方向和依据决定公开图 | 异常消息、明确确认与任务刷新；补冲突/拒绝原因/恢复与对象回链，不为通过关闭唯一约束 |
| TL | GET/POST/PATCH/DELETE `catalog/admin/theory-timeline/`及{id}；GET规范nodes/disciplines/works/scholars | `knowledge_views.AdminTheoryTimeline*`；TheoryTimelineEvent和规范节点关联 | 年份区间、事实/解释、引用来源与显示条件；NormalizedTimelineAdmin名称不代表另一套时间线API | 显式保存/删除确认及错误；补实际公开子页列分配、长来源、ID与证据保留 |
| RP | GET/POST/PATCH/DELETE `catalog/admin/theory-system/reading-paths/`及{id}；关联Work/Node/Scholar；K和knowledge-image面板 | `theory_system_views`、`reading_paths/editorial_drafts/editorial_revision/knowledge_media`；ReadingPath/Stage/Item | 人工阶段/排序与公开选择条件分开；已公开修改进草稿；原stage/item身份不得重建以掩盖兼容 | 已有指定path首载锁、显式保存/发布、刷新/删除确认；补真实公开规则和遗漏/撤回项原因 |
| REC | GET `catalog/admin/recommendations/`、公开works及旧theory/topic/scholar候选；POST `recommendations/{placement}/refresh/`，人工items或空body自动更新 | `knowledge_views.AdminRecommendation*`、`services.recommendations`；RecommendationPolicy/Recommendation/Override | 策略、人工选择和自动计算范围独立；不是给所有推荐内容写一个普通文本框；只应公开有资格对象 | 手工/自动刷新反馈和重读；需真实预览使用同选择规则，说明旧理论候选兼容与历史影响 |
| H | GET `ingestion/items/?scope=processing&page_size=100`、queue-health、processing-center、review-tasks；GET semantic-index、`catalog/admin/functional-health/`；POST processing-center任务pause/resume/retry/cancel、task/action；POST functional-health(run_probe/recover)；ResearchSourceRegistry用research-sources/provider-secrets | `ingestion.views`、`services.processing/health/system_health/review_tasks`；`catalog.services.system_health/research_sources/provider_secrets`；ProcessingJob/CapabilityDemand/HealthCheckRun/Incident/RecoveryAction | 配置、连接、功能、产出和检查时间独立；running不等于ready，productive不等于研究正确；真实任务/来源修订需回当前对象 | 明确按钮执行检测/恢复；Owner限制全局词典/索引恢复和密钥；任务202只是接受，轮询真实终态；补对象影响、过期与可追溯使用记录 |
| X | GET `catalog/admin/query-lexicon/?q=&status=`；POST同端点执行受限操作，term inspector另由后端提供 | `backoffice_views`、`query_lexicon.operations`；QueryLexiconState/词条/Outbox | 权威来源与派生generation、pending/failed状态；候选不直接写正式词典；列表/源对象绑定 | A/O只读，O reconciliation；错误可刷新，记录幂等/影响，禁止直接重建活动索引制造正常 |
| SI | GET/POST `catalog/admin/semantic-index/`；POST test-query；GET/POST search-evaluations/sets和runs，query导入/归档 | `views.SemanticIndex*`、`search_evaluation_views`、`semantic_indexing/semantic_search/search_evaluation` | active/building/retired索引与具体Asset job分开；评估计划、执行、结果非同状态；旧任务不能覆盖新修订 | A可局部resume/retry_failed/rebuild_asset/rechunk/reembed；O全局切换/重建；失败保留稳定旧index，评估范围/成本须真实 |
| ST | GET `catalog/admin/system-status/` | `backoffice_views.AdminSystemStatusView`、`services.backoffice.system_status_snapshot` | 系统快照与开放任务/权限/版本概览，只读，不证明某Edition公开 | 重试读取、回原处理页；补对象级追踪，不能将系统总绿替代上架验收 |
| SH | GET/POST `ingestion/system-health/`，POST明确端到端自检 | `ingestion.services.system_health.system_health_snapshot/run_end_to_end_self_test` | 旧诊断快照/执行结果，不是主工作流事实；需显式范围/检查时间/真实环境 | UI有自检操作与错误；需限制并解释真实调用和有副作用检查，未配置/失败/未执行分开 |
| KD | GET `catalog/admin/candidate-review/?status=&kind=&q=`；POST candidate descriptor/`candidate-review/{kind}/{id}/decision/`；诊断知识页POST publish_url/projection动作 | `enrichment_views`、`field_enrichment/candidate_decision_protocol/knowledge_growth`；既有Metadata/EntityResolution/Enrichment/Theory候选 | 候选来源、证据、人工决定、锁和公开修订独立；无结果/未执行/不适用/失败不可混写 | 决定descriptor与受限create_draft、重试/来源工作台；拒绝理由、撤销与人工锁保护需行为验收，不以静态按钮断言替代 |
| B | GET/POST/PATCH `distribution/providers/`及{id}、POST provider/usage；DELETE仅O；GET/POST `distribution/backups/`在Settings页仅O | `distribution.views`、`database_backup`、原CloudProvider/CloudUsage/BackupJob | Provider就绪/配额与备份queued/running/completed/failed分开；备份文件存在不等于可恢复；对象存储不取代NAS馆藏 | 凭据alias变更/删除O；普通配置A/O。补最近验证/隔离恢复演练依据与精确恢复步骤，不从页面直接恢复生产 |
| SET | GET/PUT `catalog/site-config/`；GET/PUT或显式test `admin/reader-submission/ocr-runtime/semantic-runtime/`；GET/PUT `reading/admin/ai-runtime-profiles/`、POST test；O `catalog/admin/prompt-registry/`创建/激活 | `views.SiteConfigView/*RuntimeSettingsView`、reading AI runtime、`backoffice_views.AdminPromptRegistryView`；SiteSetting/Runtime/Prompt版本 | site-config公开字段直接生效；服务参数按下一任务/请求读取；测试成功仅本次功能范围。Prompt注册/激活与Provider健康独立 | 保存错误保留输入，显式检测与反馈；补角色分隔、重载/影响说明和来源时间，不显示密钥，也不把普通打开变付费探测 |
| US | GET `auth/users/`；PATCH user/{id}；POST user/{id}/set-password | `accounts.views/serializers/ownership`；User及审计 | 用户激活/角色/密码影响权限，不属于书目发布；读者私人记录不合并、不对外发送 | A/O管理，Owner身份/角色约束由后端执行；拒绝说明不泄漏凭据，保留确认和审计，不提升Editor权限 |

## 权限差异基线与3.0.6明确政策

权限证据为`api/common/capabilities.py`、`permissions.py`与各视图最终检查。Owner由`accounts/ownership.py`认定，不是任意is_superuser。下表“目标”是本轮实施选择，必须有A26接口行为结果后才可标完成。

| 操作 | 当前后端基线 | 3.0.6目标/不可扩大项 | 本轮证据状态 |
| --- | --- | --- | --- |
| 上传、保存编目、采用候选 | E/A/O的各对应capability | 保留；单人可完成书目 | 源码已核对；行为待核实 |
| 正式发布Work/Authority | E/A/O，对象发布能力最终校验 | 保留Editor发布，不新增双人审批 | 源码已核对；行为待核实 |
| 撤回已发布Work | 历史：ingestion要求A/O；maintenance曾可E/A/O | 已统一CanWithdrawWork为A/O；按钮和两个接口一致，Editor发布保留 | 当前源码已再次核对；实现期权限测试见验证账本，不称本次重跑 |
| 查询人物和重复提示 | E/A/O | 保留，不能要求必须有ScholarProfile | 源码已核对；行为待核实 |
| 人物合并预览/执行/历史/回滚 | O | 保留指纹、完整引用、幂等、冲突及条件回滚 | 源码已核对；行为待核实 |
| 理论/主题合并及敏感恢复 | O，具体恢复额外capability | 不因统一UI扩大 | 源码已核对；行为待核实 |
| 单项任务恢复 | 部分上传retry/resume E/A/O；ProcessingCenter POST A/O | 按安全操作现有范围，统一解释，不一概扩大E | 源码已核对；行为待核实 |
| 服务健康读取/明确检测 | A/O；全局词典/索引恢复另O | 保留；健康普通打开只读旧结果 | 源码已核对；行为待核实 |
| Provider普通参数 | A/O；删除/敏感凭据alias O | 与菜单匹配但不开放敏感操作 | 源码已核对；行为待核实 |
| 备份创建和列表、全局索引切换、Prompt敏感管理 | O | 保留；恢复必须按部署约束与演练依据 | 源码已核对；行为待核实 |
| 用户管理 | A/O；Owner身份/角色保护 | 保留后端拒绝，私人数据权限不变 | 源码已核对；行为待核实 |
| 草稿预览 | 后台证据能力E/A/O；R拒绝 | 复用公开组件但不能用公开API暴露草稿 | 源码已核对；行为待核实 |

## 六类入口与旧实现退役条件

- H1整合R01/R05/R06/R09/R18/R19/R23/R24/R42的共同任务来源与状态，旧URL可以保留，但须证明对象、Edition、session、filter、return保留及错误恢复。不能只把R23菜单改名。
- H2保留R11/R12版本级维护与R13全库媒体。Work归组与Edition文件上下文分开；维护任务不能要求查UploadItem ID。
- H3将R10及全部专门知识编辑/关系/人物入口保留。R34/R37是兼容来源，先修有效映射和死链，再按运行观察与迁移条件退役；不删除旧表/历史关系。
- H4提供公开页面/模块管理入口，但仍读取既有public_knowledge_control登记。R02/R21/R22和Settings公开配置字段具有不同写入/公开机制，应按来源说明，不强行全部套书目发布。
- H5让处理/健康/来源/模型可从当前对象打开。R20/R27/R29/R31/R32/R33保留专业诊断，不成为普通编辑完成任务的必经页。
- H6保留用户、敏感运行配置、备份与恢复证据。将“存储Provider”和“备份可恢复”明确分开，不使用菜单标签替代恢复演练。

旧组件只有在新入口承接全部真实调用、权限、状态、失败恢复和上述兼容A31行为验证后才能删除。当前清单没有授权删除旧实现、迁移用户数据或直接修改生产数据库。

## 验收与更新规则

1. 每个R行必须最终填写“已修改”“验证后保留”“不适用且说明原因”或“未完成”。当前所有R行均未完成3.0.6验收；“已源码盘点”不是通过。
2. A31逐路由检查不止HTTP200：以真实对象打开、选择/修改或只读诊断、失败/恢复、角色拒绝、返回与分页上下文验证。专业诊断的测试不能替代Studio主入口A19/A20。
3. A01–A04验证同一Edition在首页/待办/馆藏/工作台/发布/公开核验中同义，含无有效修订、错误归属、非主版本和PDF三态。
4. A05–A18对手工、上传PDF、旧PDF复核、维护修订和替换分别记录实际操作与接口/页面结果；使用隔离数据库，模拟Worker/Provider明确标注范围。
5. A19–A25覆盖真实公开模块读写、人物影响、过期健康与可追溯指标；读取registry或静态字段存在不能算实际公共组件消费成功。
6. A26/A32接口强制权限、草稿隔离；A27五视口真实路由回归不得删内容/隐藏溢出；A28/A29公共Reader和私人记录回归；A30独立恢复、旧稳定版与失败恢复。
7. 每条证据记录数据条件、角色、步骤、接口结果、页面结果、环境、命令/退出码及截图/日志位置。构建通过与上线readiness不能代替任务验收；生产变更由主任务在备份/回退条件满足后统一执行。

### 本文件生成核验

已执行只读命令`rg --files web/app/admin -g page.tsx`并逐个读取page文件，枚举43；读取admin-shell导航及权限分支，21个菜单链接/19个不同pathname。PowerShell解析文档R行并以`Compare-Object`核对实际路由集合：源码43、文档43、文档唯一43、缺失/多余0，命令退出0。`git diff --check -- docs/V3.0.6_ADMIN_CAPABILITY_INVENTORY.md`退出0。当前未运行任何A01—A32业务行为场景，不引用3.0.5旧测试结果作为3.0.6通过。

## 实施增量与尚未闭合的验收（不改写基线事实）

本节为清单完成后获得实施授权的增量，基线表保留便于对照。应用版本和部署由主任务统一完成；本子任务没有连接生产。所有“已修改”只说明代码变化，是否版本验收通过仍取决于真实场景证据。

| 入口/范围 | 处理结果 | 已实现行为 | 当前验证与剩余项 |
| --- | --- | --- | --- |
| R01 | 已修改 | Dashboard读取全来源队列的完整counts；摘要可到同条件完整列表；来源与当前工作页来自后端；上传阶段比例明确分母和失败范围，不伪称OCR/公开就绪 | 29项定向单测通过；65项队列/45项真实API浏览器测试已写，待统一运行 |
| R04/R19/R24，R18的旧?item入口 | 已修改 | 同一query保留助手完成重定向；保留Edition/session/来源/筛选/return_to；不新增平行旧页面 | 保留多值参数和嵌套返回地址的行为单测通过；实际页面重定向待浏览器 |
| R11，含editions/quality视图 | 已修改 | 服务端page/view/q/document_type/ordering/work_id；完整分页；Work与Edition行区分；文件原件/历史/PDF三态和当前阅读选择；准确Edition操作/返回 | 11项新纯函数测试与29项定向回归通过；41fixture及45真实DB遍历、5视口文件测试已写待运行 |
| R18/R23 | 已修改 | 共同队列API按source/category/publication筛选再分页；完整count；手工/维护不依赖UploadItem；发布台选中对象才读取精确工作区、真实差异、PDF和关系影响，不默认另一本 | 选择缺失不替代、错误Edition、无UploadItem请求上下文单测通过；跨API页及真实预览待浏览器 |
| R42的item兼容上下文 | 已修改 | 新UploadSourceContext精确GET原UploadItem并核验ID，显示错误/派发/尝试/来源、原任务安全retry及接受与完成分层反馈；原AdminUpload批量功能未删 | 源码与TypeScript检查；真实90天未绑定失败来源及retry202场景已写待执行 |
| R08/R28/R34 | 已修改 | 侧栏存储配置按can_configure_providers；备份/Prompt按Owner capability才读与显示操作；未取得备份记录不冒充空库；旧TheorySchool链接转带legacy_id的规范理论页 | 普通Admin真实API拒绝与无多余请求场景已写待执行；不因此宣称备份恢复或legacy映射已完整验收 |
| AdminShell及全部非focus后台入口 | 已修改 | 六个任务分组；按权限保留既有和专业入口；会话只称身份已验证；显示当前管理范围；登录next保留完整query/hash | 上下文安全与范围分流单测通过；角色/六组菜单浏览器场景已写待执行。共享说明不替代各模块操作验收 |
| R05/R06/R09/R10/R12/R13/R14/R15/R16 | 未完成本子任务验收 | 主任务或其他并行任务负责共同工作台、公共页面登记、对象媒体/人物影响；本子任务未擅自覆盖其文件 | 须由相应实现记录和真实操作补证，不自动标为本子任务已完成 |
| R02/R03/R07/R17/R20/R21/R22/R25/R26/R27/R29/R30/R31/R32/R33/R35/R36/R37/R38/R39/R40/R41/R43 | 未完成逐模块行为验收 | 专门编辑、推荐/路径、诊断/模型、审计、用户能力保留；共享导航变化不等于每项功能已验收 | 需逐模块真实角色/对象/保存或只读范围/恢复/公开读取证据，不能填“验证后保留”而没有执行记录 |

### 本子任务实际命令与失败记录

- `node --import tsx --test --test-force-exit tests/admin-collections-v306.test.mjs`：11 passed；不含浏览器或真实后端。
- 指定本次组件、route helper、测试和兼容page的`npx eslint ...`：退出0；`npx tsc --noEmit --pretty false`两次退出0。此时其他Agent仍在实施，不能替代最终冻结后的构建。
- `node --import tsx --test --test-force-exit tests/admin-collections-v306.test.mjs tests/admin-workflow-v280.test.mjs tests/admin-session-v305.test.mjs tests/admin-preview-v292.test.mjs`：初次28通过/1失败，旧测试静态要求page源中直接出现`#bibliography`，而新实现交由保留query的helper生成。已改为检查helper接入及实际生成hash；复核29 passed，退出0，约0.754秒。没有取消业务权限或降低行为断言。
- 两次apply_patch首次失败分别为同文件Delete/Add重复目标、一次长行上下文空格不匹配；均未部分写入。改为单文件Update精确替换后完成。记录不算代码测试失败。
- `playwright.admin-collections.config.ts`为11个构建后UI/HTTP fixture场景，覆盖65项/41项、身份、登录、权限及5个视口；尚未执行，不能声称通过。
- `playwright.admin-collections-live.config.ts`为5个真实loopback API/独立SQLite场景，覆盖45手工书目遍历与精确返回、45会话跨页预览、旧失败来源/原任务重试、Admin Owner边界和Reader拒绝。仅mock runtime API地址，没有伪造业务成功响应；尚未执行，不能声称真实流程通过。

### 发布前必须继续核对的模块

1. R08/R28的备份UI目前有归档/校验/失败与边界说明，不能从这些字段认定已有最近隔离恢复演练证据；需关联本次真实恢复记录。普通Admin不应为该卡发送Owner-only请求。
2. R03审计/统计的来源、时间范围与可回查事件，以及R17服务/模型的实际产出、过期检查和可恢复步骤，须分别核验。不能把新共同范围说明当作这些能力已实现。
3. R21/R22阅读路径与推荐、R02关于内容/R28品牌首页的实际持久与公开读取规则不同，需要真实保存/预览/前台读取；不能统一套书目CatalogRevision文案后认定全部一致。
4. R34/R37旧理论身份和兼容路径须在有效映射及无映射条件各走一次，避免改死链为一个能打开却选错节点的页面。
5. R05/R06手工新建后的return_to传递、结束会话到精确Edition、R15/R16预览返回及私人Reader数据保护，由主任务共同工作台/公开回归补齐。所有R行仍要求最终逐项结果，不能因只改共享导航而批量标验收通过。

## 旧实现退役记录

用户进一步授权对整个后台进行实质删减/重构后，已按运行调用核对退役以下不可达实现。没有删除API、数据模型、原件、失败记录或兼容URL；可从Git恢复源码。

| 旧实现 | 调用核验证据 | 实际替代 | 退役结果 |
| --- | --- | --- | --- |
| [metadata-review.tsx历史源码](https://github.com/WinstonMo0807/social-theory-library/blob/aa97727e344bca1e72f17c4a1efddcd4cb8cadb1/web/components/metadata-review.tsx)，2206行 | page与组件无运行import；只有旧静态测试读文件 | 现有WorkflowEditor及共享FieldAssistant/候选动作/Evidence；review单项薄重定向 | 已删除；候选字段、人工锁、拒绝、证据定位断言迁到实际共享合同 |
| [item-publication-control.tsx历史源码](https://github.com/WinstonMo0807/social-theory-library/blob/aa97727e344bca1e72f17c4a1efddcd4cb8cadb1/web/components/item-publication-control.tsx)，247行 | 唯一引用是上方不可达MetadataReview的type import；新PublicationDesk不使用旧控件 | 精确当前对象WorkflowEditor、PublicationDiff/History/Retry与权威状态 | 已删除；旧publish/withdraw后端兼容接口保留 |
| `admin-sections.tsx`中的LibraryAdmin及专属AdminUploadItem/status映射，170行 | 全仓无外部引用，真实R11已使用WorkLibrary | Work/Edition分页、文件历史及当前版本工作页 | 已删除；其未完成上传重试由UploadSourceContext/处理中心保留 |
| `knowledge-admin.tsx`中的TimelineAdmin，约12行大行JSX及专属类型 | 无路由或外部import | R39实际NormalizedTimelineAdmin | 已删除；现有规范时间线接口/证据字段保留 |
| `theory-system-admin.tsx`中的ReadingPathsAdmin及专属types/helpers，160行左右 | 无路由或外部import | R21实际ReadingPathWorkbench，保留稳定阶段/项目及媒体草稿 | 已删除；未删除真实ReadingPath模型或公开读组件 |

`rg`复核运行源中不再出现MetadataReview、ItemPublicationControl、LibraryAdmin、TimelineAdmin旧导出或ReadingPathsAdmin；NormalizedTimelineAdmin保留。旧静态测试不再围绕不可达实现检查字符串，改为共享candidate action、锁定禁用、拒绝理由、正文/页定位等实际函数行为。46项相关定向测试通过；一次文件写入被占用失败后先核对文件完整，再用单文件Update成功，未shell强写或丢失原修改。

## 当前版本选择与返回链增量

R11/R12、R06/R09的共同身份区新增PrimaryEditionControl。GET/POST使用原`catalog/admin/editions/{id}/primary/`：先明确读取影响，确认后复用同一request_key与fingerprint提交；当前未保存字段或其他操作进行中时不可切换。未取得合法当前版本、不具备发布权限、有未发布修订或冲突时显示后端阻断，不扩大Owner权限。结果分别报告listing_effective与projections_complete，链接原发布事件对应版本。

手工新建有return_to时传到真实session，默认旧URL不额外加参数。结束会话保留精确Work/Edition维护链接；无Edition会话回原UploadItem。完整预览先回准确编辑URL，再回该编辑URL中的原筛选/页码，不跳到默认第一本书。知识预览页面切换同样保留返回和对象参数。

新增`primary-edition-v306.test.mjs`三项分别验证同请求重试、错误Edition/指纹/阻断拒绝、预览→编辑→列表嵌套返回。连同状态和预览20项通过，tsc/eslint退出0。新增真实浏览器的手工返回与无效主版本阻断场景尚未运行；完整成功主版本切换待对应双活动版本隔离fixture及真实API演练，不能据纯函数测试宣称完成。

## A31 每入口的可执行证据索引

下面每项同时映射`admin-route-layout-v306-live.spec.ts`的`A31 all 43 real admin routes layout and hit targets at {360,390,768,1280,1440}px`中同R编号步骤。该布局套件先等待实际API响应和DOM布局，再测文档宽度、控件裁切、列交叠和中心命中遮挡，记录文字截断并对高风险对象截图。它是5个测试、215个路由/视口组合，不伪写成215个独立功能测试。**v306-full-2301已有执行通过证据；最新最终源码仍须组合复验。** R09/R19/R24原矩阵仅覆盖无Edition错误页，后续真实服务上传演练单列，不能混称同一测试。

索引缩写仅为阅读方便：

- `C-live` = `web/tests/admin-collections-v306-live.spec.ts`，前五项由主任务报告初次真实隔离API执行通过；后续新增项及删减后的最终源码需统一复核。
- `M-live` = `web/tests/admin-module-matrix-v306-live.spec.ts`，25项实际API模块测试，在v306-full-2301执行通过。测试名含完整R编号，不是只检查页面200；最新源码组合仍待复验。
- `W-live` = `web/tests/cataloging-session-v305.spec.ts`，既有真实隔离编目/媒体用例，本轮最终代码执行结果由主任务总账绑定。
- `K-live` = `web/tests/public-management-v306.spec.ts`，主入口公共模块和同对象诊断行为，结果由对应Agent/主任务记录。
- `API` = 下列精确后端pytest名称；这里只记录已存在可执行检查，不将历史结果记为本轮通过。

| 入口 | 可执行测试名或检查 | 仍需与本版绑定的限制 |
| --- | --- | --- |
| R01 | W-live `editor dashboard never requests admin-only statistics`、`ordinary administrator dashboard reads real usage statistics`、`queue transport failure is visible and retries the real API` | 全量队列counts与摘要使用新契约；旧测试需本轮执行 |
| R02 | M-live `A31 R02 real API list fields and rendered records agree`；API `test_knowledge_matrix_v231.py::test_about_page_reports_configured_even_when_a_slot_is_hidden` | 列表为空时仅验证真实空数据；实际保存—公开任务仍应核对 |
| R03 | M-live `A31 R03 statistics reflect real denominator and temporary read failure is recoverable` | 只证明现有匿名统计，不证明新增智能使用指标或逐事件审计已覆盖 |
| R04 | K-live `Studio never substitutes a different object when the requested identity is missing`；纯函数 `A31 legacy item redirects preserve edition, session, filters and nested return location` | 兼容跳转需结合本R布局步骤及目的对象确认 |
| R05 | W-live `non-superuser creates a real manual session, saves and reopens it`；C-live `A31 manual creation and preview preserve exact editor and nested list return` | 新增返回用例待跑；不把未保存输入当已落库 |
| R06 | C-live同上；API `test_cataloging_sessions_v305.py::test_normal_admin_can_create_reopen_and_read_session_without_writes` | 结束/放弃会话路径需并列核对 |
| R07 | M-live `A31 R07 real API list fields and rendered records agree`；API `test_knowledge_matrix_v231.py::test_future_discipline_appears_in_matrix_without_forcing_classification` | 专门编辑保存/公开沿原修订权限 |
| R08 | M-live `A31 R08 real API list fields and rendered records agree`；C-live普通Admin Owner边界测试 | 无真实对象存储时只能证明未配置与权限，不能称传输可用 |
| R09 | API `test_complete_ingestion_workflow.py::test_complete_chinese_pdf_http_ingestion_review_publish_and_withdraw`；`test_admin_workflow_v280.py::test_workflow_order_section_confirmation_and_curation_skip` | 本R布局源可能是无Edition错误条件；成功路径需上述真实上传/服务检查 |
| R10 | K-live `main Studio shows actual public modules and edits through a protected draft before explicit publication` | 必须主入口，不用诊断页替代 |
| R11 | C-live `A11 live API 45 manual works match browser pages and restore exact filter after editing`；fixtureUI `A11 41 works can be traversed with server paging and exact-version return links` | 初次真实45项通过；最后主版本增量后需重新绑定 |
| R12 | C-live A11精确当前版本返回；API `test_admin_workflow_v280.py::test_maintenance_workspace_and_mutation_stay_on_requested_edition`；Primary专项 | 文件替换/Page/私人数据另按A08/A28核验 |
| R13 | W-live `media upload keeps a private original and creates a responsive preview`；五类媒体专项spec | 新引用显示需对应真实媒体选择记录 |
| R14 | `person-resolution-v305.spec.ts`中的`owner merges, reloads the saved operation and rolls back through the real API`及响应丢失/冲突用例 | 仅非冲突合并；双Profile仍明确拒绝 |
| R15 | API `test_public_knowledge_control_v303.py::test_theory_protected_preview_exposes_real_secondary_page_inputs`；K-live编辑/预览/公开；本R布局步骤 | 返回到精确对象和page/module的新链需验证 |
| R16 | C-live新增手工嵌套返回；API `test_admin_preview_and_health_permissions_v292.py`；纯函数预览回链 | 预览不得执行Reader/下载/私人写操作 |
| R17 | M-live `A31 R17 workers list uses real task rows and does not fabricate completed recovery` | 只读任务/恢复资格，不代表真实Worker/OCR/Provider工作完成 |
| R18 | C-live `A10/A12 live unified queue pages contain all 45 manual records and their real sessions`；fixtureUI `A12 publication desk spans API pages and includes manual and maintenance sources` | 真实首批跨页已通过；发布控制由共同工作页实际操作覆盖 |
| R19 | 纯函数 `A31 legacy item redirects preserve edition, session, filters and nested return location`；本R真实跳转步骤；R09成功路径 | 无Edition错误不能当发布成功；完整请求上下文必须保留 |
| R20 | M-live `A31 R20 dictionary reports the active source generation and only Owner gets mutation controls`；词典事务/同步后端套件 | 本轮未执行正式reconciliation，不改活动索引 |
| R21 | M-live `A31 R21 real API list fields and rendered records agree`、`A31 R21-selected exact object is loaded into its actual editor`；`knowledge-media-v305.spec.ts`路径保存发布 | 真实阶段/项目ID、推荐填补及公开规则分别核验 |
| R22 | M-live `A31 R22 recommendation placements use real policies and selecting a policy is read-only`；API `test_knowledge_matrix_v231.py::test_recommendations_are_shared_rotate_together_and_accept_all_document_types`和人工排序/旧任务保护 | 选择只读与真正刷新/公开不同，不能混记 |
| R23 | C-live旧来源完整队列；fixtureUI `A10 older anomaly after 31 tasks remains visible through server filtering` | 分类在服务端完整范围先执行 |
| R24 | 纯函数兼容query测试；本R真实跳转步骤；R09复核保存API用例 | 不使用旧MetadataReview组件完成度代替共同工作页 |
| R25 | M-live `A31 R25 real API list fields and rendered records agree`；学者图片专项 | 规范Person/ScholarProfile身份仍分开 |
| R26 | M-live `A31 R26 exact object is loaded into its actual editor`；`scholar-portrait-v305.spec.ts::an editor selects, publishes and clears a scholar portrait without exposing the draft` | 不能由已保存肖像断定公开读取已更新 |
| R27 | M-live `A31 R27 semantic manager displays real eligible counts and ordinary Admin cannot switch indexes`；`test_semantic_index_v2_lifecycle.py`等后端生命周期 | 无模型和索引服务时不证明真实向量/检索功能 |
| R28 | C-live `A26 live ordinary Admin settings do not read Owner backup or Prompt registries`；API `test_distribution_backup.py::test_backup_job_records_runtime_manifest_and_checksum`及失败脱敏 | BackupJob仅有manifest/哈希/归档，无独立恢复演练确认字段；真实恢复由主任务证据补充 |
| R29 | M-live `A31 R29 system status exposes current source fields and a real refresh request` | 系统组件字段来源正确不等于每项外部服务可用 |
| R30 | M-live `A31 R30 real API list fields and rendered records agree`；API `test_knowledge_matrix_v231.py::test_public_subdiscipline_list_serializes_related_scholars` | 专门编辑数据与公开关联分别读 |
| R31 | M-live `A31 R31 old self-check renders every returned component without running a probe on open` | 没有运行自检时不得绿化产出；状态过期由原服务处理 |
| R32 | M-live `A31 R32 candidate diagnostics show returned candidate counts and preserve explicit decisions`；共享候选动作/来源页定位行为单测 | 通用诊断不替代当前字段的采用/拒绝操作旅程 |
| R33 | M-live `A31 R33 knowledge diagnostic keeps the exact object and returns to its Studio`；K-live `knowledge diagnostics are read-only and return to the same object in the sole Studio` | 旧重复编辑/发布实现已退役，权限与查询保留 |
| R34 | M-live `A31 R34 compatibility taxonomy preserves both source lists and canonical editor links` | 无映射的旧理论还需明确报原因，不能默认其他节点 |
| R35 | M-live `A31 R35 exact object is loaded into its actual editor`；API `test_theory_system_v231.py::test_editor_can_create_and_publish_node_without_superadmin` | 规范节点与兼容TheorySchool数据不可合表 |
| R36 | M-live `A31 R36 exact object is loaded into its actual editor`；`knowledge-media-v305.spec.ts::direct node opening protects input until the real record is ready` | 路径参数与query冲突、初载保护继续核对 |
| R37 | M-live `A31 R37 exact object is loaded into its actual editor`；规范理论相同发布API | 兼容入口不能默认换对象；无调用旧ReadingPathsAdmin已删除 |
| R38 | API `test_theory_system_v231.py::test_review_confirmation_drafts_then_publishes_relation_and_page_evidence`、`test_published_work_relation_direct_crud_requires_workbench_revision`；本R布局步骤 | UI具体新增/采用/拒绝/证据动作需与API结果合并核验 |
| R39 | API `test_theory_system_v231.py::test_timeline_and_graph_apply_backend_filters`、`test_knowledge_matrix_v231.py::test_public_timeline_and_graph_only_use_approved_evidence`；本R布局步骤 | 已退役旧TimelineAdmin；真实规范编辑仍在R39 |
| R40 | M-live `A31 R40 real API list fields and rendered records agree`；API `test_knowledge_matrix_v231.py::test_public_topic_list_filters_by_approved_discipline_relation` | Topic人工合并/冲突仍按Owner服务验收 |
| R41 | M-live `A31 R41 exact object is loaded into its actual editor`；K-live公共字段编辑/发布 | new与UUID输入不可混淆，错误对象不替代 |
| R42 | C-live `A10 live older unbound upload opens its original source and preserves identity on retry`；R09上传流水线API | 初次原来源retry202已通过；不把接受请求计为处理完成 |
| R43 | M-live `A31 R43 selected user values and Owner protection match the account API`；API `test_accounts.py::test_superadmin_can_set_new_password_but_cannot_read_old_password`；C-live读者拒绝 | 没有账户变更请求时只记读/选/拒绝，不称全部角色写入通过 |

以上索引覆盖43个R，无空白。2026-09-16核对`output/playwright/v306-full-2301/results.xml`：C-live 8项、M-live 25项、K-live 3项和43路由五视口5项均通过；共215个路由/视口组合。编目/媒体/肖像/人物后续20项通过，退出0，归档`output/playwright/v306-cataloging-regression-1454`。下方及上述索引中“待跑”描述是最初交接状态，以本段和表末当前结果为准。R09/R19/R24的原矩阵覆盖无Edition错误页，不能用布局通过证明上传完成；R38/R39具体UI写入、真实文件生命周期与备份恢复仍列明限制，A31未整项关闭。

### 统一构建前最后交接

- `npx playwright test --config=playwright.admin-modules-live.config.ts --list`实际列出30项：25个真实API模块/键盘场景，5个每项43路由的布局矩阵。列出不是执行通过。
- C-live现为8项：原5项，加手工创建/预览嵌套返回、未就绪版本拒绝切主版、成功主版本切换的响应丢失重试。`scripts/v306_e2e_fixtures.py`在原隔离SQLite路径guard内调用`tests.test_primary_editions_v306.pair()`建立两份初始合法活动快照；只把初始数据标为model fixture，切换和公开读取依然走真实API，不把模型种子称作发布流程验收。
- 最后20项主版本/返回/状态/预览单测通过，退出0；全仓tsc、相关eslint和diff检查退出0。旧预览测试硬编码直接return_url的断言已升级为safeAdminHref接入和合法fallback/外部路径拒绝行为。
- 路由数量再次核对为源码43、清单43。此刻未执行最终统一构建及新增30+8浏览器矩阵，没有部署或迁移生产。

### F01 最后上传入口核对

新鲜检索确认原AdminUpload仍把UploadItem.status=published称为“已经发布”。已将其限定为“发布决定已保存”，并在原书目元数据区域使用UploadPublicationResult读取同一`review_data.publication`，不增加请求或单独成功标记。只有共同结果明确有活动修订、public_state=published且listed_publicly=true时，才显示公开核验链接；非主版本、无Edition来源、未知/矛盾状态均不生成伪链接。Workflow折叠发布摘要与公开状态文字也改用相同presentation helper，发布/撤回按钮使用编辑决定的既有逻辑没有擅改。

`upload-publication-v306.test.mjs`以真实React服务端渲染验证无活动修订、合法公开版、非主版、无Edition、未知/矛盾响应及公开链接范围；并核对实际上传卡传入原API公共状态。与共同状态测试合计16 passed，退出0。定向eslint另发现共同工作台在effect内直接setPreviewExpanded，已将其与既有初次刷新一同放入原下一事件回调；复核退出0，仅保留3项既有内部导航warning。馆藏与发布台核验链接也使用同一publicationPublicHref，不在状态未知时继续给出公开链接；diff检查退出0。同期全仓tsc遇主任务正在编辑的Reader props/loadAttempt中间态错误，不能写本次全仓类型通过，须统一冻结后复核。

同次全后台检索另发现`knowledge_studio._work_status`仍聚合Edition.state，已交对应Agent改权威来源；AdminWorkPagePreview响应的旧publication_state/public_url仍取state，已交状态Agent收敛。其余知识节点、Topic、关系、候选和EditorialRevision中的published用于各自正式对象或编辑决定，不因字符串相同而删除合法发布动作。WorkDetailView目前不使用预览public_state制造公开链接，公开页面展示仍由正式接口控制。
