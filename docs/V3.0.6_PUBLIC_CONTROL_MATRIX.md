# 3.0.6 后台编辑与公开读取对照

首次核对：2026-09-13，实施前源码基线`aa97727`；当前入口复核：2026-09-20。本文件是源码审计与验收索引，不是第二份运行时字段注册表，不拥有内容，也不新增写入路径。下方保留实施前观察和阶段增量，不能把历史缺口或源码声明当当前缺陷/场景通过。

当前`PUBLIC_PAGE_CONTRACTS`已有32条页面/能力契约：学者9、理论7、主题8，加作品、独立阅读路径、学科、子学科、全站内容、推荐、媒体和Reader各1。最后8类是对原登记的扩展，不是新registry。当前封面、时间线证据、学科图片、推荐预览等公开消费者与保存语义见[管理端画像](ADMIN_ARCHITECTURE_PROFILE.md)，测试和线上边界见[最终交付](V3.0.6_COMPLETION_RELEASE.md)。下文“当前未登记/未实现/未测试”如属于较早日期，应按本段及实际源码解释，不能再次列作新缺口。

## 权威登记与读取规则

2026-09-20封面追加未新增公开字段或平行登记。编辑入口统一EditionCoverEditor，准备来源仍CoverCandidate/Asset；选择或上传仍Work.cover/cover_rendition及原CatalogFieldDecision，已公开修改仍EditorialRevision。公开API沿用WorkCardSerializer/PublicWorkCoverView及CatalogPublicationRevision，普通候选读取/预览/选择不激活公开快照。推荐卡片配图单独可选，不替代封面。默认样式与无封面文章允许发布。新封面API/图片预览受后台权限控制；16项PG（含旧公开稳定及正式发布读取）和浏览器选择/默认流程通过，公网匿名封面预览401通过。其他字段权威登记仍在public_knowledge_control.py，不复制为另一套运行时注册表。

2026-09-20剩余开发交付（未测试）：没有新增公开字段、公开API或另一份登记。知识表单预填仍是浏览器输入，保存才进入原实体/EditorialRevision；候选及来源在同一编辑事务中记录，学者译名补齐原PersonNameVariant结构化资料。预览明确仅展示已保存修改，发布仍调用既有修订资格服务。OCR任务标题属于后台只读展示，动态读取Work及编辑修订并标注未发布名称；不会据此公开草稿题名。全文更新仍走原Page/DocumentRevision/CatalogPublicationRevision及任务事件，不能用进度100%代替公开生效。用户本轮取消测试/检查，以上为实现映射，不是对前台消费者的新验收。

2026-09-20最新增量：时间线具体Edition/Asset/页序选择已完成，`timeline_evidence.py`批量核对原active_asset_q，主时间线/学者等既有公开事件消费者共用合法Reader链接。保存/发布双重校验，过时文件引用保留但不冒充可读；147 API及44浏览器组合有证据，下方缺少选择器的描述为历史。

新增人工全本OCR不增加管理员公开文本字段或第二份登记：编辑入口为当前Edition文件区和处理中心，持久化于既有ProcessingJob/Asset/Page/DocumentRevision，已发布新正文暂存，公开结果仍是CatalogPublicationRevision及原事件派送。原Reader私人内容不进入请求。新增OCR实现未测试，统一验收须核对Reader/检索消费者与旧公开稿/私人引用保护。

2026-09-20身份建议增量未增加公开字段或读取来源。作者/译者仍为当前Edition的Contributions与规范Person关联，出版社保留名称/PublisherAuthority；预填只在当前表单，显式保存走原EditorialRevision隔离，正式公开仍由既有CatalogPublicationRevision流程。未来数据准备仅复用候选/决定/日志，不采集Reader私人内容或推定训练许可。原public_knowledge_control登记未复制，相关来源、已公开修改隔离及同名职责检查见`test_workspace_edits_v306.py`与验证账本。

2026-09-20增量：登记原有的理论`?section=timeline`编辑入口已经接入实际TheoryNodesAdmin次级视图，按当前node ID查询，公开独立时间线继续按slug读取原API及同一事件关系。没有复制registry或另造时间线数据。真实浏览器已验证从理论编辑进入、默认关联、旧event/node/q/returnTo兼容、错误理论上下文拒绝；专门事件的草稿/发布/前台读取和五宽度延续同组测试，详见V3.0.6_VERIFICATION最新节。新建事件尚缺具体阅读文件选择，不能以Work+页码宣称Reader证据定位已完成。

继续以 `api/catalog/services/public_knowledge_control.py` 的 `PUBLIC_PAGE_CONTRACTS`、`SCHOLAR_MODULES`、`THEORY_MODULES`、`TOPIC_MODULES`、`SOURCE_AUTHORITY_MATRIX`、`ADMIN_FIELD_USAGE` 和 `LEGACY_PUBLIC_DEPENDENCIES` 为单一运行时登记。原学者/理论/主题页面契约为9/7/8，共24个；3.0.6已在同一源码扩展为32个。本文件按既有module_id说明，不复制并行registry；统计只是声明数量，不证明实际渲染通过。

登记的 `public_component` 有部分是模块名称，不是实际导出组件名。实际渲染组件见下表。`public_management_coverage()` 校验声明、路由解析和字段分类，没有执行浏览器读取；100%仅表示它所核对的声明完备，不表示A19/A20通过。

以下共享约定是每条模块记录的一部分。

| 代码 | 编辑、持久与草稿 | 公开资格、权限与更新时间 | 预览与核验 |
| --- | --- | --- | --- |
| S | `/admin/scholars/{id}`，`ScholarProfile`及其`Person`；专门编辑与Studio共用知识工作区接口。已发布变更进入`EditorialRevision`，肖像选择另记`EditorialRevisionMedia` | 编辑使用`IsKnowledgeEditor`/对应编辑能力，发布使用既有Authority发布权限；公开需要Profile published且Person verified。公开serializer读正式对象，不把编辑草稿替代正式对象；投影由现有事件更新 | `/admin/preview/knowledge/scholar/{id}?page=...`受控；比较旧公开GET、草稿preview、发布后GET及真实组件。接口`/api/catalog/scholars/{slug}/` |
| T | `/admin/theories/{id}`与关系/时间线专页，`KnowledgeNode`及规范关系；已公开对象经`EditorialRevision`，媒体另记草稿引用 | `IsKnowledgeEditor`，Authority发布能力不增加第二人审批；公开node及对应关系需published，受TheorySystemFeatureMixin现有开关约束；正文/证据另受原公开文档资格限制 | `/admin/preview/knowledge/theory/{id}?page=...`；详情`/api/catalog/theory-system/nodes/{slug}/`，图/时间线/路径为独立API，不能只验详情serializer |
| O | `/admin/topics/{id}`，`Topic`与已确认的规范关系；已公开变更经`EditorialRevision` | 既有Knowledge编辑/发布能力；公开Topic published，关联对象与关系也分别过滤；人工内容在正式发布后读取，关系生成内容随正式关系及关联对象资格计算 | `/admin/preview/knowledge/topic/{id}?page=...`；`/api/catalog/topics/{slug}/`，其中works/scholars/theories/passages由`TopicDetailView.retrieve`在serializer后补入 |
| W | 上传`/admin/intake/{itemId}`、手工`/admin/cataloging/{sessionId}`、维护`/admin/library/works/{workId}?edition=...`共用WorkflowEditor；Work/Edition保持身份分工，已公开修改经EditorialRevision | 书目编辑/发布由后端能力校验。公开需同Edition的active、metadata_ready的CatalogPublicationRevision，列表另需主版本；不得只看Edition.state。异步投影状态不等于书目快照激活 | 现有受控Work preview、publication prepare差异、正式Work/Edition API、实际WorkDetailView和Reader。对应A01-A09/A13-A18/A32 |

Source种类的控制范围：`editorial`是管理员可写正式字段；`curated`是人工选择与编排，不等于任意文本；`relation`应修改规范关系；`computed`来源于馆藏关系、公开资格或统计，不能提供写不生效的文本框；`ai_optional`只公开已人工策展的CuratedClaim，模型建议不自动成为事实。推荐的人工选择与自动轮换见R01-R02，统计见H02。阅读笔记、批注、书签、收藏、进度及私人书单不在管理员公开编目范围内。

## 现有学者模块逐项对照

各行继承S的编辑位置、草稿、权限、时机及受控预览；`section`直接对应现有注册的编辑栏目。主要组件路径为`web/components/public/scholar-public-view.tsx`和`scholar-section-public-view.tsx`，适配位于`web/lib/public-data-adapters.ts`，数据源位于`api/catalog/serializers.py:1628`。

| module_id / 编辑section | 持久字段或关系、公开来源 | 实际页面组件与显示条件 | 公开位置 / 逐项核验 |
| --- | --- | --- | --- |
| scholar-identity / identity | Person名称、年份、别名、肖像；Profile与Person关联 | ScholarPublicView的人物标题和肖像，PersonCompactSerializer输出；肖像还受媒体公开资格 | `/scholars/{slug}`；A19/A25/A32，修改Person及肖像先验旧公开不变 |
| scholar-position / overview | Profile.short_description/affiliations/key_concerns | ScholarPublicView按实际非空内容显示 | 主页；A19，分别填写三字段并检查真实DOM |
| scholar-biography / biography | Person.biography | ScholarSectionPublicView，section=biography | `/biography`；A19/A27，长中文/英文段落与空状态 |
| scholar-timeline / timeline | Profile.timeline | ScholarSectionPublicView，section=timeline；每行time+p两个子项 | `/timeline`；A19/A27，年份/正文正确两列，不复用三列 |
| scholar-quote / overview | Profile.featured_quote/quote_source | ScholarPublicView引文及出处，非空才显示 | 主页；A19，长出处和空引文 |
| scholar-works / works | approved Contribution +合法公开Work/Edition，由ScholarProfileSerializer.get_works计算；当前最多24部 | ScholarSectionPublicView section=works，当前`essentialWorks = curated.essentialWorks.length ? ... : works`，有策展作品时不会独立呈现全部馆藏 | `/works`；A19/A20。存在24条截断及策展覆盖全集的行为缺口，不能由模块名判定完整浏览 |
| scholar-representative-works / curation-works | Profile.curation.essential_work_ids，经公开Work过滤并保序 | ScholarPublicView代表作品、ScholarSectionPublicView作品页的优先数组 | 主页与`/works`；A19/A20，策展作品和全馆藏需分别验证 |
| scholar-concepts / curation-concepts | Profile.curation.key_concepts；前端还会回退规范KnowledgeNode概念关系 | 两个Scholar组件都读概念；section页三主体子项+可选small出处 | 主页与`/concepts`；A19/A27。登记curated单一类型未解释规范回退，需要完善同一registry |
| scholar-concept-map / curation-concept-map | Profile.curation.concept_map | ScholarSectionPublicView + KnowledgeMap；空值有说明 | `/concept-map`；A19/A27，关系节点、长标签、键盘 |
| scholar-network / relations | Profile.curation.network，经public_scholar_queryset解析；注册还声明PersonNodeRelation | ScholarSectionPublicView主要消费curated.network，不把任意PersonNodeRelation直接渲染为学者网络 | `/network`；A19/A20。登记关系存在不能代替实际network消费 |
| scholar-theories / knowledge-relations | PersonNodeRelation/WorkNodeRelation；规范theory优先，legacy curation.related_theory_ids及作品理论为回退 | ScholarSectionPublicView按规范→人工旧理论→馆藏旧理论选择；旧链接保留`/theory-schools/{slug}` | `/theories`；A19/A20，规范和legacy分开测，不能删除fallback |
| scholar-frequently-read / curation-related | Profile.curation.frequently_read_scholar_ids，经public Scholar过滤 | ScholarSectionPublicView section=frequently-read；这是人工关联阅读，不是读者行为统计 | `/frequently-read`；A19/A25，顺序与失效学者过滤 |
| scholar-curated-claims / claims | 已公开CuratedClaim及Evidence；DerivedClaim不是公开值 | ScholarPublicView + CuratedClaimSections；无人工确认内容则为空 | 主页；A15-A16/A19/A32，证据定位、草稿不漏 |

## 现有理论模块逐项对照

各行继承T。实际组件为`web/components/public/knowledge-node-public-view.tsx`、`theory-timeline-public-view.tsx`、`reading-path-public-view.tsx`及TheoryGraphExplorer。后端详情为`api/catalog/theory_serializers.py`，列表/时间线/图/路径为`api/catalog/theory_system_views.py`。

| module_id / 编辑section | 持久字段或关系、公开来源 | 实际组件与显示条件 | 公开位置 / 逐项核验 |
| --- | --- | --- | --- |
| theory-identity / identity | KnowledgeNode canonical_name_zh/en、aliases、image_selection | KnowledgeNodePublicView hero；外文名非空显示，图片用TheoryBanner/响应式媒体 | `/theories/nodes/{slug}`；A19/A25/A32 |
| theory-definition / definition | Node.definition/summary | 实际显示`definition || summary`，两字段不是同时作为两段显示 | node主页；A19/A20，分别测试优先级 |
| theory-core-questions / core-content | Node.core_questions | KnowledgeNodePublicView仅展示第一个问题 | node主页；A19/A20，登记完整数组不代表全部内容已显示 |
| theory-propositions / core-content | Node.basic_propositions/theoretical_boundary | KnowledgeNodePublicView显示前5命题及边界 | node主页；A19/A20/A27，超5项行为须明确不伪称全量 |
| theory-development / development | 登记Node.start_year/end_year/period_label；实际发展列表是TheoryTimelineEvent/TimelineEventRelation独立API结果 | KnowledgeNodePublicView使用timeline数组，非空显示前6条；TheoryTimelinePublicList显示事件 | node主页、`/theories/timeline?node={slug}`；A19/A20/A27。须在同一registry明确字段和事件两种来源，不能只检查Node年份 |
| theory-disciplines / knowledge-structure | KnowledgeNodeDiscipline、KnowledgeNodeSubdiscipline、primary_discipline及公开目标 | KnowledgeNodePublicView学科pills、Discipline理论目录 | node主页、`/theories/disciplines/{slug}`；A19，关系发布前后与目标资格 |
| theory-topics / knowledge-structure | KnowledgeNodeTopic及公开Topic | KnowledgeNodePublicView topic_links；TopicSectionPublicView规范理论 | node主页、Topic theory-schools二级页；A19/A20 |
| theory-scholars / scholars | published PersonNodeRelation +公开Scholar身份 | KnowledgeNodePublicView representative_scholars；有scholar_slug则链接，否则名称 | node主页及Scholar理论页；A19/A22，Person身份与Profile分别过滤 |
| theory-relations / relations | published KnowledgeRelation及节点资格 | KnowledgeNodePublicView direct_relations；TheoryGraphExplorer图节点/边 | node主页、`/theories/graph?center={slug}`；A19/A27，关系方向不翻转 |
| theory-works / works | published WorkNodeRelation、合法公开Work，按职责生成work_groups | KnowledgeNodePublicView groupedWorks + WorkCompactCard；空组不渲染，链接完整Explore | node主页；A19/A20，职责、列表资格和全部入口 |
| theory-evidence / evidence | EvidenceSnippet/EvidenceSpan与对应有效正文/访问资格 | KnowledgeNodePublicView evidence表含书名、关系、页码、原文、reader_href | node主页；A17/A19/A28/A32，定位实际PDF而非仅有URL |
| theory-reading-paths / reading | ReadingPathItem、published ReadingPath；注册为reading_path_count | KnowledgeNodePublicView实际过滤独立allPaths数组中node_data.id，不读取reading_path_count；ReadingPathPublicView展示完整阶段 | node主页、`/theories/reading-paths/{slug}`；A19/A20/A25，独立输入缺失必须失败 |
| theory-curated-claims / claims | CuratedClaim与Evidence | KnowledgeNodePublicView + CuratedClaimSections；debate节点保留相应组 | node主页；A15-A16/A19/A32 |

原登记中theory的discipline-entry/scholar-entry/topic-entry明确`preview_support=False`，不能将其公开链接标成已完成草稿预览。必须从所属对象进行预览，正式发布后再访问受影响页面。

## 现有主题模块逐项对照

各行继承O。组件为`web/components/public/topic-public-view.tsx`、`topic-section-public-view.tsx`。源码来源`api/catalog/serializers.py:1477`与`api/catalog/views.py:1915`须一起看，`TopicSerializer`并非完整详情响应。

| module_id / 编辑section | 持久字段或关系、公开来源 | 实际组件与显示条件 | 公开位置 / 逐项核验 |
| --- | --- | --- | --- |
| topic-identity / identity | Topic.name/description，hero_image另为现有字段 | TopicPublicView标题/介绍/图片 | `/topics/{slug}`；A19/A25/A32 |
| topic-problem / research-framework | Topic.problem_statement/core_questions | TopicPublicView问题模块，按非空显示 | 主页；A19/A27 |
| topic-framework / research-framework | Topic.formation_context/research_dimensions/methods | TopicPublicView框架与方法 | 主页；A19，各字段差异与读取 |
| topic-works / works | approved WorkTopicRelation +合法公开Work；registry记work_count，实际详情retrieve补works | TopicSectionPublicView works/recent使用策展数组优先，否则topic.works | `/works`、`/recent`；A19/A20，count与rows来源不同，须单独验证 |
| topic-evidence / evidence | Passage/EvidenceSpan，活动DocumentRevision及文件访问过滤；详情retrieve补passages | TopicPublicView原文卡片、Reader passage定位 | 主页；A17/A19/A28/A32；serializer声明缺passages不能据registry宣称已消费 |
| topic-curated-works / curation-works | curation.foundational_work_ids/recent_work_ids，经合法公开Work过滤和保序 | TopicSectionPublicView优先人工选择；当前不会同时显示完整关系馆藏 | `/works`、`/recent`；A19/A20/A25，人工与关系范围须解释 |
| topic-scholars / knowledge-relations | registry声明PersonTopicRelation及curation.related_scholar_ids；详情retrieve还合并approved贡献者派生学者 | TopicSectionPublicView实际读topic.scholars，非直接curated.related_scholars | `/scholars`；A19/A20，人工选择是否对该页生效需真实行为验证 |
| topic-theories / knowledge-relations | KnowledgeNodeTopic优先；TopicTheoryRelation→TheorySchool legacy supplement保留 | TopicSectionPublicView规范theory优先，否则topic.theories | `/theory-schools`；A19/A20。linked_theories登记与详情theories/适配的关系须行为核对 |
| topic-concepts / knowledge-relations | Topic.key_concepts；规范KnowledgeNodeTopic概念优先 | TopicSectionPublicView有规范concept时显示规范概念，否则Topic概念文字 | `/concepts`；A19/A27，不能同时编辑却不解释优先级 |
| topic-timeline / development | Topic.timeline | TopicSectionPublicView每行time+p，其中p包含标题及正文 | `/timeline`；A19/A27，两项结构及长文本 |
| topic-reading-paths / reading | Topic.curation.reading_paths内嵌人工路径及work_ids，不等于独立ReadingPath实体 | TopicSectionPublicView title/description/level/works；独立路径不会自动写入此JSON | `/reading-paths`；A19/A25，两种路径范围不能混淆 |
| topic-curated-claims / claims | CuratedClaim/Evidence | TopicPublicView + CuratedClaimSections | 主页；A15-A16/A19/A32 |

## 实施前未纳入等价模块契约的公开范围

下列编号仅是本次验收编号，不是第二个运行时注册表。实施时应在既有`public_knowledge_control.py`补足必要的通用契约，并以真实组件使用测试防漂移。

| 编号 / 编辑位置 | 持久数据与草稿 | 公开来源、接口、实际组件 | 显示条件 / 权限 / 更新时机 | 预览及核验 |
| --- | --- | --- | --- | --- |
| W01 书目身份及出版版本 / W | Work题名/类型/语言；Edition出版字段；CatalogingSession保留来源；已发布修改为EditorialRevision | CatalogPublicationRevision.snapshot；`/api/catalog/works/{slug}/`、`/editions/{id}/`；WorkDetailSerializer、WorkDetailView、BookCard | W公开资格；多版本及非主版本不可误报未发布；保存不泄露草稿，激活新快照后生效 | prepare差异/受控WorkPagePreview；A01-A07/A14/A32 |
| W02 责任者、知识关系、引用 / W的contributors/knowledge/curation | Contribution保留role和原始署名；WorkNodeRelation、WorkTopicRelation等，人工锁/字段决定；正式快照保存关联内容 | WorkCard/WorkDetailSerializer从active_catalog_snapshot读取并过滤已撤回分类；WorkDetailView、WorkCitationPanel、Reader关联栏 | 公开关系资格与角色分开校验；人物合并不得把译者改作者；公开修订激活后核验其他页面投影 | A07/A15/A18/A21/A28，实际作者/引用/关联页逐一查 |
| W03 作品目录、文件、阅读元数据 / W的file/reading | Asset/Page/DocumentRevision及Edition阅读策略、页标签/章节；ORIGINAL不可覆盖，私人锚点引用Page不可重建 | manifest/page/content、distribution受保护PDF；WorkDetailView目录与ReaderShell | 文件状态/校验/访问资格独立于OCR全文；私有读取权限不因管理员统一界面放宽；文件/正文切换沿现有修订服务 | A04/A06/A08-A09/A17-A18/A28-A29；PDF Range206/%PDF-、页ID与旧URL保护 |
| H01 首页、页眉页脚、About文案 / `/admin/settings` | SiteSetting(key=site_config)，当前PUT直接持久化并AuditEvent；没有EditorialRevision草稿 | `/api/catalog/site-config/`；Home、SiteHeader、SiteFooter、About；AboutPageBlock另由公开块API与专用管理API | GET公开、PUT IsLibraryAdmin；保存立即影响后续读取，不应错误标成等待发布。硬编码标题和装饰不属于可编目字段 | 现有页面读取可核验，无统一受控草稿preview；A19/A25/A26/A32。保存提示必须说明即时作用范围 |
| H02 首页馆藏数量及热门检索 / 由馆藏和检索汇总 | 合法公开Edition/Scholar/legacy Theory、Search统计；不是管理员可改数字 | `loadCatalogOverview`取`/catalog/works/`、`/catalog/scholars/`、`/catalog/theory-schools/`分页响应count；另有`/catalog/site-stats/`和hot-searches。Home stats与popularTags；热门无结果时回退主题/理论标签 | 数量由有效查询计算，不能以任意state标志或手写数字充数；公开GET；随数据和统计来源更新 | A01/A20/A24，记查询范围与统计口径；不把假数据当“动态管理” |
| R01 推荐规则与人工选择 / `/admin/recommendations` | RecommendationPolicy、RecommendationOverride、RecommendationSnapshot、RecommendationItem；现有规则保存无EditorialRevision，显式refresh生成新快照，旧快照保留 | `/api/catalog/recommendations/`；RecommendationsAdmin、Home、RandomRecommendation；共享本期选择，非私人画像 | IsLibraryStaff管理；公开目标需资格；current_snapshot在过期/缺失时可经原服务生成下期。策略改变、人工override和当前快照生效不是同一时刻 | 管理器显示当前快照并核对真实首页；A25/A26。新增统一preview必须调用原选择规则，不能另算假结果 |
| R02 作品推荐编排及推荐图片 / 工作台curation、媒体 | WorkRecommendationOverride +媒体selection；推荐图写入Work的编辑草稿，媒体资料有独立版本 | 原recommendation snapshot + Work推荐图公开接口/metadata；RandomRecommendation、推荐卡片 | 推荐是否入选与推荐图片是否发布是两个事实；图片不能越过活动书目快照；Staff/元数据编辑能力 | A07/A25/A32；核对当前引用、草稿、历史快照及真实位置 |
| P01 独立阅读路径 / `/admin/reading-paths` | ReadingPath、Stage、Item，公开后编辑进入EditorialRevision；image_selection复用EditorialRevisionMedia | `/api/catalog/theory-system/reading-paths/{slug}/`；ReadingPathPublicView，Theory详情另读路径列表 | IsKnowledgeEditor；path published且子项目规范对象/Work公开；正式发布后生效，项选择/排序/必读/前置条件/理由均保留 | 原Knowledge preview/object adapter；A19/A25/A32。Topic内嵌路径另见topic-reading-paths |
| M01 媒体全库 / `/admin/media`及edition/scholar/object上下文 | MediaAsset原件及来源许可；MediaRendition；EditorialRevisionMedia、CatalogPublicationMedia保护历史；现有媒体资料PATCH不直接改正式书目 | 专用受控rendition文件；公开cover/recommendation/portrait/knowledge-image按使用对象提供；ResponsiveMediaImage | CanAccessBackOffice读、CanEditMetadata写及对应对象权限；选择进入对象草稿，公开文件受已发布引用约束；元数据版本冲突检查保留 | A25/A32。实施前MediaListView最多60条，前端没有引用清单/翻页；修复结果见文末 |
| D01 学科与子学科目录 / 专门编辑页 | Discipline/Subdiscipline及规范关联；已发布修改使用既有编辑修订 | 公开disciplines/subdisciplines详情及Theory目录，相关Work计数/关联列表 | 编辑规范对象与关系，不可把计算的作品数当字段；公开对象过滤保持 | A19/A20/A31，补同一registry控制范围，实际目录及二级组件测试 |
| L01 兼容理论路由 / 规范Theory编辑与legacy定位入口 | TheorySchool/legacy关系及LegacyKnowledgeMapping，非第二套新写模型 | `/theory-schools/{slug}`及`/{section}`、图和时间线；旧公开组件/路由仍在消费 | 原公开过滤、原URL和角色不放宽；迁移映射未覆盖且读取未归零前不退役 | A19/A27/A31，映射目标/返回位置/过滤参数/旧概念出处，不能只断言路由存在 |

## F06-F10、F12 实施前源码复核

| 问题 | 当前证据与结论 | 必要处理及测试，不是通过记录 |
| --- | --- | --- |
| F06 | `knowledge_studio.py:997-1021`已给Scholar/Theory/Topic selection附public_control；`knowledge-workspace.tsx:125-191`主ObjectFields不消费它，StudioSelection类型亦未声明；专门`knowledge-object-context-panel.tsx:472`才渲染PublicPageTree。问题仍存在，不能说后端无能力 | 主Studio复用同一PublicPageTree与selection结果，不二次创建对象/登记；按当前对象看来源、空缺、草稿、影响、入口。A19执行主入口真实编辑/预览/发布，A20用“已登记但组件不消费”的变异样例证明会失败 |
| F07 | `system_health.py:825`的layers按2×probe.interval判断过期；`841-884`的capabilities/dependencies直接沿用历史status/四维布尔值，overall又基于capabilities，存在过期层unknown而卡片仍healthy的分歧。`functional-health-panel.tsx`只显示运行四维及时间；`processing-center.tsx:766`只有item任务能回intake，asset级无对象链接。当前对象及采纳记录联系不完整 | 统一只读freshness和失效状态；从当前Edition/知识对象已有任务、候选、决定/日志聚合影响与使用结果，不在GET发外部收费调用。A16/A17/A23/A24分开验证；有产出不标为准确 |
| F08 | `person-resolution-workspace.tsx:99-109`默认对比大量技术字段，作品/版本藏details；`186-187`结果以ID和计数为主。`person_merges.py:34`Owner限制，`237-260`幂等与双指纹/锁校验，`305+`后改冲突及回滚限制都存在 | 保留安全服务；显示作品题名、出版版本、署名职责、实际公开位置及异步公开更新链接；技术明细收进details。A21/A22不能只测文案，需真实非冲突/重复请求/后续修改阻断/受限回滚 |
| F09 | `web/tests/public-page-control-v303.test.mjs`主要readFile+正则；`knowledge-studio-v300.test.mjs:5`将诊断组件赋studioUrl，多项主体断言未覆主Studio。后端既有真实API tests存在，不应一概称全是静态；但无法据此证明主Studio完整旅程 | 增加真实主入口浏览器行为、记录API的draft/public差异和组件内容，保留静态检查作为辅证。A19/A20/A31/A32 |
| F10 | `editorial-v2.css:2717-2722`仍将timeline-detail-list和definition-list定义相同3列；Scholar/Topic timeline均time+p两项；`scholar-section-public-view.tsx:77`第四small出处未显式跨列；`2829-2834`窄屏p跨满也混用两种结构 | 在真实组件限定样式分开两列timeline和三主体概念+出处行；核对旧theory-schools概念页及所有五视口。A27真实路由修前/修后、列位置和完整文本，不只横向溢出 |
| F12 | `reader.server.ts:23`catch所有异常返回null；`reader/[assetId]/page.tsx:27-36`友好错误页将原因合并成处理/下架，不是直接return404。ServerApiError已经保留HTTP status，可复用 | 保留友好页面并分网络/限流/拒绝/不存在等可恢复原因；404本身不能可靠区分权限隐匿和不存在。A29需模拟503/429/403/404、保留页面定位参数重试；A28继续实际Reader回归 |

额外需要纳入统一语义的当前源码观察：`public_knowledge_control.py:_appearances`的作品数仍有`edition__state="published"`查询，与有效快照资格不同；`AdminRecommendationRefreshView._resolve_manual_target`对Work仍按state筛选。以上不能只给主书目状态打补丁而保留管理影响/推荐误判。

## 实施前验证边界

本子项只执行当前源码读取、精确符号检索、Git状态核对和本审计文档差异检查。没有运行Django/PostgreSQL、前端构建、浏览器、OCR、Provider或生产验证；本文件所有A编号是待执行映射，不计通过。路径存在不等于行为通过，历史版本测试数不计入3.0.6。

下一步最小复用顺序：先主线统一书目状态/身份及列表；随后把现有PublicPageTree接主Studio、统一现有健康快照过期语义、从现有字段决定/候选/任务记录提供对象使用结果、为人物预览与结果补业务使用位置；扩展原registry覆盖本表的缺口。它们不要求迁移、第二份数据或新分析系统。任何新增持久事件只有现有记录确实不足时单独说明。

兼容退役保持`LEGACY_PUBLIC_DEPENDENCIES`既定条件：映射完整、公开读取不再依赖且有实际观察期，才可提出移除；本版本不能因新面板存在就删除TheorySchool/Concept/旧关系/旧URL。工作台上下文保持work/edition/session/object，不用首条记录替换缺失对象。媒体、Page、DocumentRevision和人物历史只沿原服务处理。

## 2026-09-13 实施结果与验收状态

本子项没有新增model或migration，没有运行生产操作，没有更改人物合并、并发指纹、Owner校验及回滚执行器。公开读取、候选与媒体都复用原领域服务。应用React修改按React性能技能复用现有对象payload与组件；不为面板另发同一知识对象请求，也不在页面打开时运行外部探测。普通字段预取已核对为本地/已有候选聚合和缓存，保留原有行为。

| 范围 | 实际改动 | 验证及限制 |
| --- | --- | --- |
| F06 主Studio和专门编辑 | `knowledge-workspace.tsx`真正渲染selection.public_control、原PublicPageTree、实际位置及草稿差异；专门context panel使用相同树，避免重复呈现。无选择时也可展开首页/推荐/媒体/Reader管理范围 | 后端真实Studio payload和草稿不泄露API断言通过；主入口全旅程浏览器spec已交主线统一运行，未在本子项声称浏览器通过 |
| 单一公共契约 | 原PUBLIC_PAGE_CONTRACTS从24扩为32页面，新增Work、独立ReadingPath、Discipline、Subdiscipline、首页、推荐、媒体和Reader；仍是原文件同一注册表。source authority及字段使用解释同步复用 | 所有注册API解析及原Scholar/Theory/Topic字段覆盖回归通过；新增分类说明不意味着每个全站场景均已验收 |
| 源与实际消费者 | TopicDetailView抽出原公开详情producer，Studio复用它取得works/passages等真实补充字段；理论控制状态使用原secondary preview的真实时间线/路径数组，不再用一个数量假定组件有内容 | 新Topic公开API与Studio公开perspective的同对象works逐项相同；保护预览的原来源测试通过 |
| F07 新鲜度与服务 | `system_health.py`的layer/capability/dependency/overall用同一probe寿命；过期状态unknown且四维null，保留实际检查时间与原record ID。functional-health-panel明确过期及“有产出不是准确” | 过期、未检测、真实失败的后端行为测试通过；GET未调用run_health_probe。没有替用户解暂停任务 |
| F07 使用记录 | selection.assistance_usage按当前对象的EnrichmentCandidate完整查询统计pending/accepted/rejected/superseded，并列AuditEvent与候选原始记录查看；明确分子、分母、起止范围 | 正反对象隔离、原事件ID、无数据、采纳率非准确率均通过。统计范围明确仅字段补全候选，未伪称覆盖全部Metadata/Entity/Claim来源；修改后采用、撤回、抽检、活跃人工耗时和成本无可靠记录时为null，无虚构指标 |
| F08 人物业务影响 | 原preview/record响应旁补`person_business_impact`，默认显示作品题名、版本出版信息、作者/译者/编者职责、准确Edition维护链接、合法公开位置、原事件逐消费者状态及恢复入口；技术明细保留折叠 | 业务装饰不加入指纹，既有非冲突合并、幂等、双档案/后改冲突、回滚测试通过；新增测试核对译者不改作者且无有效修订不显示假公开链接 |
| F09 实际行为证据 | 增加后端真实payload/公开读取/字段分母/61条媒体遍历测试；`public-management-v306.spec.ts`从真实主Studio走保存、受控预览、正式发布及公网页读取，断言组件实际消费修改值；旧Person浏览器测试转验默认业务影响区 | 不能以静态字符串代替场景；本子项只运行指定后端、TS、ESLint和拒绝理由行为单测；浏览器由主线统一记录 |
| 不采用理由 | 共享CandidateDecisionBar增加人物不符/版本不符/来源不可靠/内容不支持/其他，明确确认后用原descriptor/API记录reason；其他理由必填 | 行为单测验证5种body互异、保留原endpoint/action/candidate、原descriptor不被修改；服务与人工锁不变 |
| 媒体全库及引用 | 原Media服务增加只读真实引用清单；原detail返回当前公开/对象选择/待发草稿/历史引用及对象链接。新增分页collection复用原模型与权限，MediaLibrary可遍历全部并读取当前选图的引用；原无分页list为兼容保留 | 61条完整遍历、默认旧60条响应、坏页码、当前肖像+草稿并存及原文件checksum不变测试通过；新collection加入schema导出白名单，最终生成由主线统一执行 |
| 处理任务回到对象 | ProcessingCenter原job响应带Work/Edition和当前版本入口，前端优先该入口；无Edition的上传保持来源链接，不能用另一本书替代 | TypeScript/ESLint通过；原业务任务和动作不变。处理中心全量分页/总数等非本子项改动范围由主线验收 |
| 推荐及影响计数 | 公开影响中的作品count改用有效修订谓词；人工推荐选择也拒绝仅state=published的Work和未核验Person的Scholar | 追加负例；没有改变推荐快照、策略保存或轮换时机，也没有开启另一推荐系统 |

实际执行记录：

- 第一组54项为53通过、1失败；失败是新测试fixture未满足`enrichment_candidate_keys_not_empty`及accepted authority约束，补齐合法fixture，不删除约束。
- 随后指定8文件90项通过，退出0：`test_public_management_v306.py`、`test_functional_readiness_v305.py`、`test_public_knowledge_control_v303.py`、`test_person_resolution_ui_api_v305.py`、`test_person_merge_v305.py`、`test_media_v305.py`、`test_scholar_portrait_media_v305.py`、`test_knowledge_media_v305.py`。
- Topic producer与真实理论输入改动后，`test_public_management_v306.py`及`test_public_knowledge_control_v303.py`22项通过，退出0。XML位于`output/verification/v306/public-management-final.xml`，从临时`.local`单文件迁入，原件未删除，报告不提交Git。
- `node --import tsx --test tests/public-management-v306.test.mjs` 1项通过，退出0；`npx tsc --noEmit --pretty false`通过，退出0；指定11个相关UI/动作文件的ESLint通过，退出0；`git diff --check`通过。
- 后端命令明确使用`DATABASE_URL=sqlite:///:memory:`，NAS测试路径在系统临时目录，媒体文件测试另外使用pytest tmp_path。SQLite通过不等于PostgreSQL并发、真实Worker、OCR、Provider、公网或生产验证。最后追加的推荐负例和统一集成结果由后续记录补充。

尚未被本子项单独验证：主Studio浏览器最终截图、五视口视觉、PostgreSQL全流程、真实服务、所有角色的每个全站操作与正式部署。不得据上述局部结果认定A19-A32全部通过；F10/F12由主线独立实现和验证。所有旧公开路由、候选记录及媒体/人物历史均保留，没有退役数据表。

### 新授权后的重复实现退役

用户明确允许重做后台并要求不要保留无用旧实现后，已核对`KnowledgeWorkspaceDiagnostics`唯一实际入口为`/admin/system-health/knowledge`；它原本又持有一整套候选决定、修订发布、Canonical表单摘要和Preview展开，与主Studio重复。现将该文件623行收为175行，移除448行重复交互实现，保留被主Studio引用的共享类型、原诊断路由/对象参数、投影来源修订、原文定位、机器非正式记录和只读修订。诊断页不再拥有POST候选/发布命令，返回链接保留准确对象。未删业务API、模型、原资料或真正专业诊断。

对应旧静态检查已改为读取实际主Studio；新诊断检查要求只有useApiResource读路径、禁止apiRequest/候选决定/发布handler，保留来源修订及返回上下文。两文件10项Node检查通过，TypeScript和诊断组件ESLint通过，均退出0；新增真实浏览器spec核对诊断不写入并回到同一Studio，交由主线统一运行。

主线首组28浏览器为27通过、1失败；本子项已读取失败trace，403实际是正常CSRF安全校验失败，不是业务权限错误。测试通过正常网页登录取得csrftoken后，明确发送X-CSRFToken和原同源Origin/Referer修正测试fixture创建请求，没有改鉴权或关闭安全校验。更新后的主Studio场景需由主线复核。

最后推荐资格负例和本子项10项后端检查全部通过，退出0，完整XML为`output/verification/v306/public-management-final-scope.xml`。原22项报告已从临时`.local`移到`output/verification/v306/public-management-final.xml`，文件仍可恢复；没有删除报告。上述文件不应提交Git。

### 公开布局补充只读复核

用户补充要求检查Reader及全部潜在排版问题后，本子项只读核对Reader之外的规范理论、路径、学者/主题、关系卡和页脚。依据当前Web Interface Guidelines检查长文本、网格、可聚焦目标及粘性遮挡；未修改公开CSS或Reader。本节为源码风险，不是已经复现的浏览器缺陷。

- `web/app/editorial-v2.css:2941`：理论/路径hero存在`overflow:hidden`，`.theory-node-intro h2`没有对应长英文断行规则。只测文档scrollWidth可能漏掉被hero裁掉的文字。
- `web/styles/features/knowledge/theory-system.css:258`：阅读阶段为110px、至少280px和至少360px三列，仅≤820px改两列；821–900px还需加上48px列间距和后置v2内边距，容易超过可用宽度。小屏阶段标题及目标卡`1fr`轨道亦没有可靠的min-content缩小约束。
- 同文件`:113`、`:135`、`:266`：核心问题、关系卡与路径目标采用固定/auto加`1fr`轨道，长无空格词的容器缺`min-width:0`/明确断行，可能挤走旁边图标、标题或操作。
- 同文件`:144-147`、`:279-280`、`:307-308`：五子项证据表在三列/两列断点自动流入后续行，需实测列分配、页码/操作可见和来源说明，不把有横向滚动或裁切当作通过。

新增`web/tests/public-secondary-geometry-v306.spec.ts`共22项待主线统一执行。规范Theory/独立Path覆盖360、390、768、860、1280、1440，额外860检查断点空窗；Scholar/Topic主页和Footer覆盖要求的五视口。DOM Range用于比较实际文字与元素/父容器边界，并检查阶段内部列、可见点击目标及Footer键盘顺序。路径必须有真实ReadingPathItem，只有空Stage时明确失败，不制造无内容通过。新增spec后TypeScript和diff检查退出0；没有在本子项运行浏览器或将源码风险宣称为实测结果。

### 补充布局红测及正式修复（等待统一构建后复核）

随后经主线授权，使用既有隔离API和已构建应用实际运行`npx playwright test --config=playwright.v306.config.ts tests/public-secondary-geometry-v306.spec.ts`。22项为12通过、10失败，命令退出1；用例时间17.764秒，总运行约1.6分钟。源码审计风险由此分为已复现和仍需内容样本核对，不能只沿用审计推论。

完整原始截图、trace、DOM Range测量和XML已复制保留在`output/playwright/v306-secondary-before`；本次built CSS `index-7e9VPUHX.css`的SHA256为`B78943B17DB05A037C28FB080ACD2A4C8E1F00BFF7911CF1417811ECDCA40E64`。已实际查看Theory1280、Scholar360、Topic360截图，确认可见文字/肖像越界或被裁切，不是仅测试阈值问题。

| 真实页面与视口 | 实际红测 | 根因与正式修改 |
| --- | --- | --- |
| `/theories/nodes/v306-layout-node` 1280/1440 | 外文H2文字right约2231/2240，元素right约610/690；文字伸入图片或被hero裁掉 | 规范理论文本`overflow-wrap:anywhere`及布局子项min-width:0；关系卡和阅读顺序同步改为可收缩轨道，保留原字重/字号/内容 |
| `/theories/reading-paths/v306-layout-path` 860/1280/1440 | 阶段长词right约1551/1532/1537，元素right约519/624/659 | 阶段轨道用minmax(0,fr)，≤1180两列、≤560清楚堆为单列；长词正常换行，不缩小字号、不裁掉文字；路径hero的dt/dd用成对两列 |
| `/scholars/v306-layout-scholar` 360/390 | h1元素right511；肖像和简介可见部分越出卡片 | 窄屏英雄区显式minmax(0,1fr)，各子项min-width:0；生平摘要正文轨道可收缩，长名/机构/关切正常换行 |
| `/topics/v306-layout-topic` 360/390/768 | h1元素right约1031/1031/1049；长概念标签撑宽整列 | 限定Topic hero的子项及长概念链接收缩和换行；保持全部概念及原目标链接，不用overflow隐藏达标 |

证据表的桌面/平板/手机列位改为显式grid-areas，保持书名、关系、页码、原文、Reader操作；窄屏使用真实字段标签，补ARIA row/cell/columnheader，Reader仍是可聚焦link。移除原关系摘要/证据正文的硬性行数裁切；单个基础论述模块跨满可用列，不留下第二空列。未删除模块、路由、数据或原件。

修改范围为`theory-system.css`、`detail-pages.css`、`editorial-v2.css`对应知识hero/学者摘要规则，以及`knowledge-node-public-view.tsx`证据单元。未改Reader和主线F10的2717附近规则；共享Work样式保持原值。TypeScript、三个样式文件PostCSS解析及diff-check均退出0。此时尚未重新构建或宣称22项已修复通过，等待主线统一构建后实测。

### 六项能力共享状态收口（2026-09-13，进行中）

本子项沿用现有文件计划，限定修改`catalog_availability.py`及其workspace/queue/Studio接入和新增测试；主线负责根进度和统一验收。已核对未提交的同源响应和Studio `_work_status`，后者已按有效活动公开修订判断，不再读取单独state当成功。接下来先补来源/健康边界测试，再修共享判定，最后运行隔离SQLite的新增能力、v306状态与功能健康三组。不得用本子项结果代替全站、真实文件字节、OCR、Provider或生产验收。

当前审计发现需要重点验证：合法同Document的旧索引来源可延续，但不能跨Edition借用；投递完成与必要投影、来源版本必须一致；全局Reader健康只检查一份样本，不能当成每本PDF的读取证据；过期/未来/未完成检查不能显示当前绿色。纯书目应为不适用，pending/invalid文件不能通过。页面读取不得探测网络/文件、派发任务或读取私人读者记录。
