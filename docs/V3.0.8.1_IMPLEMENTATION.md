# 3.0.8.1 实施与验收记录

版本目标是修复管理员原文七项使用问题，截图只辅助定位，不将截图编号替代需求。

3.0.8.1已于NAS2026-09-23T22:23:34+08:00上线，2026-09-24公网8项检查通过。准确源码、镜像、七项交付、首轮回退和剩余验证边界见[发布记录](V3.0.8.1_COMPLETION_RELEASE.md)。

| 要求 | 可验证行为 | 当前状态 |
|---|---|---|
| 1 错误上传无法退出待办 | 上传详情与列表可修改、替换、删除；可从回收站恢复；迟到任务不复活记录 | 已上线，生命周期测试通过 |
| 2 选PDF页与手工图片封面保存失败 | 两条路径独立保存；其他未保存字段保留；冲突不丢选择 | 已上线，接口/冲突测试通过，真实浏览器未执行 |
| 3 提示不明且不能定位 | 提示说明具体问题、影响和动作；点击聚焦正确字段；未保存改动与旧校验分开 | 已上线，自动检查通过，真实点击待核实 |
| 4 作者新建入口隐藏 | 每行可选择已有或新建人物，无需进入填写建议 | 已上线，人物回填测试通过 |
| 5 译者关联/重复/延迟 | 新建回填原行，重复提交安全，创建候选立即可选，保存与未保存清楚 | 已上线，重复提交及权限测试通过 |
| 6 人物整理无删除/合并 | 删除可恢复；现有权限下合并可达、有影响预览与历史；同名不自动合并 | 已上线，引用保护及回收测试通过；合并仍限所有者 |
| 7 学者原名与文献长简介 | 首页显示已知原名，长简介占完整宽度、可展开 | 已上线，SSR/构建通过，视口对照未执行 |

追加范围：各同类管理对象删除与下架分开；OCR可靠运行和真实状态；NAS API/推理内存与性能；观点检索速度、召回与NAS支持更大模型的实测评估。原件、人工确认、私密数据、原文定位与权限保留。以下中间记录保留当时状态，最新上线结论以上方发布记录为准。

2026-09-23 中间实现记录：已写入人物内联创建及持久请求回执、封面保存解耦、字段高亮与未保存校验、可恢复回收站及主要入口、学者原名和全文简介展开；OCR 单页请求、服务去重与队列状态、worker heartbeat 及相同 pending task wakeup。新增迁移 catalog0063/ingestion0019，尚未集中验证或部署。NAS 12:59 只读复核：N5105、7810 MiB RAM、available1350、SwapUsed4376、API534.8、Inference799.1 MiB。

## 已执行检查

- Django check、migration drift、TypeScript、生产Web构建通过。受影响lint无错误，workflow-editor保留两项既有导航警告。
- 人物/请求键4项、相关SSR4项；后端首次75项70通过5失败，根因修正后的生命周期/队列18项通过；追加编辑发布25项、OCR/检索13项通过。NAS隔离PG41项、OCR服务7项通过。证据保存在本地忽略目录output/verification/v3081及NAS发布记录。
- NAS备份SHA256为55bd532abf4ae648ac368f991875fde0b27ce2e6106c952d53825facff000707；已隔离恢复、应用两项迁移、新旧ORM保护摘要一致。
- API相同3 worker、相同数据库样本与4并发请求，Docker内存586.4→514.3MiB。24请求首轮4.79→6.08秒，随后两轮3.36/3.18→2.92/2.78秒。不能将有限对照说成全场景绝不退步；恢复GUNICORN_PRELOAD=0入口保留，并修复该模式启动初始化。
- 推理旧/新均在NAS、CPU上限2、相同FP32模型和文本运行3轮，向量/排序逐值相同。热态总时长13.23–14.73→9.83–9.89秒，RSS815356→831260KiB（受换页影响，未证明降低）。本次不扩模型、不量化、不减worker；召回质量尚无人工标注基准。
- 本地页面服务自动审批返回blocked by policy，依用户明确授权不做真实浏览器验收，不虚构UI已实测。后续镜像、上线时间、实际OCR与公网结果另记。
